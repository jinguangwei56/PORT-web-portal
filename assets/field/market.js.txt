/* FONKON Field Market Development V1 */
(function(){
  const API=U+'/functions/v1/field-development',BUCKET='field-evidence',E=window.esc||((v)=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))),D=v=>v?new Date(v).toLocaleString('zh-CN',{month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit'}):'-';
  const LEVEL={quick:['快速记录','20–30秒，只记识别与结果'],effective:['有效沟通','2–5分钟，记录货、路、痛点与下一步'],priority:['重点客户访谈','15–20分钟，完整事实与可验证下一步'],office_visit:['办公室拜访','门口照片可单独补充，并同步正式拜访']};
  const INTEREST={unknown:'待判断',brief:'简短交流',effective:'有效沟通',follow_up:'愿意跟进',priority:'重点机会',rejected:'明确拒绝'};
  const FS={data:null,zone:'',timer:null,aiExtraction:null,sourceMethod:'manual',teamDays:30,loadedAt:0,loadFlight:null,dirty:false,screen:'mine',lastApiMs:0,restored:false,currentLocation:null,locationFlight:null,locationPermission:'unknown',locationPermissionHandle:null,mapSdkFlight:null,mapConfigFlight:null,mapInstances:new WeakMap(),placeCache:new Map(),mapHydrationTimer:null};
  const snapshotKey=()=>ses?.user?.id?'fonkon_field_snapshot_'+ses.user.id:null;
  function restoreFieldSnapshot(){if(FS.data||FS.restored)return;FS.restored=true;try{const key=snapshotKey(),x=key?JSON.parse(sessionStorage.getItem(key)||'null'):null;if(x?.user_id===ses?.user?.id&&x?.data&&Date.now()-Number(x.saved_at||0)<600000){FS.data=x.data;FS.loadedAt=Number(x.saved_at||0)}}catch{}}
  function saveFieldSnapshot(){try{const key=snapshotKey();if(key&&FS.data)sessionStorage.setItem(key,JSON.stringify({user_id:ses.user.id,saved_at:FS.loadedAt,data:FS.data}))}catch{}}
  function clearFieldSnapshot(){try{const key=snapshotKey();if(key)sessionStorage.removeItem(key)}catch{}}
  const style=document.createElement('style');style.textContent=`
  .field-hero{background:linear-gradient(135deg,#06364a,#0f766e);color:#fff;border-radius:22px;padding:20px;box-shadow:0 18px 42px rgba(6,54,74,.2)}.field-hero h2{margin:3px 0 8px;font-size:25px}.field-hero .muted{color:#c9f7f0}.field-hero-top{display:flex;justify-content:space-between;gap:12px;align-items:flex-start}.field-live{display:inline-flex;align-items:center;gap:7px;background:rgba(255,255,255,.15);padding:7px 10px;border-radius:999px;font-size:12px;font-weight:800}.field-live:before{content:"";width:8px;height:8px;border-radius:50%;background:#5eead4;box-shadow:0 0 0 6px rgba(94,234,212,.15)}
  .field-kpis{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px;margin-top:14px}.field-kpi{background:rgba(255,255,255,.12);border:1px solid rgba(255,255,255,.13);border-radius:15px;padding:11px}.field-kpi b{display:block;font-size:22px}.field-kpi span{font-size:11px;color:#ccfbf1}.field-actions{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px;margin:14px 0}.field-action{min-height:92px;border:1px solid var(--line);background:#fff;border-radius:18px;padding:14px;text-align:left;color:var(--ink);cursor:pointer;box-shadow:0 8px 20px rgba(15,23,42,.05)}.field-action b{display:block;font-size:15px;margin-bottom:5px}.field-action span{display:block;font-size:11px;color:var(--muted);line-height:1.45}.field-action.primary{background:linear-gradient(135deg,#0f766e,#0891b2);color:#fff;border:0}.field-action.primary span{color:#d5fffa}
  .field-zones{display:flex;gap:8px;flex-wrap:wrap;margin:12px 0}.field-zone{border:1px solid #cbd5e1;background:#fff;border-radius:999px;padding:9px 14px;font-weight:800;color:#475569;cursor:pointer}.field-zone.active{background:#0f766e;color:#fff;border-color:#0f766e}.field-evidence-row{display:flex;gap:8px;flex-wrap:wrap}.field-evidence-chip{border:1px solid #bae6fd;background:#f0fdfa;border-radius:12px;padding:8px 10px;font-size:11px;cursor:pointer}.field-evidence-chip.missing{background:#fff7ed;border-color:#fed7aa;color:#9a3412;cursor:default}
  .field-feed{display:grid;gap:9px}.field-feed-item{display:grid;grid-template-columns:76px 1fr auto;gap:10px;align-items:start;padding:11px;border:1px solid var(--line);border-radius:14px;background:#fff}.field-level{display:inline-block;padding:4px 7px;border-radius:999px;background:#ecfeff;color:#0e7490;font-size:10px;font-weight:900}.field-feed-main b{font-size:13px}.field-feed-main div{font-size:11px;color:#64748b;margin-top:4px}.field-empty{padding:20px;text-align:center;color:#64748b}.field-start{max-width:920px;margin:auto}.field-photo-preview{width:100%;max-height:220px;object-fit:cover;border-radius:15px;border:1px solid var(--line);display:none;margin-top:8px}.field-location{padding:10px;border-radius:12px;background:#f8fafc;border:1px solid var(--line);font-size:12px}.field-location.good{background:#ecfdf5;border-color:#a7f3d0;color:#065f46}.field-location.warn{background:#fff7ed;border-color:#fed7aa;color:#9a3412}.field-location-card{padding:13px;border-radius:15px;background:#f8fafc;border:1px solid var(--line)}.field-location-card.good{background:#ecfdf5;border-color:#6ee7b7}.field-location-card.warn{background:#fff7ed;border-color:#fdba74}.field-location-head{display:flex;justify-content:space-between;gap:10px;align-items:center}.field-location-head b{font-size:14px}.field-location-badge{padding:4px 8px;border-radius:999px;background:#e2e8f0;color:#475569;font-size:10px;font-weight:900}.field-location-card.good .field-location-badge{background:#a7f3d0;color:#065f46}.field-location-card.warn .field-location-badge{background:#fed7aa;color:#9a3412}.field-location-detail{margin-top:7px;color:#64748b;line-height:1.55}.field-map-shell{margin-top:10px}.field-map-preview{position:relative;overflow:hidden;height:280px;border:1px solid rgba(148,163,184,.35);border-radius:14px;background:linear-gradient(135deg,#e0f2fe,#ecfdf5);isolation:isolate}.field-map-shell.compact .field-map-preview{height:210px}.field-amap-host{display:block;width:100%;height:100%;background:#e2e8f0}.field-amap-host[hidden],.field-map-placeholder[hidden]{display:none}.field-map-placeholder{position:absolute;inset:0;z-index:5;display:grid;place-items:center;padding:24px;text-align:center;color:#64748b;background:linear-gradient(135deg,#e0f2fe,#ecfdf5);font-weight:800;line-height:1.6}.field-map-placeholder:before{content:"⌖";display:block;font-size:34px;color:#0f766e}.field-map-address{display:flex;gap:10px;align-items:flex-start;padding:11px 12px;margin-top:8px;border:1px solid rgba(148,163,184,.28);border-radius:13px;background:rgba(255,255,255,.88);color:#0f172a}.field-map-address-icon{display:grid;place-items:center;flex:0 0 30px;width:30px;height:30px;border-radius:10px;background:#ccfbf1;color:#0f766e;font-size:18px;font-weight:900}.field-map-address div{min-width:0}.field-map-address b,.field-map-address span{display:block}.field-map-address b{font-size:13px;line-height:1.45}.field-map-address span{margin-top:3px;color:#64748b;font-size:11px;line-height:1.45}.field-current-marker{position:relative;width:24px;height:24px;border-radius:50%;background:#1683ff;border:4px solid #fff;box-shadow:0 2px 10px rgba(15,76,148,.42)}.field-current-marker:after{content:"";position:absolute;inset:-10px;border-radius:50%;border:2px solid rgba(22,131,255,.28);animation:field-pulse 1.8s ease-out infinite}@keyframes field-pulse{0%{transform:scale(.55);opacity:.9}100%{transform:scale(1.35);opacity:0}}.field-location-meta{display:flex;gap:8px;flex-wrap:wrap;margin-top:9px}.field-location-meta span{display:inline-flex;align-items:center;padding:6px 9px;border-radius:999px;background:rgba(255,255,255,.82);border:1px solid rgba(148,163,184,.25);font-size:11px;color:#475569}.field-location-actions{display:flex;gap:8px;flex-wrap:wrap;margin-top:10px}.field-location-actions .btn{min-height:38px}
  .field-form-note{background:#eff6ff;border:1px solid #bae6fd;color:#0c4a6e;border-radius:13px;padding:10px;font-size:11px;line-height:1.55}.field-voice{display:flex;gap:8px;align-items:center}.field-voice textarea{flex:1;min-height:92px}.field-ai-confirm{display:none;background:#ecfdf5;border:1px solid #a7f3d0;border-radius:12px;padding:10px;color:#065f46}.field-analysis{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}.field-analysis-box{border:1px solid var(--line);border-radius:15px;padding:13px;background:#fff}.field-analysis-box h4{margin:0 0 8px}.field-analysis-box ul{margin:0;padding-left:18px}.field-analysis-box li{margin:5px 0;font-size:12px;line-height:1.45}.field-bars{display:grid;gap:8px}.field-bar{display:grid;grid-template-columns:100px 1fr 34px;gap:8px;align-items:center;font-size:11px}.field-bar-track{height:8px;border-radius:99px;background:#e2e8f0;overflow:hidden}.field-bar-fill{height:100%;border-radius:99px;background:linear-gradient(90deg,#0f766e,#06b6d4)}.field-team-table td,.field-team-table th{white-space:nowrap}.field-tabs{display:flex;gap:8px;flex-wrap:wrap}.field-tabs .btn.active{background:#0f766e;color:#fff}.field-warning{color:#9a3412;font-size:11px}.field-inline-checks{display:flex;gap:8px;flex-wrap:wrap}.field-inline-checks label{display:flex;align-items:center;gap:5px;padding:8px 10px;border:1px solid var(--line);border-radius:10px;background:#fff;font-size:12px}.field-inline-checks input{width:auto}.field-loading{padding:35px;text-align:center;color:#64748b}.field-photo-large{max-width:100%;max-height:65vh;border-radius:14px;display:block;margin:auto}.field-governance{font-size:11px;color:#64748b;line-height:1.6}.field-detail-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px}.field-record-actions{display:grid;gap:6px;justify-items:end}.field-scene-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:8px}.field-scene-grid label{font-size:11px;color:#475569}.field-scene-grid select{margin-top:4px}
  .field-place-entry{border:1px solid #bfdbfe;background:linear-gradient(145deg,#f8fbff,#eff6ff);border-radius:16px;padding:13px}.field-place-entry input{background:#fff}.field-place-help{display:flex;gap:8px;align-items:flex-start;margin-top:8px;color:#475569;font-size:11px;line-height:1.5}.field-place-help:before{content:"i";display:grid;place-items:center;flex:0 0 18px;width:18px;height:18px;border-radius:50%;background:#dbeafe;color:#1d4ed8;font-weight:900}.field-place-help.matched:before{content:"✓";background:#ccfbf1;color:#0f766e}.field-photo-journey{border:1px solid #bae6fd;background:linear-gradient(145deg,#f8fafc,#f0fdfa);border-radius:18px;padding:14px}.field-photo-journey-head{display:flex;justify-content:space-between;gap:10px;align-items:center;margin-bottom:11px}.field-photo-journey-head b{font-size:15px}.field-photo-count{display:inline-flex;align-items:center;padding:5px 9px;border-radius:999px;background:#0f766e;color:#fff;font-size:11px;font-weight:900}.field-photo-steps{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px;margin-bottom:11px}.field-photo-step{display:flex;gap:9px;align-items:center;padding:10px;border:1px solid #cbd5e1;border-radius:13px;background:#fff;color:#64748b}.field-photo-step strong{display:grid;place-items:center;flex:0 0 27px;width:27px;height:27px;border-radius:50%;background:#e2e8f0;color:#475569}.field-photo-step b,.field-photo-step span{display:block}.field-photo-step b{font-size:12px;color:#334155}.field-photo-step span{font-size:10px;margin-top:2px}.field-photo-step.now{border-color:#22c55e;background:#f0fdf4}.field-photo-step.now strong,.field-photo-step.done strong{background:#0f766e;color:#fff}.field-photo-step.done{border-color:#99f6e4;background:#f0fdfa}.field-photo-step.pending{border-style:dashed}.field-photo-picker{position:relative;overflow:hidden;border:1px solid #cbd5e1;border-radius:16px;background:#fff}.field-photo-input{position:absolute;width:1px;height:1px;opacity:0;pointer-events:none}.field-photo-trigger{width:100%;min-height:154px;border:0;background:linear-gradient(145deg,#f8fafc,#eefcf9);display:flex;flex-direction:column;align-items:center;justify-content:center;gap:6px;padding:18px;color:#0f172a;cursor:pointer}.field-photo-trigger:hover{background:#ecfeff}.field-camera-icon{display:grid;place-items:center;width:52px;height:52px;border-radius:17px;background:linear-gradient(135deg,#0f766e,#0891b2);color:#fff;font-size:25px;box-shadow:0 10px 24px rgba(8,145,178,.2)}.field-photo-trigger b{font-size:16px}.field-photo-trigger span:last-child{max-width:440px;color:#64748b;font-size:11px;line-height:1.5}.field-photo-picker .field-photo-preview{display:none;width:100%;height:230px;max-height:none;margin:0;border:0;border-radius:0;object-fit:cover}.field-photo-ready{display:none;align-items:center;justify-content:space-between;gap:10px;padding:11px 12px;background:#ecfdf5;border-top:1px solid #a7f3d0}.field-photo-ready b,.field-photo-ready span{display:block}.field-photo-ready b{color:#065f46;font-size:12px}.field-photo-ready span{color:#047857;font-size:10px;margin-top:2px}.field-photo-ready button{border:1px solid #6ee7b7;border-radius:10px;background:#fff;color:#0f766e;padding:8px 11px;font-weight:800;cursor:pointer}.field-photo-picker.has-photo .field-photo-trigger{display:none}.field-photo-picker.has-photo .field-photo-preview{display:block}.field-photo-picker.has-photo .field-photo-ready{display:flex}.field-photo-next{margin-top:10px;padding:10px 11px;border-radius:12px;background:#fff7ed;border:1px solid #fed7aa;color:#9a3412;font-size:11px;line-height:1.55}.field-photo-progress-card{margin:12px 0;padding:14px;border:1px solid #bae6fd;border-radius:17px;background:linear-gradient(135deg,#eff6ff,#f0fdfa)}.field-photo-progress-card.complete{border-color:#86efac;background:linear-gradient(135deg,#f0fdf4,#ecfdf5)}.field-photo-progress-head{display:flex;justify-content:space-between;gap:10px;align-items:center}.field-photo-progress-head b{font-size:15px}.field-photo-progress-head span{font-size:11px;color:#475569}.field-photo-progress-track{height:8px;margin:10px 0;border-radius:999px;background:#dbeafe;overflow:hidden}.field-photo-progress-fill{height:100%;border-radius:999px;background:linear-gradient(90deg,#0f766e,#06b6d4);transition:width .25s ease}.field-photo-progress-actions{display:flex;gap:8px;align-items:center;justify-content:space-between}.field-photo-progress-actions .muted{font-size:11px;line-height:1.45}.field-action.required{border-color:#0ea5a5;background:#f0fdfa;box-shadow:0 8px 22px rgba(13,148,136,.12)}
  @media(max-width:820px){.field-kpis{grid-template-columns:repeat(2,1fr)}.field-actions{grid-template-columns:repeat(2,1fr)}.field-feed-item{grid-template-columns:68px 1fr}.field-feed-item>div:last-child{grid-column:2}.field-analysis,.field-detail-grid,.field-scene-grid{grid-template-columns:1fr}.field-hero{border-radius:18px;padding:16px}.field-hero h2{font-size:20px}.field-team-table{font-size:11px}.modal:has(#fieldTouchForm),.modal:has(#fieldStartForm){width:min(96vw,760px);max-height:92vh}.field-voice{display:block}.field-voice .btn{width:100%;margin-top:7px}.field-action{min-height:88px}.field-record-actions{display:flex;align-items:center;justify-content:flex-start}.field-location-actions .btn{flex:1}.field-map-preview{height:260px}.field-map-shell.compact .field-map-preview{height:200px}.field-map-address{padding:10px}.field-current-marker{width:22px;height:22px}}
  @media(max-width:560px){.field-photo-steps{grid-template-columns:1fr}.field-photo-step{padding:9px}.field-photo-trigger{min-height:142px}.field-photo-picker .field-photo-preview{height:210px}.field-photo-progress-actions{display:block}.field-photo-progress-actions .btn{width:100%;margin-top:9px}.field-place-entry{padding:11px}}
  .field-start{max-width:1120px}.field-start-card{padding:20px}.field-start-heading{display:flex;align-items:flex-start;justify-content:space-between;gap:16px;margin-bottom:12px}.field-start-heading h2{margin:0;font-size:20px;line-height:1.3}.field-start-heading p{margin:4px 0 0;color:var(--muted);font-size:11px;line-height:1.5}.field-start-badge{display:inline-flex;align-items:center;gap:6px;flex:0 0 auto;padding:7px 10px;border-radius:999px;background:#ccfbf1;color:#115e59;font-size:11px;font-weight:900}.field-rule-strip{display:flex;gap:10px;align-items:flex-start;margin-bottom:16px;padding:11px 13px;border:1px solid #bae6fd;border-radius:14px;background:#f0f9ff;color:#0c4a6e;font-size:11px;line-height:1.6}.field-rule-strip:before{content:"2";display:grid;place-items:center;flex:0 0 26px;width:26px;height:26px;border-radius:9px;background:#0f766e;color:#fff;font-weight:950}.field-start-form{display:grid;grid-template-columns:minmax(0,5fr) minmax(0,7fr);gap:16px;align-items:start}.field-start-section{min-width:0;border:1px solid var(--line);border-radius:18px;background:#fff;padding:15px}.field-start-section-place{grid-column:1/-1;background:linear-gradient(145deg,#fbfdff,#f6faff)}.field-plan-panel{grid-column:1}.field-proof-panel{grid-column:2}.field-section-head{display:flex;align-items:center;gap:9px;margin-bottom:12px}.field-section-index{display:grid;place-items:center;flex:0 0 28px;width:28px;height:28px;border-radius:10px;background:#0f766e;color:#fff;font-size:12px;font-weight:950}.field-section-head b,.field-section-head span{display:block}.field-section-head b{font-size:14px;color:#0f172a}.field-section-head span{margin-top:2px;color:#64748b;font-size:10px}.field-section-head .field-section-index{display:grid;margin-top:0;color:#fff;font-size:12px}.field-start .span7{grid-column:span 7}.field-start .span2{grid-column:span 2}.field-place-entry{padding:0;border:0;background:transparent}.field-location-field{margin-top:14px}.field-location-card:not(.has-location) .field-map-shell{display:none}.field-location-card:not(.has-location){padding:12px;background:#f8fafc}.field-location-card.has-location .field-map-preview{height:210px}.field-proof-panel .field-photo-journey{padding:0;border:0;background:transparent}.field-start-submit{display:grid;gap:9px;margin-top:12px}.field-start-submit .btn{width:100%;min-height:46px}.field-start-submit .field-location{line-height:1.55}.field-screen .mobile-fab{display:none!important}
  @media(max-width:940px){.field-start-form{grid-template-columns:1fr}.field-start-section-place,.field-plan-panel,.field-proof-panel{grid-column:1}.field-start{max-width:760px}}
  @media(max-width:700px){.field-screen .page-head>.actions{display:flex!important;width:100%;gap:8px}.field-screen .page-head>.actions .btn{flex:1;min-width:0}.field-screen .field-refresh-action{display:none!important}.field-start-card{padding:12px;border-radius:18px}.field-start-heading{margin-bottom:10px}.field-start-heading h2{font-size:18px}.field-start-heading p{font-size:10px}.field-start-badge{padding:6px 9px;font-size:10px}.field-rule-strip{margin-bottom:12px;padding:10px;font-size:10px}.field-start-form{gap:11px}.field-start-section{padding:12px;border-radius:16px}.field-section-head{margin-bottom:10px}.field-place-entry .grid{grid-template-columns:repeat(12,minmax(0,1fr))}.field-place-entry .field.span7{grid-column:1/-1!important}.field-place-entry .field.span3{grid-column:span 7!important}.field-place-entry .field.span2{grid-column:span 5!important}.field-place-entry .field.span12{grid-column:1/-1!important}.field-place-entry input,.field-place-entry select{min-width:0}.field-inline-checks{gap:7px}.field-inline-checks label{padding:9px 11px}.field-location-field{margin-top:12px}.field-location-actions{display:grid;grid-template-columns:1fr 1fr}.field-location-actions .btn{width:100%;padding:9px 8px}.field-location-card.has-location .field-map-preview{height:190px}.field-photo-trigger{min-height:132px;padding:14px}.field-camera-icon{width:46px;height:46px;border-radius:14px;font-size:22px}.field-photo-next{font-size:10px}.field-start-submit .btn{min-height:48px}.field-start+.section{margin-top:11px}}
  @media(max-width:360px){.field-place-entry .field.span3,.field-place-entry .field.span2{grid-column:1/-1!important}.field-location-actions{grid-template-columns:1fr}.field-start-heading{display:block}.field-start-badge{margin-top:8px}}
  `;document.head.appendChild(style);

  async function api(body){if(!ses?.access_token)throw Error('请先登录');const started=performance.now(),r=await fetch(API,{method:'POST',headers:{apikey:K,authorization:'Bearer '+ses.access_token,'content-type':'application/json'},body:JSON.stringify(body)}),raw=await r.text();let j={};try{j=raw?JSON.parse(raw):{}}catch{}FS.lastApiMs=Math.round(performance.now()-started);document.documentElement.dataset.fonkonFieldApiMs=String(FS.lastApiMs);if(!r.ok)throw Error(j.error||j.message||'现场开发服务暂时不可用');if(!['bootstrap','session_detail','photo_url','extract_note','team_dashboard','team_analysis','amap_config','reverse_geocode'].includes(body?.action)){FS.dirty=true;clearFieldSnapshot()}return j}
  function fieldNav(){const side=document.querySelector('.sidebar');if(!side)return;let b=side.querySelector('[data-view="field"]');if(!b){b=document.createElement('button');b.className='nav-btn';b.dataset.view='field';b.innerHTML='<span class="ico">⌖</span><span>市场开发</span>';const visits=side.querySelector('[data-view="visits"]');(visits?.parentNode||side).insertBefore(b,visits?.nextSibling||null)}if(!b.dataset.bound){b.dataset.bound='1';b.addEventListener('click',e=>{e.preventDefault();window.render('field')})}}
  function heading(title,sub,actions=''){return pageHead(title,sub,actions)}
  function uuid(){return crypto.randomUUID?crypto.randomUUID():'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g,c=>{const r=Math.random()*16|0,v=c==='x'?r:(r&3|8);return v.toString(16)})}
  function arrays(v){return String(v||'').split(/[、,，;；\n]/).map(x=>x.trim()).filter(Boolean)}
  function locationFresh(loc,maxAge=180000){return !!(loc&&loc.status!=='unavailable'&&loc.capturedAt&&Date.now()-loc.capturedAt<maxAge)}
  function coord(v){return Number.isFinite(Number(v))?Number(v).toFixed(6):'-'}
  function locationLabel(loc){if(!loc)return '尚未获取当前位置';if(loc.status==='unavailable')return loc.reason||'定位不可用';return '当前位置已获取 · 精度约 ±'+Math.round(loc.accuracy||0)+' 米'}
  function permissionLabel(){return FS.locationPermission==='granted'?'已授权':FS.locationPermission==='denied'?'已拒绝':FS.locationPermission==='prompt'?'待授权':'等待检测'}
  function pct(v){return v===null||v===undefined?'待形成':E(v)+'%'}
  function fenceLabel(s){if(!s)return '市场入口基准待设置';if(s.start_geofence_status==='inside')return '入口范围内 · 距基准约 '+Math.round(s.start_distance_m||0)+' 米';if(s.start_geofence_status==='outside')return '距入口基准约 '+Math.round(s.start_distance_m||0)+' 米（仅留痕，不阻断）';if(s.start_geofence_status==='unavailable')return '定位不可用，已自动留痕';return '市场入口基准待设置'}
  function sessionLocationText(s){const lat=Number(s?.start_lat),lng=Number(s?.start_lng),accuracy=Number(s?.start_accuracy_m);return Number.isFinite(lat)&&Number.isFinite(lng)?'已取得地图位置'+(Number.isFinite(accuracy)?' · 精度约 ±'+Math.round(accuracy)+'米':''):'未取得地图位置'}
  function amapMarkerUrl(lat,lng,name='FONKON现场位置'){lat=Number(lat);lng=Number(lng);if(!Number.isFinite(lat)||!Number.isFinite(lng))return '';return 'https://uri.amap.com/marker?position='+encodeURIComponent(lng+','+lat)+'&name='+encodeURIComponent(name)+'&src=FONKON&coordinate=wgs84&callnative=0'}
  function mapKey(lat,lng){return Number(lat).toFixed(5)+','+Number(lng).toFixed(5)}
  function scheduleMapHydration(){clearTimeout(FS.mapHydrationTimer);FS.mapHydrationTimer=setTimeout(()=>hydrateFieldMaps(document),0)}
  function mapPreviewHtml(lat,lng,name='FONKON现场位置',compact=false,accuracy=30){lat=Number(lat);lng=Number(lng);if(!Number.isFinite(lat)||!Number.isFinite(lng))return '';scheduleMapHydration();return '<div class="field-map-shell'+(compact?' compact':'')+'"><div class="field-map-preview"><div class="field-amap-host" data-field-map="1" data-latitude="'+E(lat)+'" data-longitude="'+E(lng)+'" data-accuracy="'+E(accuracy||30)+'" data-map-name="'+E(name)+'" aria-label="'+E(name)+'地图"></div><div class="field-map-placeholder">正在加载当前位置地图…</div></div><div class="field-map-address"><span class="field-map-address-icon">⌖</span><div><b class="field-map-address-main">正在识别中文地址…</b><span class="field-map-address-sub">定位仅用于本次现场记录，不持续后台跟踪</span></div></div></div>'}
  async function loadAmapSDK(){if(window.AMap?.Map)return window.AMap;if(FS.mapSdkFlight)return FS.mapSdkFlight;FS.mapSdkFlight=(async()=>{const config=await (FS.mapConfigFlight||(FS.mapConfigFlight=api({action:'amap_config'})));if(!config?.key||!config?.service_host)throw Error('地图组件配置不完整');window._AMapSecurityConfig={serviceHost:config.service_host};await new Promise((resolve,reject)=>{const loaded=document.querySelector('script[data-fonkon-amap]');if(loaded){if(window.AMap?.Map)return resolve();loaded.addEventListener('load',resolve,{once:true});loaded.addEventListener('error',()=>reject(Error('地图组件加载失败')),{once:true});return}const script=document.createElement('script');script.dataset.fonkonAmap='1';script.async=true;script.src='https://webapi.amap.com/maps?v=2.0&key='+encodeURIComponent(config.key);script.referrerPolicy='strict-origin-when-cross-origin';script.onload=resolve;script.onerror=()=>reject(Error('地图组件加载失败，请检查网络'));document.head.appendChild(script)});if(!window.AMap?.Map)throw Error('地图组件未正确启动');return window.AMap})().catch(error=>{FS.mapSdkFlight=null;throw error});return FS.mapSdkFlight}
  async function mapPlace(lat,lng){const key=mapKey(lat,lng);if(FS.placeCache.has(key))return FS.placeCache.get(key);const flight=api({action:'reverse_geocode',latitude:Number(lat),longitude:Number(lng)}).catch(error=>{FS.placeCache.delete(key);throw error});FS.placeCache.set(key,flight);return flight}
  function destroyMapHost(host){const instance=FS.mapInstances.get(host);if(instance?.map)try{instance.map.destroy()}catch{}FS.mapInstances.delete(host)}
  function mapAddress(host,main,sub){const shell=host.closest('.field-map-shell'),headline=shell?.querySelector('.field-map-address-main'),detail=shell?.querySelector('.field-map-address-sub');if(headline)headline.textContent=main;if(detail)detail.textContent=sub}
  async function mountMapHost(host){const lat=Number(host?.dataset?.latitude),lng=Number(host?.dataset?.longitude),accuracy=Math.max(0,Number(host?.dataset?.accuracy)||30),name=host?.dataset?.mapName||'FONKON现场位置';if(!host||!Number.isFinite(lat)||!Number.isFinite(lng))return;const key=mapKey(lat,lng)+'|'+Math.round(accuracy),current=FS.mapInstances.get(host),loader=host.closest('.field-map-preview')?.querySelector('.field-map-placeholder');if(current?.key===key){try{current.map.resize()}catch{}return}host.dataset.locationKey=key;host.hidden=false;if(loader){loader.hidden=false;loader.textContent='正在加载当前位置地图…'}mapAddress(host,'正在识别中文地址…','定位仅用于本次现场记录，不持续后台跟踪');try{const [AMap,place]=await Promise.all([loadAmapSDK(),mapPlace(lat,lng)]);if(!host.isConnected||host.dataset.locationKey!==key)return;destroyMapHost(host);host.replaceChildren();const center=[Number(place.map_position.longitude),Number(place.map_position.latitude)],map=new AMap.Map(host,{viewMode:'2D',zoom:accuracy<=30?18:accuracy<=80?17:accuracy<=200?16:15,center,mapStyle:'amap://styles/whitesmoke',showLabel:true,resizeEnable:true,animateEnable:false,jogEnable:false,pitchEnable:false,rotateEnable:false}),radius=Math.min(Math.max(accuracy,10),500),circle=new AMap.Circle({center,radius,strokeColor:'#1683ff',strokeOpacity:.55,strokeWeight:2,fillColor:'#1683ff',fillOpacity:.12,zIndex:40}),marker=new AMap.Marker({position:center,anchor:'center',title:name,content:'<div class="field-current-marker" aria-hidden="true"></div>',zIndex:120});map.add([circle,marker]);map.setZoomAndCenter(accuracy<=30?18:accuracy<=80?17:accuracy<=200?16:15,center,false);map.on('complete',()=>{try{map.resize()}catch{}});FS.mapInstances.set(host,{map,key});if(loader)loader.hidden=true;const poi=place.nearby_poi,nearby=poi?.name?'附近：'+poi.name+(Number.isFinite(Number(poi.distance))?' · 约'+Math.round(Number(poi.distance))+'米':''):'地址由高德地图识别';mapAddress(host,place.formatted_address||'当前位置',nearby)}catch(error){destroyMapHost(host);host.hidden=true;if(loader){loader.hidden=false;loader.textContent='地图暂时无法加载，请重新定位后重试'}mapAddress(host,'暂时无法识别中文地址',error?.message||'请检查网络后重试')}}
  function hydrateFieldMaps(root=document){root.querySelectorAll?.('.field-amap-host[data-field-map="1"]').forEach(host=>mountMapHost(host))}
  function getLocation(){return new Promise(resolve=>{if(!navigator.geolocation)return resolve({status:'unavailable',reason:'此浏览器不支持定位',capturedAt:Date.now()});navigator.geolocation.getCurrentPosition(p=>resolve({status:p.coords.accuracy<=200?'verified':'low_accuracy',latitude:p.coords.latitude,longitude:p.coords.longitude,accuracy:p.coords.accuracy,capturedAt:Number(p.timestamp)||Date.now()}),e=>resolve({status:'unavailable',reason:e.code===1?'浏览器定位权限未允许':e.code===2?'手机暂时无法取得位置':'获取定位超时',capturedAt:Date.now()}),{enableHighAccuracy:true,timeout:12000,maximumAge:15000})})}
  function updateLocationPanel(){const box=document.querySelector('#fieldLocationPanel'),title=document.querySelector('#fieldLocationTitle'),detail=document.querySelector('#fieldLocationDetail'),badge=document.querySelector('#fieldLocationBadge'),accuracy=document.querySelector('#fieldLocationAccuracy'),time=document.querySelector('#fieldLocationTime'),locate=document.querySelector('#fieldLocateBtn'),map=document.querySelector('#fieldMapBtn'),host=document.querySelector('#fieldLocationMap'),placeholder=document.querySelector('#fieldLocationPlaceholder'),loc=FS.currentLocation,ok=loc&&loc.status!=='unavailable';if(!box)return;box.className='field-location-card '+(ok?(loc.status==='verified'?'good has-location':'warn has-location'):(loc?'warn':''));if(title)title.textContent=locationLabel(loc);if(badge)badge.textContent=permissionLabel();if(accuracy)accuracy.textContent=ok?'定位精度 ±'+Math.round(loc.accuracy||0)+'米':'定位精度待获取';if(time)time.textContent=ok?'更新于 '+new Date(loc.capturedAt).toLocaleTimeString('zh-CN',{hour:'2-digit',minute:'2-digit',second:'2-digit'}):'尚未定位';if(detail){if(ok)detail.textContent=(locationFresh(loc)?'蓝色标记为业务员当前位置，浅蓝圆圈为定位精度范围；超过3分钟自动刷新。':'该位置已超过3分钟，开始前会自动重新获取。');else if(FS.locationPermission==='denied')detail.textContent='请点浏览器地址栏左侧权限图标，将“位置信息”改为允许，再点击重新定位。';else if(loc?.reason)detail.textContent=loc.reason+'。请确认手机定位服务已开启，然后重新定位；失败也不会丢失已填写内容。';else detail.textContent='点击下方按钮后，浏览器会请求定位授权；允许后显示地图、中文地址和附近地点。'}if(ok&&host){const key=mapKey(loc.latitude,loc.longitude)+'|'+Math.round(loc.accuracy||0);host.dataset.fieldMap='1';host.dataset.latitude=String(loc.latitude);host.dataset.longitude=String(loc.longitude);host.dataset.accuracy=String(loc.accuracy||30);host.dataset.mapName='FONKON现场位置';host.hidden=false;if(host.dataset.locationKey!==key)mountMapHost(host)}else if(host){destroyMapHost(host);host.hidden=true;host.dataset.locationKey='';if(placeholder){placeholder.hidden=false;placeholder.textContent=FS.locationPermission==='denied'?'请允许浏览器使用位置信息后重新定位':'授权定位后，在这里直接显示当前位置地图'}mapAddress(host,'定位后显示中文地址','不会在业务界面显示经纬度')}if(locate){locate.disabled=!!FS.locationFlight;locate.textContent=FS.locationFlight?'正在获取位置…':ok?'重新定位':FS.locationPermission==='denied'?'重新检查定位':'授权并显示地图'}if(map){map.disabled=!ok;map.textContent='在高德地图全屏查看'}}
  async function inspectLocationPermission(){if(!document.querySelector('#fieldLocationPanel'))return;try{if(!navigator.permissions?.query){FS.locationPermission='unknown';updateLocationPanel();return}const p=await navigator.permissions.query({name:'geolocation'});FS.locationPermission=p.state;FS.locationPermissionHandle=p;p.onchange=()=>{FS.locationPermission=p.state;updateLocationPanel();if(p.state==='granted'&&!locationFresh(FS.currentLocation))window.fieldAcquireStartLocation(false)};updateLocationPanel();if(p.state==='granted'&&!locationFresh(FS.currentLocation))window.fieldAcquireStartLocation(false)}catch{FS.locationPermission='unknown';updateLocationPanel()}}
  window.fieldAcquireStartLocation=async function(force=true){if(!force&&locationFresh(FS.currentLocation)){updateLocationPanel();return FS.currentLocation}if(FS.locationFlight)return FS.locationFlight;FS.locationFlight=(async()=>{updateLocationPanel();const loc=await getLocation();FS.currentLocation=loc;if(loc.status!=='unavailable')FS.locationPermission='granted';else if(/权限|未允许/.test(loc.reason||''))FS.locationPermission='denied';updateLocationPanel();return loc})().finally(()=>{FS.locationFlight=null;updateLocationPanel()});updateLocationPanel();return FS.locationFlight};
  window.fieldOpenMapAt=function(lat,lng,name='FONKON现场位置'){const url=amapMarkerUrl(lat,lng,name);if(!url)return toast('请先获取有效定位');window.open(url.replace('callnative=0','callnative=1'),'_blank','noopener,noreferrer')};
  window.fieldOpenCurrentMap=function(){const loc=FS.currentLocation;if(!loc||loc.status==='unavailable')return toast('请先获取当前位置');window.fieldOpenMapAt(loc.latitude,loc.longitude,'FONKON现场位置')};
  function bindLocationPanel(){const locate=document.querySelector('#fieldLocateBtn'),map=document.querySelector('#fieldMapBtn'),file=document.querySelector('#fieldEntryPhoto');if(locate)locate.onclick=()=>window.fieldAcquireStartLocation(true);if(map)map.onclick=window.fieldOpenCurrentMap;if(file)file.addEventListener('change',()=>{if(file.files?.[0]&&!locationFresh(FS.currentLocation))window.fieldAcquireStartLocation(false)});updateLocationPanel();inspectLocationPermission()}
  function filePreview(input,img){const f=input.files?.[0];if(!f){img.removeAttribute('src');img.style.display='none';return}const u=URL.createObjectURL(f);img.src=u;img.style.display='block';img.onload=()=>URL.revokeObjectURL(u)}
  function photoPickerHtml(inputId,previewId,title,tip){return '<div class="field-photo-picker" id="'+inputId+'Card"><input class="field-photo-input" id="'+inputId+'" type="file" accept="image/*" capture="environment"><button type="button" class="field-photo-trigger" id="'+inputId+'Trigger"><span class="field-camera-icon">📷</span><b>'+E(title)+'</b><span>'+E(tip)+'</span></button><img id="'+previewId+'" class="field-photo-preview" alt="'+E(title)+'预览"><div class="field-photo-ready"><div><b>照片已准备</b><span id="'+inputId+'Meta">提交前仍可重拍或更换</span></div><button type="button" id="'+inputId+'Retake">重拍 / 更换</button></div></div>'}
  function bindPhotoPicker(inputId,previewId,autoLocate=false){const input=document.querySelector('#'+inputId),img=document.querySelector('#'+previewId),card=document.querySelector('#'+inputId+'Card'),trigger=document.querySelector('#'+inputId+'Trigger'),retake=document.querySelector('#'+inputId+'Retake'),meta=document.querySelector('#'+inputId+'Meta');if(!input||!img||!card)return;const open=()=>input.click(),render=()=>{const f=input.files?.[0];filePreview(input,img);card.classList.toggle('has-photo',!!f);if(meta&&f)meta.textContent='已选择照片 · '+Math.max(1,Math.round(f.size/1024))+' KB';if(f&&autoLocate&&!locationFresh(FS.currentLocation))window.fieldAcquireStartLocation(false)};if(trigger)trigger.onclick=open;if(retake)retake.onclick=open;input.onchange=render;render()}
  async function compressImage(file){if(!file)throw Error('请先拍摄照片');if(!/^image\//i.test(file.type))throw Error('请选择手机照片文件');const url=URL.createObjectURL(file);try{const img=await new Promise((ok,bad)=>{const x=new Image();x.onload=()=>ok(x);x.onerror=()=>bad(Error('照片读取失败，请换一张照片重试'));x.src=url}),max=1600,scale=Math.min(1,max/Math.max(img.naturalWidth,img.naturalHeight)),w=Math.max(1,Math.round(img.naturalWidth*scale)),h=Math.max(1,Math.round(img.naturalHeight*scale)),canvas=document.createElement('canvas');canvas.width=w;canvas.height=h;const ctx=canvas.getContext('2d');ctx.drawImage(img,0,0,w,h);const blob=await new Promise(ok=>canvas.toBlob(ok,'image/jpeg',.78));if(!blob)throw Error('照片压缩失败');if(blob.size>8388608)throw Error('照片仍然过大，请降低相机分辨率后重拍');return blob}finally{URL.revokeObjectURL(url)}}
  async function uploadPhoto(file,sessionId,type){const blob=await compressImage(file),path=ses.user.id+'/'+sessionId+'/'+type+'-'+Date.now()+'-'+Math.random().toString(36).slice(2,7)+'.jpg',r=await fetch(U+'/storage/v1/object/'+BUCKET+'/'+path.split('/').map(encodeURIComponent).join('/'),{method:'POST',headers:{apikey:K,authorization:'Bearer '+ses.access_token,'content-type':'image/jpeg','x-upsert':'false'},body:blob});if(!r.ok){let j={};try{j=await r.json()}catch{}throw Error(j.message||j.error||'照片上传失败，请检查网络后重试')}return {path,mime_type:'image/jpeg',file_size_bytes:blob.size}}
  async function discardUpload(sessionId,path){if(!path)return;try{await api({action:'discard_upload',session_id:sessionId,storage_path:path})}catch{}}
  function allMarketEntries(){const seen=new Set();return [...(FS.data?.markets||[]),...(FS.data?.personal_markets||[])].filter(x=>{const key=String(x.id||'')+'|'+String(x.name||'').trim().toLowerCase()+'|'+String(x.city||'').trim().toLowerCase();if(seen.has(key))return false;seen.add(key);return true})}
  function marketEntryByName(){const name=document.querySelector('#fieldMarketName')?.value?.trim().toLowerCase();return name?allMarketEntries().find(x=>String(x.name||'').trim().toLowerCase()===name):null}
  function selectedMarket(){const id=document.querySelector('#fieldMarketId')?.value;return (FS.data?.markets||[]).find(x=>x.id===id)}
  function renderZoneChecks(){const m=selectedMarket(),box=document.querySelector('#fieldZoneChecks'),office=document.querySelector('#fieldWorkMode')?.value==='office_cluster';if(!box)return;const zones=office?['办公室区']:(m?.zones?.length?m.zones:['A区','B区','C区','D区','办公室区']);box.innerHTML=zones.map(x=>'<label><input type="checkbox" name="planned_zone" value="'+E(x)+'" '+(office||x==='A区'||x==='B区'||x==='C区'||x==='D区'?'checked':'')+'> '+E(x)+'</label>').join('')}
  function syncMarketEntry(){const entry=marketEntryByName(),id=document.querySelector('#fieldMarketId'),city=document.querySelector('#fieldMarketCity'),address=document.querySelector('#fieldMarketAddress'),help=document.querySelector('#fieldMarketHelp'),approved=entry&&(FS.data?.markets||[]).some(x=>x.id===entry.id);if(id)id.value=approved?entry.id:'';if(entry&&city)city.value=entry.city||city.value;if(entry&&address)address.value=entry.address||'';if(help){help.classList.toggle('matched',!!entry);help.textContent=approved?'已匹配公司常用场地，城市、区域和入口基准会自动带入。':entry?'已匹配你此前填写的场地，可直接继续使用。':'可填写任意市场、办公楼或办公场地名称；新场地会自动保存，便于下次直接选择。'}renderZoneChecks()}
  function startViewLegacy(){
    const ms=FS.data?.markets||[],personal=FS.data?.personal_markets||[],canAdmin=FS.data?.can_view_team,first=ms[0]||personal[0]||{},firstApproved=ms.some(x=>x.id===first.id),placeOptions=allMarketEntries().map(x=>'<option value="'+E(x.name)+'">'+E(x.city||'')+(x.active===false?' · 已填写':' · 常用场地')+'</option>').join('');
    let html=heading('市场现场开发','一次市场级现场验证，连续记录客户，不要求逐个客户拍照。','<button class="btn btn-soft" onclick="fieldReload()">刷新</button>'+(canAdmin?'<button class="btn btn-soft" onclick="fieldTeamView()">团队市场洞察</button><button class="btn btn-soft" onclick="fieldMarketModal()">＋维护市场</button>':''));
    html+='<div class="field-start"><div class="card card-pad"><div class="section-title"><h2>开始市场开发</h2><span class="hint">现场照片至少2张 · 分两步完成</span></div><div class="field-form-note"><b>现场规则：</b>先在入口或办公楼门口拍第1张；进入大棚、办公区域后拍第2张现场照片。无需逐客户、逐档口拍照，超过2张仅按实际需要补充。</div><form id="fieldStartForm" class="grid" style="margin-top:12px"><div class="field span12"><div class="field-place-entry"><div class="grid"><div class="field span7"><label>市场或办公场地名称*</label><input id="fieldMarketName" name="custom_market_name" list="fieldMarketOptions" value="'+E(first.name||'')+'" placeholder="例如：广州江秾汇进口水果批发市场" autocomplete="off" required><datalist id="fieldMarketOptions">'+placeOptions+'</datalist><input id="fieldMarketId" name="market_id" type="hidden" value="'+E(firstApproved?first.id:'')+'"></div><div class="field span3"><label>城市 / 区域*</label><input id="fieldMarketCity" name="custom_city" value="'+E(first.city||'')+'" placeholder="例如：广州" required></div><div class="field span2"><label>场景</label><select id="fieldWorkMode" name="work_mode"><option value="market_shed">批发市场</option><option value="office_cluster">办公场地</option></select></div><div class="field span12"><label>详细地址（选填）</label><input id="fieldMarketAddress" name="custom_address" value="'+E(first.address||'')+'" placeholder="例如：白云区增槎路附近；也可定位后留空"></div></div><div id="fieldMarketHelp" class="field-place-help '+(first.name?'matched':'')+'">'+(firstApproved?'已匹配公司常用场地，城市、区域和入口基准会自动带入。':'可填写任意市场、办公楼或办公场地名称；新场地会自动保存，便于下次直接选择。')+'</div></div></div>'+input('focus_fruits','今日重点水果','','text','span8','placeholder="例如：榴莲、车厘子、葡萄"')+input('target_contacts','今日目标接触数',12,'number','span4','min="1" max="200"')+'<div class="field span12"><label>计划覆盖区域</label><div id="fieldZoneChecks" class="field-inline-checks"></div></div><div class="field span12"><label>当前位置*</label><div id="fieldLocationPanel" class="field-location-card"><div class="field-location-head"><b id="fieldLocationTitle">尚未获取当前位置</b><span id="fieldLocationBadge" class="field-location-badge">等待检测</span></div><div id="fieldLocationDetail" class="field-location-detail">点击下方按钮后，浏览器会请求定位授权。</div><div class="field-map-shell"><div class="field-map-preview"><div id="fieldLocationMap" class="field-amap-host" title="业务员当前位置地图" hidden></div><div id="fieldLocationPlaceholder" class="field-map-placeholder">授权定位后，在这里直接显示当前位置地图</div></div><div class="field-map-address"><span class="field-map-address-icon">⌖</span><div><b id="fieldLocationAddress" class="field-map-address-main">定位后显示中文地址</b><span id="fieldLocationNearby" class="field-map-address-sub">不会在业务界面显示经纬度</span></div></div></div><div class="field-location-meta"><span id="fieldLocationAccuracy">定位精度待获取</span><span id="fieldLocationTime">尚未定位</span></div><div class="field-location-actions"><button type="button" id="fieldLocateBtn" class="btn btn-primary">授权并显示地图</button><button type="button" id="fieldMapBtn" class="btn btn-soft" disabled>在高德地图全屏查看</button></div><div class="muted" style="margin-top:7px">定位仅用于本次现场记录、地图显示和中文地址识别，不会持续在后台跟踪；经纬度只在后台治理记录中留痕。</div></div></div><div class="field span12"><div class="field-photo-journey"><div class="field-photo-journey-head"><b>现场照片</b><span class="field-photo-count">第1张 / 共2张</span></div><div class="field-photo-steps"><div class="field-photo-step now"><strong>1</strong><div><b>入口或门口</b><span>现在拍，保存后开始</span></div></div><div class="field-photo-step pending"><strong>2</strong><div><b>现场或办公区域</b><span>进入现场后拍，结束前必填</span></div></div></div>'+photoPickerHtml('fieldEntryPhoto','fieldEntryPreview','拍摄第1张：入口或门口','点击打开手机后置相机；电脑端或相机不可用时可从相册选择。')+'<div class="field-photo-next"><b>第2张何时拍？</b> 进入大棚、冷柜销售区或办公区域后，系统会在工作台顶部提醒并提供大按钮；完成2/2后才可结束本次开发。</div></div></div><div class="span12"><div id="fieldLocationState" class="field-location">先定位并拍摄第1张入口/门口照片；手机定位失败会自动记录原因，不会清空已填写内容。</div></div><div class="span12 actions"><button id="fieldStartBtn" class="btn btn-primary">保存第1张并开始现场开发</button></div></form></div><div class="card card-pad section"><div class="section-title"><h2>最近现场记录</h2><span class="hint">每次市场开发完整留痕</span></div>'+historyList(FS.data?.recent_sessions||[])+'</div></div>';
    document.querySelector('#view').innerHTML=html;renderZoneChecks();const marketName=document.querySelector('#fieldMarketName'),workMode=document.querySelector('#fieldWorkMode');marketName.addEventListener('input',syncMarketEntry);marketName.addEventListener('change',syncMarketEntry);workMode.addEventListener('change',()=>{renderZoneChecks();syncMarketEntry()});bindPhotoPicker('fieldEntryPhoto','fieldEntryPreview',true);bindLocationPanel();syncMarketEntry();document.querySelector('#fieldStartForm').onsubmit=startSubmit
  }
  function startView(){
    const ms=FS.data?.markets||[],personal=FS.data?.personal_markets||[],canAdmin=FS.data?.can_view_team,first=ms[0]||personal[0]||{},firstApproved=ms.some(x=>x.id===first.id),placeOptions=allMarketEntries().map(x=>'<option value="'+E(x.name)+'">'+E(x.city||'')+(x.active===false?' · 已填写':' · 常用场地')+'</option>').join('');
    const headActions='<button class="btn btn-soft field-refresh-action" onclick="fieldReload()">刷新</button>'+(canAdmin?'<button class="btn btn-soft" onclick="fieldTeamView()">团队洞察</button><button class="btn btn-soft" onclick="fieldMarketModal()">维护场地</button>':'');
    let html=heading('市场现场开发','填写计划、确认位置并拍摄入口照，即可开始连续记录客户。',headActions);
    html+=`<div class="field-start">
      <div class="card field-start-card">
        <div class="field-start-heading"><div><h2>开始现场开发</h2><p>按三个区块完成，不要求逐客户拍照。</p></div><span class="field-start-badge">最低 2 张现场照片</span></div>
        <div class="field-rule-strip"><div><b>拍照规则：</b>开始前拍入口或办公楼门口；进入大棚、冷柜销售区或办公区域后拍第 2 张。超过 2 张仅按实际需要补充。</div></div>
        <form id="fieldStartForm" class="field-start-form">
          <section class="field-start-section field-start-section-place">
            <div class="field-section-head"><span class="field-section-index">1</span><div><b>开发场地</b><span>可选择常用场地，也可直接填写新的市场或办公地点</span></div></div>
            <div class="field-place-entry"><div class="grid">
              <div class="field span7"><label>市场或办公场地名称*</label><input id="fieldMarketName" name="custom_market_name" list="fieldMarketOptions" value="${E(first.name||'')}" placeholder="例如：广州江秾汇进口水果批发市场" autocomplete="off" required><datalist id="fieldMarketOptions">${placeOptions}</datalist><input id="fieldMarketId" name="market_id" type="hidden" value="${E(firstApproved?first.id:'')}"></div>
              <div class="field span3"><label>城市 / 区域*</label><input id="fieldMarketCity" name="custom_city" value="${E(first.city||'')}" placeholder="例如：广州" required></div>
              <div class="field span2"><label>场景</label><select id="fieldWorkMode" name="work_mode"><option value="market_shed">批发市场</option><option value="office_cluster">办公场地</option></select></div>
              <div class="field span12"><label>详细地址（选填）</label><input id="fieldMarketAddress" name="custom_address" value="${E(first.address||'')}" placeholder="例如：白云区增槎路附近；定位成功后也可留空"></div>
            </div><div id="fieldMarketHelp" class="field-place-help ${first.name?'matched':''}">${firstApproved?'已匹配公司常用场地，城市、区域和入口基准会自动带入。':'可填写任意市场、办公楼或办公场地名称；新场地会自动保存，便于下次直接选择。'}</div></div>
          </section>
          <section class="field-start-section field-plan-panel">
            <div class="field-section-head"><span class="field-section-index">2</span><div><b>计划与位置</b><span>填写今日目标，并确认当前现场位置</span></div></div>
            <div class="grid">${input('focus_fruits','今日重点水果','','text','span8','placeholder="例如：榴莲、车厘子、葡萄"')}${input('target_contacts','目标接触数',12,'number','span4','min="1" max="200"')}<div class="field span12"><label>计划覆盖区域</label><div id="fieldZoneChecks" class="field-inline-checks"></div></div></div>
            <div class="field field-location-field"><label>当前位置*</label><div id="fieldLocationPanel" class="field-location-card"><div class="field-location-head"><b id="fieldLocationTitle">尚未获取当前位置</b><span id="fieldLocationBadge" class="field-location-badge">等待检测</span></div><div id="fieldLocationDetail" class="field-location-detail">点击下方按钮后，浏览器会请求定位授权。</div><div class="field-map-shell"><div class="field-map-preview"><div id="fieldLocationMap" class="field-amap-host" title="业务员当前位置地图" hidden></div><div id="fieldLocationPlaceholder" class="field-map-placeholder">授权定位后显示当前位置地图</div></div><div class="field-map-address"><span class="field-map-address-icon">⌖</span><div><b id="fieldLocationAddress" class="field-map-address-main">定位后显示中文地址</b><span id="fieldLocationNearby" class="field-map-address-sub">业务页面不会显示经纬度</span></div></div></div><div class="field-location-meta"><span id="fieldLocationAccuracy">定位精度待获取</span><span id="fieldLocationTime">尚未定位</span></div><div class="field-location-actions"><button type="button" id="fieldLocateBtn" class="btn btn-primary">授权并获取位置</button><button type="button" id="fieldMapBtn" class="btn btn-soft" disabled>在高德地图查看</button></div><div class="muted" style="margin-top:7px">定位仅用于本次记录与地址识别，不会持续后台跟踪。</div></div></div>
          </section>
          <section class="field-start-section field-proof-panel">
            <div class="field-section-head"><span class="field-section-index">3</span><div><b>入口照片</b><span>先完成第 1 张；进入现场后再完成第 2 张</span></div></div>
            <div class="field-photo-journey"><div class="field-photo-journey-head"><b>现场照片进度</b><span class="field-photo-count">1 / 2 · 当前拍入口</span></div><div class="field-photo-steps"><div class="field-photo-step now"><strong>1</strong><div><b>入口或门口</b><span>现在拍，保存后开始</span></div></div><div class="field-photo-step pending"><strong>2</strong><div><b>现场或办公区域</b><span>进入现场后拍，结束前必填</span></div></div></div>${photoPickerHtml('fieldEntryPhoto','fieldEntryPreview','拍摄第 1 张：入口或门口','手机端直接打开后置相机；电脑端可从相册或文件中选择。')}<div class="field-photo-next"><b>第 2 张：</b>进入大棚、冷柜销售区或办公区域后，工作台会持续提醒；完成 2/2 后才可结束。</div></div>
            <div class="field-start-submit"><div id="fieldLocationState" class="field-location">请先确认位置并拍摄入口照；定位失败会记录原因，但不会清空已填写内容。</div><button id="fieldStartBtn" class="btn btn-primary">保存入口照并开始现场开发</button></div>
          </section>
        </form>
      </div>
      <div class="card card-pad section"><div class="section-title"><h2>最近现场记录</h2><span class="hint">每次开发完整留痕</span></div>${historyList(FS.data?.recent_sessions||[])}</div>
    </div>`;
    document.querySelector('#view').innerHTML=html;renderZoneChecks();const marketName=document.querySelector('#fieldMarketName'),workMode=document.querySelector('#fieldWorkMode');marketName.addEventListener('input',syncMarketEntry);marketName.addEventListener('change',syncMarketEntry);workMode.addEventListener('change',()=>{renderZoneChecks();syncMarketEntry()});bindPhotoPicker('fieldEntryPhoto','fieldEntryPreview',true);bindLocationPanel();syncMarketEntry();document.querySelector('#fieldStartForm').onsubmit=startSubmit
  }
  async function startSubmit(e){
    e.preventDefault();const form=e.currentTarget,b=document.querySelector('#fieldStartBtn'),state=document.querySelector('#fieldLocationState'),x=Object.fromEntries(new FormData(form)),zones=Array.from(form.querySelectorAll('[name="planned_zone"]:checked')).map(z=>z.value),file=document.querySelector('#fieldEntryPhoto').files?.[0],id=uuid(),place=String(x.custom_market_name||'').trim(),city=String(x.custom_city||'').trim();let uploaded=null;if(place.length<2)return toast('请填写市场或办公场地名称');if(!city)return toast('请填写所在城市或区域');if(!file)return toast('请拍摄第1张入口或门口照片');b.disabled=true;b.textContent='正在确认当前位置…';state.className='field-location';state.textContent='正在确认手机定位，请稍候…';
    try{const loc=locationFresh(FS.currentLocation)?FS.currentLocation:await window.fieldAcquireStartLocation(true);state.textContent=locationLabel(loc)+(loc.status==='unavailable'?'；系统会记录原因并继续，不需要重新填写':'；地图位置将随本次现场记录保存');state.className='field-location '+(loc.status==='verified'?'good':'warn');b.textContent='正在压缩并上传入口照片…';uploaded=await uploadPhoto(file,id,'entry');b.textContent='正在建立现场记录…';await api({action:'start_session',session_id:id,market_id:x.market_id||null,custom_market_name:place,custom_city:city,custom_address:String(x.custom_address||'').trim()||null,work_mode:x.work_mode,focus_fruits:arrays(x.focus_fruits),planned_zones:zones,target_contacts:x.target_contacts,entry_photo_path:uploaded.path,mime_type:uploaded.mime_type,file_size_bytes:uploaded.file_size_bytes,captured_at:new Date().toISOString(),location_status:loc.status,location_exception:loc.reason||null,latitude:loc.latitude,longitude:loc.longitude,accuracy_m:loc.accuracy});toast('入口照片已保存；进入现场后请拍第2张');FS.currentLocation=null;await fieldReload()}catch(z){await discardUpload(id,uploaded?.path);toast(z.message);b.disabled=false;b.textContent='保存入口照并开始现场开发'}
  }
  function metricsCards(m){m=m||{};return '<div class="field-kpis"><div class="field-kpi"><b>'+E(m.contacts_total||0)+'</b><span>接触客户</span></div><div class="field-kpi"><b>'+E(m.meaningful_contacts||0)+'</b><span>有效沟通</span></div><div class="field-kpi"><b>'+E(m.priority_leads||0)+'</b><span>重点客户</span></div><div class="field-kpi"><b>'+E(m.followup_actions||0)+'</b><span>明确下一步</span></div></div>'}
  function requiredSecondEvidence(s,ev){return s?.work_mode==='office_cluster'?ev.some(x=>x.evidence_type==='overview_photo'||x.evidence_type==='office_door_photo'):ev.some(x=>x.evidence_type==='overview_photo')}
  function photoProgressHtml(s,ev){const hasEntry=ev.some(x=>x.evidence_type==='entry_photo'),hasSecond=requiredSecondEvidence(s,ev),count=Number(hasEntry)+Number(hasSecond),second=s.work_mode==='office_cluster'?'办公区域或客户门口':'大棚整体现场';return '<div class="field-photo-progress-card '+(hasEntry&&hasSecond?'complete':'')+'"><div class="field-photo-progress-head"><b>现场照片 '+count+'/2</b><span>'+(hasEntry&&hasSecond?'最低要求已完成':'还差第2张必拍照片')+'</span></div><div class="field-photo-progress-track"><div class="field-photo-progress-fill" style="width:'+(count*50)+'%"></div></div><div class="field-photo-steps"><div class="field-photo-step '+(hasEntry?'done':'pending')+'"><strong>'+(hasEntry?'✓':'1')+'</strong><div><b>入口或门口</b><span>'+(hasEntry?'已安全保存':'照片记录缺失')+'</span></div></div><div class="field-photo-step '+(hasSecond?'done':'now')+'"><strong>'+(hasSecond?'✓':'2')+'</strong><div><b>'+E(second)+'</b><span>'+(hasSecond?'已安全保存':'进入现场后拍摄')+'</span></div></div></div><div class="field-photo-progress-actions"><div class="muted">照片只用于本次现场治理，不要求逐客户拍照。</div>'+(!hasSecond?'<button class="btn btn-primary" onclick="fieldEvidenceModal()">拍摄第2张现场照片</button>':'<button class="btn btn-soft" onclick="fieldEvidenceModal()">按需补充更多照片</button>')+'</div></div>'}
  function activeView(s){
    const m=s.deterministic_metrics||{},ev=FS.data.active_evidence||[],ts=FS.data.active_touchpoints||[],market=(FS.data.markets||[]).find(x=>x.id===s.market_id),zones=s.work_mode==='office_cluster'?['办公室区']:(market?.zones?.length?market.zones:['A区','B区','C区','D区','办公室区']),hasSecond=requiredSecondEvidence(s,ev);
    if(!FS.zone)FS.zone=s.zones_visited?.slice(-1)[0]||s.planned_zones?.[0]||zones[0];
    let html=heading('市场现场开发','现场快速记录，结束后自动生成个人复盘。','<button class="btn btn-soft" onclick="fieldReload()">刷新</button>'+(FS.data.can_view_team?'<button class="btn btn-soft" onclick="fieldTeamView()">团队市场洞察</button>':''));
    html+='<div class="field-hero"><div class="field-hero-top"><div><span class="field-live">现场进行中</span><h2>'+E(s.market_name)+'</h2><div class="muted">开始 '+E(D(s.started_at))+' · <span id="fieldElapsed"></span> · '+E(s.work_mode==='office_cluster'?'办公室集中拜访':'批发市场大棚')+'</div></div><button class="btn btn-soft" onclick="fieldEndModal()">结束本次</button></div>'+metricsCards(m)+'</div>';
    html+=photoProgressHtml(s,ev);
    html+='<div class="card card-pad section"><div class="section-title"><h2>当前区域</h2><span class="hint">切换区域只需点一下</span></div><div class="field-zones">'+zones.map(x=>'<button class="field-zone '+(FS.zone===x?'active':'')+'" onclick="fieldSwitchZone(\''+E(x)+'\')">'+E(x)+'</button>').join('')+'</div><div class="field-evidence-row">'+ev.map(x=>'<button class="field-evidence-chip" onclick="fieldOpenPhoto(\''+E(x.id)+'\')">'+(x.evidence_type==='entry_photo'?'✓ 入口照片':x.evidence_type==='overview_photo'?'✓ 整体现场 '+E(x.market_zone||''):'✓ 办公室门口 '+E(x.market_zone||''))+'</button>').join('')+(!hasSecond?'<span class="field-evidence-chip missing">必拍：还需第2张现场照片</span>':'')+'</div></div>';
    html+='<div class="field-actions"><button class="field-action primary" onclick="fieldTouchModal(\'quick\')"><b>＋ 快速记录客户</b><span>20–30秒，适合现场连续开发</span></button><button class="field-action" onclick="fieldTouchModal(\'effective\')"><b>有效沟通</b><span>记录货、路、痛点和下一步</span></button><button class="field-action" onclick="fieldTouchModal(\'priority\')"><b>重点客户访谈</b><span>15–20分钟，形成可跟进资产</span></button><button class="field-action '+(!hasSecond?'required':'')+'" onclick="fieldEvidenceModal()"><b>'+(!hasSecond?'拍摄第2张现场照':'补充现场照片')+'</b><span>'+(!hasSecond?'完成2/2后才可结束本次':'超过2张仅按实际需要补充')+'</span></button></div>';
    html+='<div class="card card-pad"><div class="section-title"><h2>本次客户记录</h2><span class="hint">快速记录不会自动污染CRM</span></div>'+touchList(ts)+'</div><div class="card card-pad section"><div class="section-title"><h2>现场治理</h2></div><div class="field-governance">至少2张现场照片、定位状态、服务器时间、区域切换、客户记录、CRM同步和AI分析全部留痕。业务员只能查看自己的记录，最高权限可查看全局；AI整理必须人工确认，固定评分仍由系统规则计算。</div><div class="actions" style="margin-top:10px"><button class="btn btn-soft" onclick="fieldCancelModal()">误开记录 / 取消本次</button></div></div>';
    document.querySelector('#view').innerHTML=html;clearInterval(FS.timer);const tick=()=>{const el=document.querySelector('#fieldElapsed');if(el)el.textContent=Math.max(0,Math.round((Date.now()-new Date(s.started_at))/60000))+'分钟'};tick();FS.timer=setInterval(tick,30000)
  }
  function touchList(rows){if(!rows?.length)return '<div class="field-empty">尚未记录客户。进入大棚后，点击“快速记录客户”开始。</div>';return '<div class="field-feed">'+rows.map(x=>'<div class="field-feed-item"><div><span class="field-level">'+E(LEVEL[x.record_level]?.[0]||x.record_level)+'</span><div class="muted" style="margin-top:5px">'+E(x.market_zone||'-')+'</div></div><div class="field-feed-main"><b>'+E(x.company_name||x.contact_name||'临时客户记录')+'</b><div>'+E((x.fruits||[]).join('、')||'水果待补')+' · '+E(INTEREST[x.interest_level]||x.interest_level)+'</div><div>'+E(x.outcome||x.customer_quote||x.raw_note||'')+'</div></div><div class="field-record-actions">'+((x.customer_id||x.synced_visit_id)?'<span class="badge b-green">已同步CRM</span>':'<span class="badge b-gray">现场记录</span>')+'<button class="btn btn-soft" onclick="fieldEditTouchpoint(\''+E(x.id)+'\')">修正</button></div></div>').join('')+'</div>'}
  function historyList(rows){if(!rows?.length)return '<div class="field-empty">还没有现场开发记录。</div>';return '<div class="field-feed">'+rows.map(x=>'<div class="field-feed-item"><div><span class="field-level">'+E(x.status==='active'?'进行中':x.status==='completed'?'已完成':'已取消')+'</span></div><div class="field-feed-main"><b>'+E(x.market_name)+'</b><div>'+E(D(x.started_at))+' · '+E((x.zones_visited||[]).join('、')||'区域待记录')+'</div><div>客户 '+E(x.deterministic_metrics?.contacts_total||0)+' · 有效 '+E(x.deterministic_metrics?.meaningful_contacts||0)+' · 重点 '+E(x.deterministic_metrics?.priority_leads||0)+'</div></div><div><button class="btn btn-soft" onclick="fieldSessionDetail(\''+E(x.id)+'\')">查看</button></div></div>').join('')+'</div>'}
  function paintField(source,started){if(!FS.data)return;document.documentElement.classList.add('field-screen');if(FS.data.active_session)activeView(FS.data.active_session);else startView();const view=document.querySelector('#view'),ms=Math.round(performance.now()-started);view?.querySelectorAll('button[onclick="fieldReload()"]')?.forEach(b=>b.setAttribute('onclick','fieldReload(true)'));if(view){view.dataset.fonkonFieldSource=source;view.dataset.fonkonFieldMs=String(ms);view.dataset.fonkonFieldApiMs=String(FS.lastApiMs||0)}document.documentElement.dataset.fonkonFieldSource=source;document.documentElement.dataset.fonkonFieldMs=String(ms)}
  window.fieldReload=async function(force=false){FS.screen='mine';clearInterval(FS.timer);restoreFieldSnapshot();const started=performance.now(),had=!!FS.data,effectiveForce=!!force||FS.dirty,age=Date.now()-FS.loadedAt;if(had){paintField(FS.loadedAt?'memory':'stale',started);if(!effectiveForce&&age<60000)return FS.data;if(FS.loadFlight)return effectiveForce?FS.loadFlight:FS.data}else if(FS.loadFlight)return FS.loadFlight;else document.querySelector('#view').innerHTML=heading('市场现场开发','正在读取现场记录…')+'<div class="card field-loading">正在安全读取数据…</div>';const run=(async()=>{try{const data=await api({action:'bootstrap'});FS.data=data;FS.loadedAt=Date.now();FS.dirty=false;saveFieldSnapshot();if(FS.screen==='mine'&&window.FONKON_R11?.currentView==='field')paintField('network',started);return data}catch(e){if(!had&&FS.screen==='mine'){document.querySelector('#view').innerHTML=heading('市场现场开发','加载失败')+'<div class="card card-pad"><div class="notice warn">'+E(e.message)+'</div><button class="btn btn-primary" style="margin-top:10px" onclick="fieldReload(true)">重新加载</button></div>'}else{document.documentElement.dataset.fonkonFieldSource='stale';if(effectiveForce)toast('现场数据暂时未能更新，已保留上次结果')}return FS.data}finally{FS.loadFlight=null}})();FS.loadFlight=run;if(had&&!effectiveForce){run.catch(()=>{});return FS.data}return run}
  window.fieldSwitchZone=async function(zone){FS.zone=zone;try{await api({action:'switch_zone',session_id:FS.data.active_session.id,market_zone:zone});FS.data.active_session.zones_visited=Array.from(new Set([...(FS.data.active_session.zones_visited||[]),zone]));activeView(FS.data.active_session)}catch(e){toast(e.message)}}
  function option(values,current=''){return values.map(x=>'<option value="'+E(Array.isArray(x)?x[0]:x)+'" '+((Array.isArray(x)?x[0]:x)===current?'selected':'')+'>'+E(Array.isArray(x)?x[1]:x)+'</option>').join('')}
  window.fieldTouchModal=function(level){const info=LEVEL[level]||LEVEL.quick,isQuick=level==='quick',isPriority=level==='priority'||level==='office_visit',crm=Array.isArray(customers)?customers:[],zone=FS.zone||'';FS.aiExtraction=null;FS.sourceMethod='manual';let body='<div class="field-form-note"><b>'+E(info[0])+'：</b>'+E(info[1])+'。只记录明确事实，未知保持待补。</div><form id="fieldTouchForm" class="grid" style="margin-top:10px"><input type="hidden" name="record_level" value="'+E(level)+'"><input type="hidden" name="market_zone" value="'+E(zone)+'">'+input('company_name',isQuick?'客户/公司简称':'公司或客户简称*','','text','span6',isQuick?'placeholder="例如：A区榴莲客人"':'required')+input('contact_name','联系人','','text','span6')+(isQuick?'':('<div class="field span6"><label>绑定已有CRM客户</label><select name="customer_id"><option value="">不绑定 / 可新建</option>'+crm.map(c=>'<option value="'+E(c.id)+'">'+E(c.company_name)+' · '+E(c.contact_name||'')+'</option>').join('')+'</select></div>'+input('phone_wechat','手机/微信','','text','span6')))+'<div class="field span4"><label>客户角色</label><select name="customer_type"><option value="">待确认</option>'+option(['货主','代卖','货主+代卖','采购/渠道','其他'])+'</select></div><div class="field span4"><label>联系人角色</label><select name="contact_role"><option value="">待确认</option>'+option(['老板/最终决策人','进口/物流负责人','采购','档口负责人','业务员','影响者'])+'</select></div><div class="field span4"><label>沟通结果</label><select name="interest_level">'+option(Object.entries(INTEREST),isQuick?'brief':level==='priority'?'priority':'effective')+'</select></div>'+input('fruits','水果','','text','span6','placeholder="榴莲、车厘子"')+(isQuick?'':input('origin_countries','来源国','','text','span6','placeholder="泰国、智利"'))+(isPriority?input('volume_range','柜量事实/区间','','text','span6','placeholder="客户原话，例如：旺季每月约5–8柜"'):'')+(isQuick?'':input('current_ports','当前口岸/路线','','text','span6','placeholder="南沙、盐田"'))+(isQuick?'':input('pain_points','明确痛点','','text','span6','placeholder="排队、查验、送检、时效不稳"'))+(isPriority?'<div class="field span6"><label>真实决策权</label><select name="decision_role"><option value="">待确认</option>'+option(['无决策权','影响者','参与决策','进口/物流负责人','老板/最终决策人'])+'</select></div>':'')+'<div class="field span12"><label>现场口述/速记</label><div class="field-voice"><textarea id="fieldRawNote" name="raw_note" placeholder="例如：A区做泰国榴莲，老板说旺季南沙提柜时间不稳定，愿意下周看江门试柜资料"></textarea><button type="button" class="btn btn-soft" id="fieldVoiceBtn">🎙 语音输入</button></div><div class="actions" style="margin-top:7px"><button type="button" class="btn btn-soft" id="fieldAiBtn">AI整理为待确认字段</button></div></div>'+(isQuick?'':('<div class="field span12"><label>客户原话</label><textarea name="customer_quote"></textarea></div>'))+'<div class="field span8"><label>结果/下一步事实</label><input name="outcome" placeholder="本次沟通结果"></div>'+input('next_action','下一步','','text','span8','placeholder="发送路线资料/约二访/补一票数据"')+input('next_followup_at','跟进时间','','datetime-local','span4')+'<div id="fieldAiConfirmBox" class="span12 field-ai-confirm"><label><input id="fieldAiConfirm" type="checkbox"> 我已逐项核对AI整理内容；确认后才作为事实保存</label><div id="fieldAiWarnings" class="muted" style="margin-top:5px"></div></div>'+(isQuick?'':'<div class="span12"><label><input type="checkbox" name="sync_crm" value="true" checked> 保存后同步CRM客户与正式拜访；若是新客户将自动建立客户卡</label></div>')+'<div id="fieldTouchStatus" class="span12 muted"></div><div class="span12 actions"><button id="fieldTouchSave" class="btn btn-primary">保存'+E(info[0])+'</button><button type="button" class="btn btn-soft" onclick="closeModal()">取消</button></div></form>';modal(info[0]+' · '+E(zone),body);const form=document.querySelector('#fieldTouchForm');form.onsubmit=fieldTouchSubmit;document.querySelector('#fieldVoiceBtn').onclick=fieldVoice;document.querySelector('#fieldAiBtn').onclick=fieldExtractNote}
  window.fieldVoice=function(){const SR=window.SpeechRecognition||window.webkitSpeechRecognition,b=document.querySelector('#fieldVoiceBtn'),ta=document.querySelector('#fieldRawNote');if(!SR)return toast('当前浏览器不支持直接语音识别，请使用手机键盘的语音输入');const r=new SR();r.lang='zh-CN';r.interimResults=false;r.continuous=false;b.disabled=true;b.textContent='正在听…';r.onresult=e=>{const t=Array.from(e.results).map(x=>x[0].transcript).join('');ta.value=(ta.value?ta.value+'；':'')+t;FS.sourceMethod='voice_transcript'};r.onerror=()=>toast('语音识别未完成，可继续手动输入');r.onend=()=>{b.disabled=false;b.textContent='🎙 语音输入'};r.start()}
  window.fieldExtractNote=async function(){const raw=document.querySelector('#fieldRawNote').value.trim(),b=document.querySelector('#fieldAiBtn'),form=document.querySelector('#fieldTouchForm'),status=document.querySelector('#fieldTouchStatus');if(!raw)return toast('请先输入或口述现场记录');b.disabled=true;b.textContent='AI正在整理…';status.textContent='AI只提取明确事实，不会自动保存。';try{const r=await api({action:'extract_note',raw_note:raw,market_name:FS.data.active_session.market_name,market_zone:form.market_zone.value,record_level:form.record_level.value}),x=r.extraction||{},set=(n,v)=>{const el=form.elements[n];if(el&&v!==null&&v!==undefined&&String(v).trim()&&!String(el.value||'').trim())el.value=Array.isArray(v)?v.join('、'):v};['company_name','contact_name','contact_role','customer_type','volume_range','customer_quote','decision_role','interest_level','outcome','next_action','next_followup_at'].forEach(k=>set(k,x[k]));['fruits','origin_countries','current_ports','pain_points'].forEach(k=>set(k,x[k]));FS.aiExtraction=x;FS.sourceMethod='ai_assisted';document.querySelector('#fieldAiConfirmBox').style.display='block';document.querySelector('#fieldAiWarnings').textContent=(x.warnings||[]).concat(x.unknown_fields?.length?['待补：'+x.unknown_fields.join('、')]:[]).join('；');status.textContent=(r.ai?'DeepSeek已整理':'AI暂不可用，已使用固定规则整理')+'，请核对后勾选确认。'}catch(e){status.textContent=e.message;toast(e.message)}finally{b.disabled=false;b.textContent='AI整理为待确认字段'}}
  async function fieldTouchSubmit(e){
    e.preventDefault();const form=e.currentTarget,b=document.querySelector('#fieldTouchSave'),status=document.querySelector('#fieldTouchStatus'),x=Object.fromEntries(new FormData(form)),editing=!!x.touchpoint_id;
    if(FS.aiExtraction&&!document.querySelector('#fieldAiConfirm').checked)return toast('请核对AI整理内容并勾选确认');b.disabled=true;b.textContent='正在保存…';status.textContent='正在写入现场治理记录…';
    try{
      const r=await api({action:editing?'update_touchpoint':'save_touchpoint',touchpoint_id:x.touchpoint_id||null,session_id:FS.data.active_session.id,record_level:x.record_level,market_zone:x.market_zone,customer_id:x.customer_id||null,company_name:x.company_name,contact_name:x.contact_name,phone_wechat:x.phone_wechat,contact_role:x.contact_role,customer_type:x.customer_type,fruits:arrays(x.fruits),origin_countries:arrays(x.origin_countries),volume_range:x.volume_range,current_ports:arrays(x.current_ports),pain_points:arrays(x.pain_points),customer_quote:x.customer_quote,decision_role:x.decision_role,interest_level:x.interest_level,outcome:x.outcome,next_action:x.next_action,next_followup_at:x.next_followup_at||null,raw_note:x.raw_note,source_method:FS.sourceMethod,ai_extraction:FS.aiExtraction||{},ai_confirmed:!!FS.aiExtraction,sync_crm:!editing&&x.sync_crm==='true'});
      let photoWarning='',uploaded=null;const officeFile=!editing?document.querySelector('#fieldOfficePhoto')?.files?.[0]:null;
      if(officeFile){status.textContent='客户记录已保存，正在关联办公室门口照片…';try{const loc=await getLocation();uploaded=await uploadPhoto(officeFile,FS.data.active_session.id,'office_door_photo');await api({action:'add_evidence',session_id:FS.data.active_session.id,touchpoint_id:r.touchpoint.id,evidence_type:'office_door_photo',market_zone:x.market_zone,storage_path:uploaded.path,captured_at:new Date().toISOString(),location_status:loc.status,location_exception:loc.reason||null,latitude:loc.latitude,longitude:loc.longitude,accuracy_m:loc.accuracy,mime_type:uploaded.mime_type,file_size_bytes:uploaded.file_size_bytes,note:'办公室拜访门口照片'})}catch(z){await discardUpload(FS.data.active_session.id,uploaded?.path);photoWarning='；门口照片未保存：'+z.message}}
      closeModal();toast((editing?'现场记录已修正':'现场记录已保存'+(r.crm?'并同步CRM':''))+(r.crm_warning?'；'+r.crm_warning:'')+photoWarning);await fieldReload();
    }catch(z){status.textContent=z.message;toast(z.message);b.disabled=false;b.textContent='重新保存'}
  }
  window.fieldEvidenceModal=function(){const session=FS.data.active_session,missing=!requiredSecondEvidence(session,FS.data.active_evidence||[]),office=session.work_mode==='office_cluster'||(!missing&&FS.zone==='办公室区'),title=missing?'拍摄第2张现场照片':'补充现场照片';modal(title,'<div class="field-form-note"><b>'+(missing?'完成现场照片 2/2：':'按需补充：')+'</b> '+(session.work_mode==='office_cluster'?'拍办公室区域或实际拜访的客户门口。':'拍摄大棚、冷柜销售区的整体现场。')+' 不要求逐客户拍照；照片保存在私有空间，DeepSeek只分析你确认的现场标签。</div><form id="fieldEvidenceForm" class="grid" style="margin-top:10px"><div class="field span6"><label>照片类型</label><select id="fieldEvidenceType" name="evidence_type"><option value="overview_photo" '+(!office?'selected':'')+'>市场 / 办公区域整体现场</option><option value="office_door_photo" '+(office?'selected':'')+'>办公室或客户门口</option></select></div>'+input('note','照片说明','','text','span6','placeholder="例如：A区榴莲销售区整体现场"')+'<div id="fieldSceneFields" class="field span12" style="display:'+(!office?'block':'none')+'"><label>现场观察（按眼前事实点选，未知留空）</label><div class="field-scene-grid"><label>客流<select name="customer_flow"><option value="">待观察</option><option value="low">较少</option><option value="medium">正常</option><option value="high">较旺</option></select></label><label>冷柜销售<select name="cold_container_activity"><option value="">待观察</option><option value="quiet">较静</option><option value="normal">正常</option><option value="busy">繁忙</option></select></label><label>拥堵<select name="congestion"><option value="">待观察</option><option value="none">无明显拥堵</option><option value="moderate">轻度拥堵</option><option value="high">明显拥堵</option></select></label></div><div class="grid" style="margin-top:8px">'+input('displayed_fruits','现场陈列水果','','text','span6','placeholder="榴莲、龙眼"')+input('competitor_brands','可确认的竞品品牌','','text','span6','placeholder="未知留空"')+'<div class="span12"><label><input type="checkbox" name="price_sign_visible" value="true"> 看见促销标识（只记录是否出现，不记录内容）</label></div></div></div><div class="field span12"><label>现场照片*</label>'+photoPickerHtml('fieldEvidenceFile','fieldEvidencePreview',missing?'拍摄第2张现场照片':'拍摄或选择现场照片','点击打开手机后置相机，拍清楚整体环境；避免人脸、客户单据和敏感商务信息。')+'</div><div id="fieldEvidenceStatus" class="span12 muted"></div><div class="span12 actions"><button id="fieldEvidenceSave" class="btn btn-primary">'+(missing?'保存第2张并完成2/2':'上传并留痕')+'</button><button type="button" class="btn btn-soft" onclick="closeModal()">取消</button></div></form>');const type=document.querySelector('#fieldEvidenceType');bindPhotoPicker('fieldEvidenceFile','fieldEvidencePreview');type.onchange=()=>document.querySelector('#fieldSceneFields').style.display=type.value==='overview_photo'?'block':'none';document.querySelector('#fieldEvidenceForm').onsubmit=fieldEvidenceSubmit}
  async function fieldEvidenceSubmit(e){e.preventDefault();const form=e.currentTarget,b=document.querySelector('#fieldEvidenceSave'),state=document.querySelector('#fieldEvidenceStatus'),x=Object.fromEntries(new FormData(form)),file=document.querySelector('#fieldEvidenceFile').files?.[0],wasMissing=!requiredSecondEvidence(FS.data.active_session,FS.data.active_evidence||[]);let up=null;if(!file)return toast('请先拍摄或选择现场照片');b.disabled=true;b.textContent='正在定位和上传…';try{const loc=await getLocation();up=await uploadPhoto(file,FS.data.active_session.id,x.evidence_type);await api({action:'add_evidence',session_id:FS.data.active_session.id,evidence_type:x.evidence_type,market_zone:FS.zone,storage_path:up.path,captured_at:new Date().toISOString(),location_status:loc.status,location_exception:loc.reason||null,latitude:loc.latitude,longitude:loc.longitude,accuracy_m:loc.accuracy,mime_type:up.mime_type,file_size_bytes:up.file_size_bytes,note:x.note,scene_observations:{customer_flow:x.customer_flow,cold_container_activity:x.cold_container_activity,congestion:x.congestion,price_sign_visible:x.price_sign_visible==='true',displayed_fruits:arrays(x.displayed_fruits),competitor_brands:arrays(x.competitor_brands)}});closeModal();toast(wasMissing?'第2张现场照片已保存，现场照片2/2已完成':'现场照片与观察标签已安全保存');await fieldReload()}catch(z){await discardUpload(FS.data.active_session.id,up?.path);state.textContent=z.message;toast(z.message);b.disabled=false;b.textContent='重新上传'}}
  window.fieldOpenPhoto=async function(id){try{modal('现场照片','<div class="field-loading">正在生成10分钟临时查看地址…</div>');const r=await api({action:'photo_url',evidence_id:id});modal('现场照片','<div id="fieldPhotoState" class="field-loading">正在安全加载照片…</div><img id="fieldPhotoImage" class="field-photo-large" alt="现场证据照片" style="display:none"><div class="muted" style="margin-top:8px">照片保存在私有空间；本次查看地址10分钟后自动失效。</div>');const img=document.querySelector('#fieldPhotoImage'),state=document.querySelector('#fieldPhotoState');img.referrerPolicy='no-referrer';img.decoding='async';img.onload=()=>{img.style.display='block';state.style.display='none'};img.onerror=()=>{img.style.display='none';state.className='notice warn';state.innerHTML='照片地址暂时未能加载。<button class="btn btn-soft" style="margin-left:8px" id="fieldPhotoRetry">重新生成地址</button>';document.querySelector('#fieldPhotoRetry').onclick=()=>fieldOpenPhoto(id)};img.src=r.url}catch(e){modal('照片打开失败','<div class="notice warn">'+E(e.message)+'</div><button class="btn btn-primary" style="margin-top:10px" onclick="fieldOpenPhoto(\''+E(id)+'\')">重新尝试</button>')}}
  function analysisHtml(a,status){a=a||{};const fmt=v=>typeof v==='string'?v:v?.name?(v.name+'：'+[v.confirmed_data,v.suggestion].filter(Boolean).join('；')):v?.label?(v.label+(v.count!==undefined?' '+v.count:'')):JSON.stringify(v),items=x=>Array.isArray(x)?x:(x===null||x===undefined||x===''?[]:[x]),list=(title,x)=>{const xs=items(x);return '<div class="field-analysis-box"><h4>'+E(title)+'</h4>'+(xs.length?'<ul>'+xs.map(v=>'<li>'+E(fmt(v))+'</li>').join('')+'</ul>':'<div class="muted">暂无明确结论</div>')+'</div>'};return '<div class="notice '+(status==='completed'?'good':'')+'"><b>'+(status==='completed'?'DeepSeek AI分析':'固定规则复盘')+'：</b>'+E(a.headline||a.executive_summary||'已完成')+'</div><div class="field-analysis" style="margin-top:10px">'+list('已确认优势 / 市场发现',a.confirmed_strengths||a.market_expansion_findings)+list('风险与缺口',a.risks||a.data_gaps)+list('下一步行动',a.next_actions||a.next_30_days)+list('下次应问 / 区域策略',a.next_visit_questions||a.zone_strategy)+(a.salesperson_coaching?list('业务员具体辅导',a.salesperson_coaching):'')+(a.market_signals?list('市场现场信号',a.market_signals):'')+'</div><div class="muted" style="margin-top:8px">'+E(a.disclaimer||'AI建议不替代系统固定指标，未知数据不按0计算。')+'</div>'}
  window.fieldEndModal=function(){if(!requiredSecondEvidence(FS.data.active_session,FS.data.active_evidence||[])){toast(FS.data.active_session.work_mode==='office_cluster'?'结束前请拍第2张办公室区域或客户门口照片':'结束前请拍第2张市场整体现场照片');return fieldEvidenceModal()}modal('结束本次市场开发','<div class="field-form-note"><b>现场照片2/2已完成。</b> 结束后系统先计算固定指标，再由DeepSeek根据匿名事实生成复盘。</div><form id="fieldEndForm" class="grid" style="margin-top:10px"><div class="field span12"><label>收尾备注</label><textarea name="close_note" placeholder="例如：A/B区已覆盖，C区明天继续；重点客户已约周三二访"></textarea></div><div id="fieldEndStatus" class="span12 muted"></div><div class="span12 actions"><button id="fieldEndBtn" class="btn btn-primary">结束并生成复盘</button><button type="button" class="btn btn-soft" onclick="closeModal()">继续开发</button></div></form>');document.querySelector('#fieldEndForm').onsubmit=fieldEndSubmit}
  async function fieldEndSubmit(e){e.preventDefault();const b=document.querySelector('#fieldEndBtn'),status=document.querySelector('#fieldEndStatus'),x=Object.fromEntries(new FormData(e.currentTarget));b.disabled=true;b.textContent='正在完成固定指标…';status.textContent='随后将生成AI复盘，通常需要几秒钟。';try{const loc=await getLocation();b.textContent='正在生成个人复盘…';const r=await api({action:'end_session',session_id:FS.data.active_session.id,close_note:x.close_note,latitude:loc.latitude,longitude:loc.longitude,accuracy_m:loc.accuracy});modal('本次市场开发已完成','<div class="field-detail-grid"><div class="card card-pad"><b>'+E(r.metrics.contacts_total)+'</b><div class="muted">接触客户</div></div><div class="card card-pad"><b>'+E(r.metrics.meaningful_contacts)+'</b><div class="muted">有效沟通</div></div><div class="card card-pad"><b>'+E(r.metrics.priority_leads)+'</b><div class="muted">重点客户</div></div></div>'+analysisHtml(r.analysis,r.ai_status)+'<div class="actions" style="margin-top:12px"><button class="btn btn-primary" onclick="closeModal();fieldReload()">返回市场开发</button></div>')}catch(z){status.textContent=z.message;toast(z.message);b.disabled=false;b.textContent='重新结束并生成复盘'}}
  window.fieldCancelModal=function(){modal('取消本次现场记录','<div class="notice warn">只用于误操作、选错市场或测试记录；已上传入口照片和取消原因仍会保留治理记录。</div><form id="fieldCancelForm" class="grid"><div class="field span12"><label>取消原因*</label><input name="reason" required></div><div class="span12 actions"><button class="btn btn-primary">确认取消</button><button type="button" class="btn btn-soft" onclick="closeModal()">返回</button></div></form>');document.querySelector('#fieldCancelForm').onsubmit=async e=>{e.preventDefault();try{await api({action:'cancel_session',session_id:FS.data.active_session.id,reason:new FormData(e.currentTarget).get('reason')});closeModal();toast('本次现场记录已取消并留痕');fieldReload()}catch(z){toast(z.message)}}}
  function eventLabel(v){return ({session_started:'开始市场开发',evidence_added:'新增照片证据',zone_switched:'切换市场区域',touchpoint_created:'新增客户记录',touchpoint_updated:'修正客户记录',session_completed:'结束市场开发',session_cancelled:'取消现场记录',session_analysis_generated:'生成个人复盘',company_analysis_generated:'生成团队分析',field_market_reference_updated:'更新市场入口基准',field_market_submitted:'登记新场地'})[v]||v||'治理事件'}
  window.fieldSessionDetail=async function(id){try{
    modal('现场开发详情','<div class="field-loading">正在读取完整治理记录…</div>');const r=await api({action:'session_detail',session_id:id}),s=r.session,m=r.metrics,status=s.status==='active'?'进行中':s.status==='completed'?'已完成':'已取消',photos=(r.evidence||[]).map(x=>'<button class="field-evidence-chip" onclick="fieldOpenPhoto(\''+E(x.id)+'\')">'+E(x.evidence_type==='entry_photo'?'入口照片':x.evidence_type==='overview_photo'?'整体现场':'办公室门口')+' '+E(x.market_zone||'')+(x.touchpoint_id?' · 已关联拜访':'')+'</button>').join('')||'<span class="muted">暂无照片证据</span>',events=(r.events||[]).map(x=>'<div class="field-feed-item"><div><span class="field-level">'+E(D(x.created_at))+'</span></div><div class="field-feed-main"><b>'+E(eventLabel(x.event_type))+'</b><div>'+E(x.entity_type||'现场治理')+'</div></div></div>').join('')||'<div class="field-empty">暂无治理事件</div>',hasCoords=Number.isFinite(Number(s.start_lat))&&Number.isFinite(Number(s.start_lng)),mapBlock=hasCoords?mapPreviewHtml(s.start_lat,s.start_lng,'FONKON现场位置',true,s.start_accuracy_m)+'<button class="btn btn-soft" style="margin-top:8px" onclick="fieldOpenMapAt('+Number(s.start_lat)+','+Number(s.start_lng)+')">全屏查看位置</button>':'';
    modal('现场开发详情','<div class="field-form-note"><b>'+E(s.market_name)+'</b> · '+E(status)+'<br>业务员：'+E(r.salesperson_name||'未命名业务员')+'<br>'+E(D(s.started_at))+' 至 '+E(s.ended_at?D(s.ended_at):'进行中')+' · '+E((s.zones_visited||[]).join('、')||'区域待补')+'<br>定位：'+E(fenceLabel(s))+'<br>地图位置：'+E(sessionLocationText(s))+mapBlock+'</div><div class="field-detail-grid" style="margin-top:10px"><div class="card card-pad"><b>'+E(m.contacts_total)+'</b><div class="muted">接触客户</div></div><div class="card card-pad"><b>'+pct(m.effective_rate)+'</b><div class="muted">有效沟通率</div></div><div class="card card-pad"><b>'+pct(m.data_completeness)+'</b><div class="muted">记录完整度</div></div></div><div class="section-title" style="margin-top:12px"><h2>照片证据</h2></div><div class="field-evidence-row">'+photos+'</div><div class="section-title" style="margin-top:12px"><h2>客户记录</h2></div>'+touchList(r.touchpoints)+'<div class="section-title" style="margin-top:12px"><h2>治理轨迹</h2><span class="hint">'+E((r.events||[]).length)+'项不可变操作留痕</span></div><div class="field-feed">'+events+'</div>'+(s.ai_summary&&Object.keys(s.ai_summary).length?analysisHtml(s.ai_summary,s.ai_status):''))
  }catch(e){modal('详情加载失败','<div class="notice warn">'+E(e.message)+'</div>')}}
  function bars(items){if(!items?.length)return '<div class="field-empty">暂无数据</div>';const max=Math.max(...items.map(x=>x.count),1);return '<div class="field-bars">'+items.map(x=>'<div class="field-bar"><span>'+E(x.label)+'</span><div class="field-bar-track"><div class="field-bar-fill" style="width:'+Math.round(x.count/max*100)+'%"></div></div><b>'+E(x.count)+'</b></div>').join('')+'</div>'}
  function teamSessions(r){const names=Object.fromEntries((r.profiles||[]).map(x=>[x.id,x.name||'未命名业务员'])),tc={},ec={};for(const x of r.touchpoints||[])tc[x.session_id]=(tc[x.session_id]||0)+1;for(const x of r.evidence||[])ec[x.session_id]=(ec[x.session_id]||0)+1;const rows=(r.sessions||[]).slice(0,50).map(s=>{const status=s.status==='active'?'进行中':s.status==='completed'?'已完成':'已取消';return '<tr><td><b>'+E(names[s.created_by]||'未命名业务员')+'</b><div class="muted">'+E(status)+'</div></td><td>'+E(s.market_name)+'<div class="muted">'+E(D(s.started_at))+'</div></td><td>'+E((s.zones_visited||[]).join('、')||'待形成')+'</td><td>'+E(tc[s.id]||0)+'</td><td>'+E(ec[s.id]||0)+'</td><td>'+E(fenceLabel(s))+'</td><td><button class="btn btn-soft" onclick="fieldSessionDetail(\''+E(s.id)+'\')">查看完整记录</button></td></tr>'}).join('');return '<div class="card card-pad section"><div class="section-title"><h2>逐场治理记录</h2><span class="hint">最近50场 · 可查看照片、客户记录与不可变操作轨迹</span></div><div class="table-wrap"><table class="table field-team-table"><thead><tr><th>业务员/状态</th><th>市场/开始时间</th><th>覆盖区域</th><th>客户</th><th>照片</th><th>定位留痕</th><th>操作</th></tr></thead><tbody>'+(rows||'<tr><td colspan="7" class="muted">暂无现场记录</td></tr>')+'</tbody></table></div></div>'}
  window.fieldTeamView=async function(days){
    FS.screen='team';
    FS.teamDays=days||FS.teamDays;document.querySelector('#view').innerHTML=heading('团队市场洞察','正在汇总匿名现场事实…','<button class="btn btn-soft" onclick="fieldReload()">返回我的现场</button>')+'<div class="card field-loading">正在计算团队市场数据…</div>';
    try{const r=await api({action:'team_dashboard',days:FS.teamDays}),m=r.metrics,aiButton='<button class="btn btn-primary" onclick="fieldTeamAI()">'+(r.ai_configured?'生成AI拓展建议':'生成固定规则建议')+'</button>';let html=heading('团队市场洞察','不按单纯打卡量评价；看有效沟通、重点客户、证据质量、跟进闭环和数据完整度。','<button class="btn btn-soft" onclick="fieldReload()">返回我的现场</button>'+aiButton);
      html+='<div class="field-tabs"><button class="btn btn-soft '+(FS.teamDays===7?'active':'')+'" onclick="fieldTeamView(7)">近7天</button><button class="btn btn-soft '+(FS.teamDays===30?'active':'')+'" onclick="fieldTeamView(30)">近30天</button><button class="btn btn-soft '+(FS.teamDays===90?'active':'')+'" onclick="fieldTeamView(90)">近90天</button></div>'+(r.ai_configured?'':'<div class="notice warn section"><b>DeepSeek尚未配置：</b>当前可生成固定规则建议，系统不会把规则回退冒充AI结果。</div>')+'<div class="kpis section"><div class="card kpi"><div class="kpi-label">现场开发场次</div><div class="kpi-value">'+E(m.sessions)+'</div><div class="kpi-foot">'+E(m.market_days)+'个开发日</div></div><div class="card kpi"><div class="kpi-label">客户接触</div><div class="kpi-value">'+E(m.contacts_total)+'</div><div class="kpi-foot">有效 '+E(m.meaningful_contacts)+'</div></div><div class="card kpi"><div class="kpi-label">重点客户</div><div class="kpi-value">'+E(m.priority_leads)+'</div><div class="kpi-foot">形成下一步 '+E(m.followup_actions)+'</div></div><div class="card kpi"><div class="kpi-label">CRM沉淀</div><div class="kpi-value">'+E(m.crm_synced)+'</div><div class="kpi-foot">有效沟通率 '+pct(m.effective_rate)+'</div></div></div>';
      html+='<div class="field-detail-grid section"><div class="card card-pad"><b>'+pct(m.entry_photo_coverage)+'</b><div class="muted">入口照片覆盖</div></div><div class="card card-pad"><b>'+pct(m.overview_photo_coverage)+'</b><div class="muted">大棚整体照片覆盖</div></div><div class="card card-pad"><b>'+pct(m.office_photo_link_rate)+'</b><div class="muted">办公室照片关联拜访</div></div><div class="card card-pad"><b>'+E(m.gps_verified_sessions)+' / '+E(m.sessions)+'</b><div class="muted">高精度GPS场次</div></div><div class="card card-pad"><b>'+E(m.geofence_configured_sessions)+'</b><div class="muted">已启用入口基准场次</div></div><div class="card card-pad"><b>'+E(m.geofence_outside_sessions)+'</b><div class="muted">范围外留痕（不阻断）</div></div></div>';
      html+='<div class="card card-pad section"><div class="section-title"><h2>业务员现场成果</h2><span class="hint">排序优先看重点客户和有效沟通，不是打卡次数</span></div><div class="table-wrap"><table class="table field-team-table"><thead><tr><th>业务员</th><th>市场日/场次</th><th>接触</th><th>有效</th><th>重点</th><th>有效率</th><th>跟进率</th><th>完整度</th><th>每小时接触</th></tr></thead><tbody>'+m.salespeople.map(x=>'<tr><td><b>'+E(x.name)+'</b></td><td>'+E(x.market_days)+' / '+E(x.sessions)+'</td><td>'+E(x.contacts)+'</td><td>'+E(x.meaningful_contacts)+'</td><td><b>'+E(x.priority)+'</b></td><td>'+pct(x.effective_rate)+'</td><td>'+pct(x.followup_rate)+'</td><td>'+pct(x.data_completeness)+'</td><td>'+E(x.contacts_per_hour??'待形成')+'</td></tr>').join('')+'</tbody></table></div></div>';
      html+=teamSessions(r)+'<div class="two section"><div class="card card-pad"><div class="section-title"><h2>开发市场覆盖</h2></div>'+bars(m.markets)+'</div><div class="card card-pad"><div class="section-title"><h2>照片现场观察</h2><span class="hint">业务员人工确认标签</span></div>'+bars(m.scene_signals)+'</div><div class="card card-pad"><div class="section-title"><h2>区域热度</h2></div>'+bars(m.zones)+'</div><div class="card card-pad"><div class="section-title"><h2>水果机会</h2></div>'+bars(m.fruits)+'</div><div class="card card-pad"><div class="section-title"><h2>客户痛点</h2></div>'+bars(m.pain_points)+'</div><div class="card card-pad"><div class="section-title"><h2>当前口岸</h2></div>'+bars(m.current_ports)+'</div></div><div id="fieldTeamAIBox"></div>';document.querySelector('#view').innerHTML=html;window.__fieldTeamData=r;
    }catch(e){document.querySelector('#view').innerHTML=heading('团队市场洞察','加载失败')+'<div class="notice warn">'+E(e.message)+'</div>'}
  }
  window.fieldTeamAI=async function(){const box=document.querySelector('#fieldTeamAIBox'),configured=!!window.__fieldTeamData?.ai_configured;box.innerHTML='<div class="card field-loading section">'+(configured?'DeepSeek正在分析团队匿名事实与固定指标…':'正在生成固定规则建议…')+'</div>';try{const r=await api({action:'team_analysis',days:FS.teamDays}),meta=r.provider==='deepseek'?'DeepSeek '+(r.model||''):'固定规则回退'+(r.error?' · '+r.error:'');box.innerHTML='<div class="card card-pad section"><div class="section-title"><h2>市场拓展建议</h2><span class="hint">'+E(meta)+'</span></div>'+analysisHtml(r.analysis,r.ai_status)+'</div>';box.scrollIntoView({behavior:'smooth',block:'start'})}catch(e){box.innerHTML='<div class="notice warn section">'+E(e.message)+'</div>'}}
  window.fieldMarketModal=function(){const markets=FS.data?.markets||[],rows=markets.map(m=>'<div class="field-feed-item"><div><span class="field-level">'+E(m.city)+'</span></div><div class="field-feed-main"><b>'+E(m.name)+'</b><div>'+(m.reference_lat!==null&&m.reference_lng!==null?'已设置入口定位 · 范围 '+E(m.geofence_radius_m||500)+' 米':'尚未设置入口定位，暂不判断市场范围')+'</div></div><div><input id="fieldRadius-'+E(m.id)+'" type="number" min="50" max="5000" value="'+E(m.geofence_radius_m||500)+'" style="width:90px;margin-bottom:5px"><button type="button" class="btn btn-soft" onclick="fieldSetMarketReference(\''+E(m.id)+'\')">在入口设置基准</button></div></div>').join('');modal('维护开发市场','<div class="field-form-note">请由最高权限站在市场入口设置一次定位基准。以后只作为真实性留痕，超出范围不会阻断业务员工作。</div><div class="field-feed" style="margin-top:10px">'+rows+'</div><div class="section-title" style="margin-top:14px"><h2>新增市场/办公室区</h2></div><form id="fieldMarketForm" class="grid">'+input('name','市场/办公区名称*','','text','span8','required')+input('city','城市*','广州','text','span4','required')+input('address','地址','','text','span12')+'<div class="field span6"><label>类型</label><select name="market_type"><option value="fruit_wholesale">水果批发市场</option><option value="office_cluster">办公室集中区</option><option value="other">其他</option></select></div>'+input('zones','区域','','text','span6','placeholder="A区、B区、C区、办公室区"')+'<div class="span12 actions"><button class="btn btn-primary">保存新市场</button><button type="button" class="btn btn-soft" onclick="closeModal()">关闭</button></div></form>');document.querySelector('#fieldMarketForm').onsubmit=async e=>{e.preventDefault();const x=Object.fromEntries(new FormData(e.currentTarget));try{await api({action:'add_market',...x,zones:arrays(x.zones)});closeModal();toast('市场已保存，可再次打开设置入口基准');fieldReload()}catch(z){toast(z.message)}}}
  window.fieldSetMarketReference=async function(id){const radius=document.querySelector('#fieldRadius-'+id)?.value||500;toast('正在获取市场入口定位…');try{const loc=await getLocation();if(loc.status==='unavailable')throw Error(loc.reason);await api({action:'update_market_reference',market_id:id,latitude:loc.latitude,longitude:loc.longitude,accuracy_m:loc.accuracy,geofence_radius_m:radius});toast('市场入口基准已设置，超出范围仍不会阻断工作');closeModal();await fieldReload()}catch(e){toast(e.message)}}

  const baseTouchModal=window.fieldTouchModal;
  window.fieldTouchModal=function(level){baseTouchModal(level);if(level==='office_visit'){const form=document.querySelector('#fieldTouchForm'),status=document.querySelector('#fieldTouchStatus'),wrap=document.createElement('div');wrap.className='field span12';wrap.id='fieldOfficePhotoWrap';wrap.innerHTML='<label>办公室门口照片（可选，拍摄后自动绑定本次拜访）</label>'+photoPickerHtml('fieldOfficePhoto','fieldOfficePreview','拍摄办公室门口照片','可选；照片会与本次办公室拜访记录自动关联');form.insertBefore(wrap,status);bindPhotoPicker('fieldOfficePhoto','fieldOfficePreview')}}
  window.fieldEditTouchpoint=function(id){const t=(FS.data?.active_touchpoints||[]).find(x=>x.id===id);if(!t)return toast('记录已刷新，请重新打开');window.fieldTouchModal(t.record_level);const form=document.querySelector('#fieldTouchForm'),hidden=document.createElement('input');hidden.type='hidden';hidden.name='touchpoint_id';hidden.value=t.id;form.prepend(hidden);document.querySelector('#fieldOfficePhotoWrap')?.remove();const value=(name,v)=>{const el=form.elements[name];if(!el||v===null||v===undefined)return;el.value=Array.isArray(v)?v.join('、'):v};['record_level','market_zone','customer_id','company_name','contact_name','phone_wechat','contact_role','customer_type','fruits','origin_countries','volume_range','current_ports','pain_points','customer_quote','decision_role','interest_level','outcome','next_action','raw_note'].forEach(k=>value(k,t[k]));if(t.next_followup_at){const d=new Date(t.next_followup_at);value('next_followup_at',new Date(d.getTime()-d.getTimezoneOffset()*60000).toISOString().slice(0,16))}FS.sourceMethod=t.source_method||'manual';const sync=form.elements.sync_crm;if(sync){sync.checked=false;sync.disabled=true;sync.closest('div').insertAdjacentHTML('beforebegin','<div class="span12 field-form-note">修正只更新现场记录，避免重复建立CRM拜访；已同步内容可在客户中心按需修正。</div>')}document.querySelector('#fieldTouchSave').textContent='保存修正';document.querySelector('#fieldTouchStatus').textContent='只记录事实修正，并保留变更字段审计。'}

  const baseActiveView=activeView;activeView=function(session){baseActiveView(session);const hero=document.querySelector('.field-hero'),age=(Date.now()-new Date(session.started_at).getTime())/3600000,notice=document.createElement('div'),hasCoords=Number.isFinite(Number(session.start_lat))&&Number.isFinite(Number(session.start_lng));notice.className='field-location '+(session.start_geofence_status==='outside'||age>8?'warn':'good');notice.style.marginTop='10px';notice.innerHTML='<b>定位留痕：</b>'+E(fenceLabel(session))+'<br><b>地图位置：</b>'+E(sessionLocationText(session))+(hasCoords?mapPreviewHtml(session.start_lat,session.start_lng,'FONKON现场位置',true,session.start_accuracy_m)+'<button class="btn btn-soft" style="margin-top:8px" onclick="fieldOpenMapAt('+Number(session.start_lat)+','+Number(session.start_lng)+')">全屏查看位置</button>':'')+(age>8?'<br><b>跨时提醒：</b>本次已进行超过8小时，数据仍安全保留；若已离场，建议结束并生成复盘。':'');hero?.insertAdjacentElement('afterend',notice);const actions=document.querySelector('.field-actions');if(actions&&!actions.querySelector('[data-office-visit]')){const b=document.createElement('button');b.className='field-action';b.dataset.officeVisit='1';b.innerHTML='<b>办公室拜访</b><span>逐户门口取证，形成正式客户拜访</span>';b.onclick=()=>fieldTouchModal('office_visit');actions.insertBefore(b,actions.lastElementChild)}};
  const prevRender=window.render;window.render=async function(v){document.documentElement.classList.toggle('field-screen',v==='field');if(v==='field'){try{localStorage.setItem('fonkon_last_view','field');sessionStorage.setItem('fonkon_last_view','field');if(window.FONKON_R11)window.FONKON_R11.currentView='field';setNav('field')}catch{};return prevRender(v)}return prevRender(v)};
  const prevShow=window.showApp;window.showApp=function(){const r=prevShow.apply(this,arguments);setTimeout(fieldNav,0);return r};
  const prevTools=window.mobileTools;if(typeof prevTools==='function')window.mobileTools=function(){prevTools();setTimeout(()=>{const g=document.querySelector('#modalRoot .tool-grid');if(g&&!g.querySelector('[data-field-tool]')){const b=document.createElement('button');b.type='button';b.className='tool-tile';b.dataset.fieldTool='1';b.innerHTML='<b>⌖ 市场开发</b><span>两阶段拍照、分区记录、AI复盘</span>';b.onclick=()=>{closeModal();window.render('field')};g.prepend(b)}},0)};
  window.fieldView=fieldReload;setTimeout(fieldNav,50);
})();
