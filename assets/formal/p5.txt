</th><th>逻辑</th><th>操作</th></tr></thead><tbody>'+rules.map(r=>'<tr><td>'+esc(r.fruit)+'</td><td>'+esc(r.cargo_group)+'</td><td><b>'+fmt(r.q_score)+'</b></td><td>'+esc(r.rationale||'')+'</td><td><button class="btn btn-soft" onclick="editFruitRule(&quot;'+r.id+'&quot;,'+r.q_score+')">调整</button></td></tr>').join('')+'</tbody></table></div></div>';}
function render(v){setNav(v);if(v==='customers')return customersView();if(v==='opportunities')return oppsView();if(v==='visits')return visitsView();if(v==='tasks')return tasksView();if(v==='manager')return managerView();if(v==='knowledge')return knowledgeView();if(v==='team')return teamView();if(v==='settings')return settingsView();return todayDashboard()}
function modal(title,body){$('#modalRoot').innerHTML='<div class="modal-back" onclick="if(event.target===this)closeModal()"><div class="modal"><div class="modal-head"><div><b>'+title+'</b></div><button class="x" onclick="closeModal()">×</button></div><div class="modal-body">'+body+'</div></div></div>'}function closeModal(){$('#modalRoot').innerHTML=''}
function options(arr,val=''){return arr.map(x=>'<option '+(x===val?'selected':'')+'>'+x+'</option>').join('')}
function input(name,label,value='',type='text',cls='span4',extra=''){return '<div class="field '+cls+'"><label>'+label+'</label><input name="'+name+'" type="'+type+'" value="'+esc(value??'')+'" '+extra+'></div>'}
function select(name,label,arr,value='',cls='span4'){return '<div class="field '+cls+'"><label>'+label+'</label><select name="'+name+'"><option value="">待确认</option>'+arr.map(x=>'<option '+(x===value?'selected':'')+'>'+x+'</option>').join('')+'</select></div>'}
function customerModal(id=null){const c=id?customers.find(x=>x.id===id):{};modal(id?'编辑客户｜事实数据':'新增客户｜15分钟首访核心数据','<form id="customerForm" class="grid">'+input('company_name','公司名称*',c?.company_name,'text','span6','required')+input('contact_name','联系人',c?.contact_name)+input('phone','手机/微信',c?.phone)+input('contact_role','联系人职务',c?.contact_role)+select('customer_type','客户类型',['货主','代卖','货主+代卖','其他'],c?.customer_type)+select('decision_role','真实决策权',['无决策权','影响者','参与决策','进口/物流负责人','老板/最终决策人'],c?.decision_role)+input('annual_containers','年进口柜量',c?.annual_containers,'number')+input('monthly_containers','月均柜量',c?.monthly_containers,'number')+select('primary_cargo_group','东货/西货',['东货','西货'],c?.primary_cargo_group)+input('primary_fruit','核心水果',c?.primary_fruit||c?.core_fruits)+input('primary_country','来源国',c?.primary_country||c?.origin_countries)+input('primary_region','产区',c?.primary_region)+select('transport_mode','当前运输',['陆运','海运','陆运+海运','未知'],c?.transport_mode)+input('current_ports','当前口岸',c?.current_ports)+input('market_name','批发市场/办公楼',c?.market_name)+input('market_area','市场区域',c?.market_area)+input('building_floor','楼栋/楼层',c?.building_floor)+input('stall_office','档口/办公室',c?.stall_office)+select('pain_frequency','痛点发生频率',['无明显痛点','偶发','每月','每周','频繁/持续'],c?.pain_frequency)+input('loss_per_container','异常损失/柜（元）',c?.loss_per_container,'number')+select('payment_willingness','付费事实',['拒绝额外付费','只看低价','可接受小幅溢价','愿为确定性付费','明确接受方案溢价'],c?.payment_willingness)+select('cooperation_signal','客户行为',['无回应','愿意了解','愿意提供数据','愿意深入沟通','愿意比较方案','愿意试柜'],c?.cooperation_signal)+select('influence_level','行业影响力',['普通客户','区域重要客户','头部/标杆客户'],c?.influence_level)+'<div class="field span4"><label>主动寻找解决方案</label><select name="actively_seeking_solution"><option value="false">否/未知</option><option value="true" '+(c?.actively_seeking_solution?'selected':'')+'>是</option></select></div>'+input('next_followup_at','下次跟进时间',c?.next_followup_at?new Date(c.next_followup_at).toISOString().slice(0,16):'','datetime-local')+'<div class="field span12"><label>客户原话/备注</label><textarea name="notes">'+esc(c?.notes||'')+'</textarea></div><div class="span12 notice">录入后系统自动计算：DP、V/Q/P/T/R/M/F/W、CV、等级、完整度和DC。业务员无需也不能人工指定分数。</div><div class="span12 actions"><button class="btn btn-primary">'+(id?'保存修改':'保存并自动评分')+'</button><button type="button" class="btn btn-soft" onclick="closeModal()">取消</button></div></form>');$('#customerForm').onsubmit=async e=>{e.preventDefault();try{const o=Object.fromEntries(new FormData(e.target));['annual_containers','monthly_containers','loss_per_container'].forEach(k=>o[k]=o[k]===''?null:+o[k]);o.actively_seeking_solution=o.actively_seeking_solution==='true';o.core_fruits=o.primary_fruit;o.origin_countries=o.primary_country;o.created_by=id?(c.created_by||ses.user.id):ses.user.id;o.owner_id=id?(c.owner_id||ses.user.id):ses.user.id;const method=id?'PATCH':'POST',path=id?'/rest/v1/customers?id=eq.'+id:'/rest/v1/customers';if(!id){const dupe=await req('/rest/v1/customers?company_name=ilike.'+encodeURIComponent(o.company_name)+'&select=id,company_name&limit=3');if(dupe.length&&!confirm('发现同名客户：'+dupe.map(x=>x.company_name).join('、')+'。仍然新增吗？'))return}await req(path,{method,headers:{Prefer:'return=minimal'},body:JSON.stringify(o)});await refreshAll();closeModal();toast('客户已保存并自动评分');customersView()}catch(z){toast(z.message)}}}
function opportunityModal(id=null,customerId=null){const o=id?opportunities.find(x=>x.id===id):{};if(!customers.length)return toast('请先录入客户');const cs=customers.map(c=>'<option value="'+c.id+'" '+((customerId||o?.customer_id)===c.id?'selected':'')+'>'+esc(c.company_name)+'</option>').join('');modal(id?'编辑商机':'新增商机｜系统自动算分','<form id="oppForm" class="grid"><div class="field span6"><label>客户*</label><select name="customer_id" required>'+cs+'</select></div>'+input('fruit','水果*',o?.fruit,'text','span3','required')+select('cargo_group','东/西货',['东货','西货'],o?.cargo_group)+input('origin_country','来源国',o?.origin_country)+input('origin_region','产区',o?.origin_region)+input('season_month_num','业务月份（1-12）',o?.season_month_num||new Date().getMonth()+1,'number')+input('current_port','当前口岸',o?.current_port)+select('current_transport','当前运输',['陆运','海运','陆运+海运'],o?.current_transport)+select('need_status','需求事实',['无明确需求','有问题未明确方案','明确痛点','主动要求解决方案'],o?.need_status)+select('planned_timeframe','项目时间',['未知/12个月后','6-12个月','3-6个月','1-3个月','30天内','7天内/正在询价'],o?.planned_timeframe)+select('action_signal','客户行动',['无行动','愿意提供资料','要求方案','比较报价','愿意试柜'],o?.action_signal)+select('competition_window','竞争窗口',['无竞争窗口','可能比较','正在比较供应商','正在做最终选择'],o?.competition_window)+select('stage','商机阶段',['待验证','需求确认','方案','报价','试柜','持续合作','关闭'],o?.stage)+input('total_volume','总进口量/柜',o?.total_volume,'number')+input('matchable_volume','FONKON可匹配量/柜',o?.matchable_volume,'number')+input('switchable_volume','可切换量/柜',o?.switchable_volume,'number')+input('willing_volume','客户愿尝试量/柜',o?.willing_volume,'number')+input('trial_volume','首批试柜量/柜',o?.trial_volume,'number')+input('current_total_days','客户当前总时效/天（未知可留空）',o?.current_total_days,'number','span4','step="0.1"')+input('current_queue_hours','旺季排队/等待小时（未知可留空）',o?.current_queue_hours,'number','span4','step="0.1"')+input('current_cost_per_container','当前综合成本/柜',o?.current_cost_per_container,'number')+input('inspection_rate','查验比例%（有真实数据再填）',o?.inspection_rate,'number','span4','step="0.1"')+input('testing_rate','送检比例%（有真实数据再填）',o?.testing_rate,'number','span4','step="0.1"')+input('inspection_days','查验耗时/天',o?.inspection_days,'number','span4','step="0.1"')+input('testing_days','送检耗时/天',o?.testing_days,'number','span4','step="0.1"')+input('jiangmen_total_days','江门总时效/天（系统无库时再手填）',o?.jiangmen_total_days,'number','span4','step="0.1"')+input('jiangmen_cost_per_container','江门综合成本/柜',o?.jiangmen_cost_per_container,'number')+input('target_trial_date','计划试柜日期',o?.target_trial_date||'','date')+input('next_followup_at','下次推进时间',o?.next_followup_at?new Date(o.next_followup_at).toISOString().slice(0,16):'','datetime-local')+'<div class="span12 notice">保存时系统会自动联动：水果季节库 → 当前口岸库 → 江门路线库，并自动带出可用的时效/排队/查验/送检/江门路线数据；随后自动计算 OT、PI、EPI、LSOI/PSOI、AQ、灯号、缺失项和推荐动作。</div><div class="span12 actions"><button class="btn btn-primary">保存并自动计算</button><button type="button" class="btn btn-soft" onclick="closeModal()">取消</button></div></form>');$('#oppForm').onsubmit=async e=>{e.preventDefault();try{const x=Object.fromEntries(new FormData(e.target));['season_month_num','total_volume','matchable_volume','switchable_volume','willing_volume','trial_volume','current_total_days','current_queue_hours','current_cost_per_container','inspection_rate','testing_rate','inspection_days','testing_days','jiangmen_total_days','jiangmen_cost_per_container'].forEach(k=>x[k]=x[k]===''?null:+x[k]);x.created_by=id?(o.created_by||ses.user.id):ses.user.id;const method=id?'PATCH':'POST',path=id?'/rest/v1/opportunities?id=eq.'+id:'/rest/v1/opportunities';await req(path,{method,headers:{Prefer:'return=minimal'},body:JSON.stringify(x)});await refreshAll();closeModal();toast('商机已自动计算');oppsView()}catch(z){toast(z.message)}}}
function visitModal(customerId=null){if(!customers.length)return toast('请先录入客户');const cs=customers.map(c=>'<option value="'+c.id+'" '+(customerId===c.id?'selected':'')+'>'+esc(c.company_name)+'</option>').join('');modal('15分钟拜访记录','<form id="visitForm" class="grid"><div class="field span6"><label>客户</label><select name="customer_id">'+cs+'</select></div>'+select('visit_type','访谈类型',['首访','二访补数','方案沟通','报价沟通','试柜复盘'],'首访')+input('duration_minutes','实际沟通分钟',15,'number')+'<div class="field span12"><label>事实数据（水果/国家/产区/柜量/运输/口岸/淡旺季）</label><textarea name="facts"></textarea></div><div class="field span12"><label>痛点（排队/查验/送检/品质/成本/资金）</label><textarea name="pain_points"></textarea></div><div class="field span12"><label>客户原话</label><textarea name="customer_quote"></textarea></div><div class="field span6"><label>我的商业假设</label><textarea name="hypothesis"></textarea></div><div class="field span6"><label>客户验证结果</label><textarea name="validation_result"></textarea></div>'+input('next_action','下一步','','text','span8')+input('next_followup_at','下次跟进','','datetime-local','span4')+'<div class="span12 actions"><button class="btn btn-primary">保存拜访</button></div></form>');$('#visitForm').onsubmit=async e=>{e.preventDefault();try{const x=Object.fromEntries(new FormData(e.ta