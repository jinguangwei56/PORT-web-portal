function opportunityModal(id=null,customerId=null){
  const o=id?opportunities.find(x=>x.id===id):{};if(!customers.length)return toast('请先录入客户');
  const cs=customers.map(c=>'<option value="'+c.id+'" '+((customerId||o?.customer_id)===c.id?'selected':'')+'>'+esc(c.company_name)+'</option>').join('');
  modal(id?'编辑商机':'新增商机｜事实驱动自动算分','<form id="oppForm" class="grid"><div class="field span6"><label>客户*</label><select name="customer_id" required>'+cs+'</select></div>'+input('fruit','水果*',o?.fruit,'text','span3','required')+select('cargo_group','东/西货',['东货','西货'],o?.cargo_group)+input('origin_country','来源国',o?.origin_country)+input('origin_region','产区',o?.origin_region)+input('season_month_num','业务月份1-12',o?.season_month_num||new Date().getMonth()+1,'number')+input('current_port','当前口岸',o?.current_port)+select('current_transport','当前运输',['陆运','海运','陆运+海运'],o?.current_transport)+select('need_status','需求事实',['无明确需求','有问题未明确方案','明确痛点','主动要求解决方案'],o?.need_status)+select('planned_timeframe','项目时间',['未知/12个月后','6-12个月','3-6个月','1-3个月','30天内','7天内/正在询价'],o?.planned_timeframe)+select('action_signal','客户行动',['无行动','愿意提供资料','要求方案','比较报价','愿意试柜'],o?.action_signal)+select('competition_window','竞争窗口',['无竞争窗口','可能比较','正在比较供应商','正在做最终选择'],o?.competition_window)+select('stage','商机阶段',['待验证','需求确认','方案','报价','试柜','持续合作','关闭'],o?.stage)+input('total_volume','总进口量/柜',o?.total_volume,'number')+input('matchable_volume','FONKON可匹配量/柜',o?.matchable_volume,'number')+input('switchable_volume','可切换量/柜',o?.switchable_volume,'number')+input('willing_volume','客户愿尝试量/柜',o?.willing_volume,'number')+input('trial_volume','首批试柜量/柜',o?.trial_volume,'number')+
  '<div class="span12 notice"><b>当前方案淡旺季：</b>东货必须尽量向客户问出淡季/旺季真实时效和排队时间；未知可以留空，系统会优先调用口岸数据库基准值。</div>'+input('current_normal_days','当前方案淡季总时效/天',o?.current_normal_days,'number','span4','step="0.1"')+input('current_peak_days','当前方案旺季总时效/天',o?.current_peak_days,'number','span4','step="0.1"')+input('current_total_days','当前月份实际总时效/天',o?.current_total_days,'number','span4','step="0.1"')+input('normal_queue_hours','淡季排队/等待小时',o?.normal_queue_hours,'number','span4','step="0.1"')+input('peak_queue_hours','旺季排队/等待小时',o?.peak_queue_hours,'number','span4','step="0.1"')+input('current_queue_hours','当前月份排队/等待小时',o?.current_queue_hours,'number','span4','step="0.1"')+input('current_cost_per_container','当前综合成本/柜',o?.current_cost_per_container,'number')+input('inspection_rate','当前口岸查验比例%',o?.inspection_rate,'number','span4','step="0.1"')+input('testing_rate','当前口岸送检比例%',o?.testing_rate,'number','span4','step="0.1"')+input('inspection_days','当前查验耗时/天',o?.inspection_days,'number','span4','step="0.1"')+input('testing_days','当前送检耗时/天',o?.testing_days,'number','span4','step="0.1"')+input('current_congestion_index','当前口岸PCI',o?.current_congestion_index,'number','span4','step="0.01"')+
  '<div class="span12 notice"><b>江门对比：</b>系统优先从江门路线库和江门口岸库自动带出。只有掌握更真实的新数据时才手动覆盖。</div>'+input('jiangmen_total_days','江门总时效/天',o?.jiangmen_total_days,'number','span4','step="0.1"')+input('jiangmen_cost_per_container','江门综合成本/柜',o?.jiangmen_cost_per_container,'number')+input('jiangmen_congestion_index','江门PCI',o?.jiangmen_congestion_index,'number','span4','step="0.01"')+input('jiangmen_inspection_rate','江门查验比例%',o?.jiangmen_inspection_rate,'number','span4','step="0.1"')+input('jiangmen_testing_rate','江门送检比例%',o?.jiangmen_testing_rate,'number','span4','step="0.1"')+input('jiangmen_inspection_days','江门查验耗时/天',o?.jiangmen_inspection_days,'number','span4','step="0.1"')+input('jiangmen_testing_days','江门送检耗时/天',o?.jiangmen_testing_days,'number','span4','step="0.1"')+input('target_trial_date','计划试柜日期',o?.target_trial_date||'','date')+input('next_followup_at','下次推进时间',o?.next_followup_at?new Date(o.next_followup_at).toISOString().slice(0,16):'','datetime-local','span4')+'<div class="span12 notice">保存后自动联动：水果上市/产量 → 当前口岸淡旺季/PCI/查验送检 → 江门航线 → 江门口岸；自动计算 ΔT、ΔC、ΔPCI、ΔR、OT、PI、EPI、LSOI/PSOI、AQ、商机DC、灯号、缺失项和下一动作。</div><div class="span12 actions"><button class="btn btn-primary">保存并自动计算</button><button type="button" class="btn btn-soft" onclick="closeModal()">取消</button></div></form>');
  $('#oppForm').onsubmit=async e=>{e.preventDefault();try{const x=Object.fromEntries(new FormData(e.target));['season_month_num','total_volume','matchable_volume','switchable_volume','willing_volume','trial_volume','current_normal_days','current_peak_days','current_total_days','normal_queue_hours','peak_queue_hours','current_queue_hours','current_cost_per_container','inspection_rate','testing_rate','inspection_days','testing_days','current_congestion_index','jiangmen_total_days','jiangmen_cost_per_container','jiangmen_congestion_index','jiangmen_inspection_rate','jiangmen_testing_rate','jiangmen_inspection_days','jiangmen_testing_days'].forEach(k=>x[k]=x[k]===''?null:+x[k]);x.created_by=id?(o.created_by||ses.user.id):ses.user.id;const method=id?'PATCH':'POST',path=id?'/rest/v1/opportunities?id=eq.'+id:'/rest/v1/opportunities';await req(path,{method,headers:{Prefer:'return=minimal'},body:JSON.stringify(x)});await refreshAll();closeModal();toast('商机已按严格模型自动计算');oppsView()}catch(z){toast(z.message)}};
}
