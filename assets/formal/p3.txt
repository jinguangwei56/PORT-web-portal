ve('hide');$('#app').classList.add('hide');return}$('#auth').classList.add('hide');$('#app').classList.remove('hide');$('#who').innerHTML='<b>'+esc(profile.name||ses.user.email)+'</b> · '+roleName(profile.role);render('dashboard')}
$('#tabLogin').onclick=()=>{$('#tabLogin').classList.add('active');$('#tabRegister').classList.remove('active');$('#loginForm').classList.remove('hide');$('#registerForm').classList.add('hide')};
$('#tabRegister').onclick=()=>{$('#tabRegister').classList.add('active');$('#tabLogin').classList.remove('active');$('#registerForm').classList.remove('hide');$('#loginForm').classList.add('hide')};
$('#loginForm').onsubmit=async e=>{e.preventDefault();try{$('#authMsg').textContent='登录中…';const x=await req('/auth/v1/token?grant_type=password',{method:'POST',body:JSON.stringify({email:$('#loginEmail').value,password:$('#loginPassword').value})});saveSession(x);await loadProfile();await refreshAll();$('#authMsg').textContent='';showApp()}catch(z){clearSession();$('#authMsg').textContent=z.message}};
$('#registerForm').onsubmit=async e=>{e.preventDefault();try{$('#authMsg').textContent='创建账号中…';await edge({action:'register',name:$('#regName').value,email:$('#regEmail').value,password:$('#regPassword').value,code:$('#regCode').value});$('#authMsg').textContent='注册成功，请登录';$('#tabLogin').click();$('#loginEmail').value=$('#regEmail').value}catch(z){$('#authMsg').textContent=z.message}};
$('#logout').onclick=()=>{clearSession();showApp()};
function setNav(v){$$('.nav-btn,.bottom-nav button').forEach(b=>b.classList.toggle('active',b.dataset.view===v))}
$$('.nav-btn,.bottom-nav button').forEach(b=>b.onclick=()=>{setNav(b.dataset.view);render(b.dataset.view)});$('#fab').onclick=()=>customerModal();
function pageHead(title,sub,actions=''){return '<div class="page-head"><div><div class="page-title">'+title+'</div><div class="page-sub">'+sub+'</div></div><div class="actions">'+actions+'</div></div>'}
function ownerName(id){return profiles.find(p=>p.id===id)?.name||customers.find(c=>c.owner_id===id)?.owner_id?.slice(0,6)||'-'}
function customerName(id){return customers.find(c=>c.id===id)?.company_name||'-'}
function isManager(){return ['admin','manager'].includes(profile?.role)}
function missingCustomer(c){const m=[];const map=[['contact_name','联系人'],['contact_role','联系人职务'],['decision_role','真实决策权'],['annual_containers','年柜量'],['primary_fruit','核心水果'],['primary_country','来源国'],['current_ports','当前口岸'],['transport_mode','运输方式'],['pain_frequency','痛点频率'],['payment_willingness','付费意愿'],['cooperation_signal','合作行为'],['market_name','所在市场'],['next_followup_at','下次跟进']];map.forEach(([k,n])=>{if(c[k]===null||c[k]===''||c[k]===undefined)m.push(n)});return m}
function dueToday(t){if(!t.due_at||t.status!=='待办')return false;const d=new Date(t.due_at),n=new Date();return d.toDateString()===n.toDateString()||d<n}
function todayDashboard(){const myTasks=tasks.filter(t=>t.status==='待办'&&(!t.owner_id||t.owner_id===ses.user.id||isManager())).filter(dueToday);const red=opportunities.filter(o=>o.alert_level==='红');const needs=customers.filter(c=>missingCustomer(c).length>0).sort((a,b)=>num(b.cv_score)-num(a.cv_score));const trial=opportunities.filter(o=>['试柜','报价'].includes(o.stage)||num(o.trial_volume)>0).slice(0,6);const high=customers.filter(c=>['S','A'].includes(c.cv_grade)).length;let html=pageHead('今日作战中心','先做高价值、高热度、窗口正在打开的客户。','<button class="btn btn-primary" onclick="customerModal()">＋ 快速新增客户</button><button class="btn btn-soft" onclick="visitModal()">✎ 记录拜访</button>');html+='<div class="kpis"><div class="card kpi"><div class="kpi-label">今日待办</div><div class="kpi-value">'+myTasks.length+'</div><div class="kpi-foot">到期/逾期跟进</div></div><div class="card kpi"><div class="kpi-label">红灯商机</div><div class="kpi-value">'+red.length+'</div><div class="kpi-foot">48小时内重点推进</div></div><div class="card kpi"><div class="kpi-label">A/S级客户</div><div class="kpi-value">'+high+'</div><div class="kpi-foot">高价值客户池</div></div><div class="card kpi"><div class="kpi-label">可切换柜量</div><div class="kpi-value">'+fmt(opportunities.reduce((s,o)=>s+num(o.switchable_volume),0))+'</div><div class="kpi-foot">已识别AQ机会</div></div></div>';
html+='<div class="two"><div><div class="card card-pad"><div class="section-title"><h2>🔥 当前最值得推进</h2><span class="hint">按最终作战优先分</span></div>'+opportunityCards(opportunities.slice(0,6))+'</div><div class="card card-pad section"><div class="section-title"><h2>🧭 数据待补客户</h2><span class="hint">二访只补缺失项，不重新聊天</span></div>'+needs.slice(0,6).map(c=>'<div class="task-card"><div class="task-top"><span class="link" onclick="openCustomer(\''+c.id+'\')">'+esc(c.company_name)+'</span>'+badge(c.cv_grade)+'</div><div class="missing-list" style="margin-top:8px">'+missingCustomer(c).slice(0,6).map(x=>'<span>'+esc(x)+'</span>').join('')+'</div></div>').join('')+(needs.length?'':'<div class="empty">暂无待补数据客户</div>')+'</div></div><div><div class="card card-pad"><div class="section-title"><h2>✅ 今日任务</h2><button class="btn btn-soft" onclick="taskModal()">＋任务</button></div>'+taskCards(myTasks.slice(0,8))+'</div><div class="card card-pad section"><div class="section-title"><h2>🚢 试柜推进</h2><span class="hint">报价→试柜→持续合作</span></div>'+opportunityCards(trial)+'</div></div></div>';$('#view').innerHTML=html}
function taskCards(arr){return arr.length?arr.map(t=>'<div class="task-card"><div class="task-top"><div class="task-title">'+esc(t.title)+'</div>'+badge(t.priority==='紧急'?'红':t.priority==='高'?'橙':'绿')+'</div><div class="task-meta">'+esc(customerName(t.customer_id))+' · '+dt(t.due_at)+'</div><div style="margin-top:8px"><button class="btn btn-soft" onclick="completeTask(\''+t.id+'\')">完成</button></div></div>').join(''):'<div class="empty">今天没有到期任务</div>'}
function opportunityCards(arr){return arr.length?arr.map(o=>{const sw=o.cargo_group==='东货'?o.lsoi_score:o.psoi_score;return '<div class="op-card"><div class="op-head"><div><div class="op-title"><span class="link" onclick="openCustomer(\''+o.customer_id+'\')">'+esc(customerName(o.customer_id))+'</span> · '+esc(o.fruit)+'</div><div class="task-meta">'+esc(o.cargo_group||'')+' · '+esc(o.current_port||'未填口岸')+' · '+esc(o.season_status||'')+'</div></div>'+badge(o.alert_level)+'</div><div class="op-grid"><div class="mini"><b>'+fmt(o.ot_score)+'</b><span>OT 热度</span></div><div class="mini"><b>'+fmt(o.epi_score)+'</b><span>EPI 有效优先</span></div><div class="mini"><b>'+fmt(sw)+'</b><span>切换指数</span></div><div class="mini"><b>'+fmt(o.final_priority)+'</b><span>作战优先分</span></div></div><div class="task-meta" style="margin-top:9px">建议：'+esc(o.recommendation||'-')+'</div></div>'}).join(''):'<div class="empty">暂无商机</div>'}
function customersView(){const html=pageHead('客户数据库','一客一档，客户级与商机级分开计算。','<button class="btn btn-primary" onclick="customerModal()">＋ 新客户</button><button class="btn btn-soft" onclick="exportCustomers()">导出Excel兼容CSV</button>')+'<div class="card card-pad"><div class="filters"><input id="cq" placeholder="搜索公司/联系人/水果/市场"><select id="cg"><option value="">全部等级</option><option>S</option><option>A</option><option>B</option><option>C</option><option>D</option></select><select id="ct"><option value="">全部类型</option><option>货主</option><option>代卖</option><option>货主+代卖</option></select><select id="cc"><option value="">东/西货</option><option>东货</option><option>西货</option></select></div><div id="customerTable"></div></div>';$('#view').innerHTML=html;['cq','cg','ct','cc'].forEach(id=>$('#'+id).oninput=renderCustomerTable);renderCustomerTable()}
function renderCustomerTable(){const q=($('#cq')?.value||'').toLowerCase(),g=$('#cg')?.value||'',t=$('#ct')?.value||'',cc=$('#cc')?.value||'';const rows=customers.filter(c=>(!q||[c.company_name,c.contact_name,c.primary_fruit,c.market_name,c.current_ports].join(' ').toLowerCase().includes(q))&&(!g||c.cv_grade===g)&&(!t||c.customer_type===t)&&(!cc||c.primary_cargo_group===cc));$('#customerTable').innerHTML='<div class="table-wrap"><table class="table"><thead><tr><th>客户</th><th>角色/决策</th><th>水果</th><th>年柜量</th><th>CV</th><th>DC</th><th>市场/口岸</th><th>下次跟进</th><th>负责人</th></tr></thead><tbody>'+rows.map(c=>'<tr><td><div class="link" onclick="openCustomer(\''+c.id+'\')">'+esc(c.company_name)+'</div><div class="muted">'+esc(c.contact_name||'')+' '+esc(c.phone||'')+'</div></td><td>'+esc(c.customer_type||'-')+'<div class="muted">'+esc(c.decision_role||'待确认')+' / DP '+fmt(c.decision_power)+'</div></td><td>'+esc(c.primary_fruit||c.core_fruits||'-')+' '+(c.primary_cargo_group?'<span class="chip">'+esc(c.primary_cargo_group)+'</span>':'')+'</td><td>'+fmt(c.annual_containers)+'</td><td>'+badge(c.cv_grade)+' <b>'+fmt(c.cv_score)+'</b></td><td><b>'+fmt(c.dc_score)+'%</b>'+progress(c.dc_score)+'</td><td>'+esc(c.market_name||'-')+'<div class="muted">'+esc(c.current_ports||'-')+'</div></td><td>'+dt(c.next_followup_at)+'</td><td>'+esc(ownerName(c.owner_id))+'</td></tr>').join('')+'</tbody></table></div>'}
function oppsView(){const html=pageHead('商机引擎','客户 × 水果 × 产区 × 月份 × 当前口岸，自动计算 OT / EPI / LSOI / PSOI。','<button class="btn btn-primary" onclick="opportunityModal()">＋ 新商机</button><button class="btn btn-soft" onclick="exportOpps()">导出Excel兼容CSV</button>')+'<div class="card card-pad"><div class="filters"><input id="oq" placeholder="搜索客户/水果/口岸"><select id="og"><option value="">东/西货</option><option>东货</option><option>西货</option></select><select id="oa"><option value="">灯号</option><option>红</option><option>橙</option><option>黄</option><option>绿</option><option>蓝</option></select><select id="os"><option value="">阶段</option>'+['待验证','需求确认','方案','报价','试柜','持续合作','关闭'].map(x=>'<option>'+x+'</option>').join('')+'</select></div><div id="oppTable"></div></div>';$('#view').innerHTML=html;['oq','og','oa','os'].forEach(id=>$('#'+id).oninput=renderOppTable);renderOppTable()}
function renderOppTable(){const q=($('#oq')?.value||'').toLowerCase(),g=$('#og')?.value||'',a=$('#oa')?.value||'',s=$('#os')?.value||'';const arr=opportunities.filter(o=>(!q||[customerName(o.customer_id),o.fruit,o.current_port,o.origin_co