/* FONKON Market OS - Governed Full-chain Route Report Center 2.0 */
(function(){
  const reportStyle=document.createElement('style');
  reportStyle.textContent='.report-route-list{display:grid;gap:9px;max-height:310px;overflow:auto;padding:3px}.report-route-option{display:flex;gap:10px;align-items:flex-start;border:1px solid var(--line,#dbe4ea);border-radius:13px;padding:11px 12px;background:#fff;cursor:pointer}.report-route-option:hover{border-color:#11a49d;background:#f1fbfa}.report-route-option.disabled{opacity:.42;pointer-events:none}.report-route-option input{margin-top:4px;accent-color:#078d87}.report-route-option span{display:grid;gap:4px}.report-route-option small{color:var(--muted,#718096);line-height:1.45}.report-check{display:flex!important;align-items:center;gap:8px}.report-check input{width:auto!important;accent-color:#078d87}.demo-pill{display:inline-block;border-radius:999px;background:#fff0c2;color:#845400;padding:2px 7px;font-size:10px;font-weight:900}.ready-pill{display:inline-block;border-radius:999px;background:#e7f8f5;color:#08746a;padding:2px 7px;font-size:10px;font-weight:900}.report-blockers{margin:8px 0 0;padding-left:18px}.segment-editor{max-height:62vh;overflow:auto}.segment-editor table{min-width:1450px}.segment-editor input,.segment-editor select{min-width:88px;padding:8px!important}.segment-editor .wide{min-width:170px}.segment-editor .source{min-width:240px}.report-status-note{max-width:270px;white-space:normal}.password-inline{display:grid;grid-template-columns:1fr auto;gap:6px}.password-inline button{border:1px solid var(--line);border-radius:10px;background:#eef3f6;font-weight:800}@media(max-width:720px){.report-route-list{max-height:270px}.report-route-option{padding:12px 10px}.report-check{grid-column:1/-1!important}.segment-editor{max-height:56vh}}';
  document.head.appendChild(reportStyle);
  const API=U+'/functions/v1/route-report',PREV_RENDER=window.render,PREV_ROUTE_VIEW=window.routeModelView,PREV_TOOLS=window.mobileTools,PREV_SHOW=window.showApp;
  const E=v=>esc(String(v??'')),D=v=>v?new Date(v).toLocaleString('zh-CN',{year:'numeric',month:'2-digit',day:'2-digit'}):'-',linkFor=t=>'https://os.fonkonsupply.com/report.html?v=20260824-r11-1-reports5&t='+encodeURIComponent(t||'');
  let reportOptionsCache=null,reportCanApprove=false;
  async function reportApi(body){if(!ses?.access_token)throw Error('请先登录');const r=await fetch(API,{method:'POST',headers:{apikey:K,authorization:'Bearer '+ses.access_token,'content-type':'application/json'},body:JSON.stringify(body)}),raw=await r.text();let j={};try{j=raw?JSON.parse(raw):{}}catch{}if(!r.ok)throw Error(j?.error||j?.message||'报告服务暂时不可用');return j}
  async function copyText(value,message='已复制'){try{await navigator.clipboard.writeText(value);toast(message);return true}catch{prompt('请复制以下内容',value);return false}}
  function addReportNav(){if(document.querySelector('[data-view="reportCenter"]'))return;const anchor=document.querySelector('.nav-btn[data-view="routeModel"]')||document.querySelector('.nav-btn[data-view="opportunities"]');if(!anchor)return;const b=document.createElement('button');b.className='nav-btn';b.dataset.view='reportCenter';b.innerHTML='<span class="ico">▤</span><span>客户报告</span>';b.onclick=()=>{try{setNav('reportCenter')}catch{};reportCenterView()};anchor.insertAdjacentElement('afterend',b)}
  function routeReadiness(r){const keys=['p50_days','p90_days','total_cost_per_container','stability_index','route_congestion_index','schedule_frequency_per_week','capacity_score','coldchain_risk_index','abnormal_handling_score'],n=keys.filter(k=>r[k]!==null&&r[k]!==undefined&&r[k]!=='').length;return n+'/'+keys.length+'项 · '+Number(r.segment_count||0)+'/9段'}
  function routeLabel(r,jiangmen=false){return (jiangmen?'江门｜':(r.port||'当前口岸')+'｜')+(r.fruit||'水果待补')+'｜'+(r.country||'国家待补')+(r.origin_region?' '+r.origin_region:'')+(r.month?'｜'+r.month+'月':'｜月份待补')+'｜'+(r.currency||'CNY')+'/'+(r.container_type||'40RF')+'｜'+routeReadiness(r)}
  function scopeMatch(a,b){return ['cargo_group','fruit','country','origin_region','month','destination_market','currency','container_type','data_scope'].every(k=>String(a?.[k]??'').trim().toLowerCase()===String(b?.[k]??'').trim().toLowerCase())}
  async function getOptions(force=false){if(reportOptionsCache&&!force)return reportOptionsCache;reportOptionsCache=await reportApi({action:'generator_options'});reportCanApprove=!!reportOptionsCache.can_approve;return reportOptionsCache}
  function demoSelect(){const jm=document.querySelector('#reportJiangmen'),demoJm=Array.from(jm.options).find(o=>o.dataset.demo==='1');if(demoJm)jm.value=demoJm.value;filterRoutes();document.querySelectorAll('input[name="current_route_ids"][data-demo="1"]:not(:disabled)').forEach(x=>x.checked=true);toast('已选择完整演示组合')}
  function filterRoutes(){const data=reportOptionsCache,jmId=document.querySelector('#reportJiangmen')?.value,jm=(data?.jiangmen_routes||[]).find(x=>x.id===jmId);document.querySelectorAll('.report-route-option').forEach(label=>{const r=(data?.current_routes||[]).find(x=>x.id===label.dataset.id),ok=!jm||scopeMatch(r,jm);label.classList.toggle('disabled',!ok);const input=label.querySelector('input');input.disabled=!ok;if(!ok)input.checked=false});const box=document.querySelector('#scopeHint');if(box)box.textContent=jm?'已自动限制为：'+routeLabel(jm,true):'请先选择江门方案，系统会自动过滤同口径路线。'}
  function customerChanged(){const data=reportOptionsCache,customer=document.querySelector('#reportCustomer'),opp=document.querySelector('#reportOpportunity'),name=document.querySelector('#reportClientName'),c=(data.customers||[]).find(x=>x.id===customer.value);if(c&&!name.value)name.value=c.company_name||'';opp.innerHTML='<option value="">不绑定商机</option>'+(data.opportunities||[]).filter(x=>!customer.value||x.customer_id===customer.value).map(x=>'<option value="'+E(x.id)+'">'+E((x.fruit||'商机')+'｜'+(x.stage||x.status||'进行中'))+'</option>').join('')}
  window.openRouteReportGenerator=async function(){
    try{modal('生成客户供应链路线报告','<div class="notice">正在读取已授权的路线、客户与商机…</div>');const data=await getOptions(true),cur=data.current_routes||[],jm=data.jiangmen_routes||[];if(!cur.length||!jm.length)return modal('暂时不能生成报告','<div class="notice warn">至少需要1条端到端现行路线和1条端到端江门路线。</div>');
      const customerOptions='<option value="">不绑定CRM客户</option>'+(data.customers||[]).map(c=>'<option value="'+E(c.id)+'">'+E(c.company_name)+'</option>').join(''),currentOptions=cur.map(r=>'<label class="report-route-option" data-id="'+E(r.id)+'"><input type="checkbox" name="current_route_ids" value="'+E(r.id)+'" data-demo="'+(r.is_demo?'1':'0')+'"><span><b>'+E(r.port||'当前路线')+' '+(r.is_demo?'<i class="demo-pill">演示</i>':'')+'</b><small>'+E(routeLabel(r,false))+' '+(r.segment_count===9?'<i class="ready-pill">9段完整</i>':'<i class="demo-pill">分段待补</i>')+'</small></span></label>').join(''),jmOptions=jm.map(r=>'<option value="'+E(r.id)+'" data-demo="'+(r.is_demo?'1':'0')+'">'+E(routeLabel(r,true))+(r.is_demo?'【演示】':'')+'</option>').join('');
      modal('生成FONKON路线决策报告','<form id="routeReportForm" class="grid"><div class="span12 notice good"><b>V2报告规则：</b>固定规则模型＋实质性阈值；先生成草稿，再提交审核，由最高权限发布。演示数据会自动加水印。</div><div class="field span5"><label>绑定CRM客户</label><select name="customer_id" id="reportCustomer">'+customerOptions+'</select></div><div class="field span4"><label>绑定商机（可选）</label><select name="opportunity_id" id="reportOpportunity"><option value="">不绑定商机</option></select></div><div class="field span3"><label>客户显示名称</label><input name="client_name" id="reportClientName" placeholder="例如：鑫华果业"></div><div class="field span12"><label>先选择江门方案</label><select name="jiangmen_route_id" id="reportJiangmen" required><option value="">请选择</option>'+jmOptions+'</select><small id="scopeHint" class="muted">选择后自动过滤同水果、同月份、同起终点、同币种和柜型。</small></div><div class="field span12"><label>选择现行/其他口岸路线（1—4条）</label><div class="actions" style="margin-bottom:8px"><button type="button" class="btn btn-soft" id="selectDemoRoutes">一键选择完整演示组合</button>'+(data.can_approve?'<button type="button" class="btn btn-soft" onclick="openRouteSegmentEditor()">维护9段链路数据</button>':'')+'</div><div class="report-route-list">'+currentOptions+'</div></div><div class="field span6"><label>报告标题</label><input name="title" value="FONKON供应链路线决策报告" required></div><div class="field span3"><label>分享有效期</label><select name="expiry_days"><option value="7">7天</option><option value="30" selected>30天</option><option value="90">90天</option><option value="365">365天</option></select></div><div class="field span3"><label>访问密码（可选）</label><div class="password-inline"><input name="access_code" id="reportAccessCode" type="password" minlength="6" maxlength="32" autocomplete="new-password" placeholder="至少6位"><button type="button" id="showReportPassword">显示</button></div></div><div class="field span12"><label>比较口径/补充说明</label><input name="assumptions" value="同起点、同终点、同水果、同一业务月份、同币种、同柜型；未知数据不按0计算。"></div><label class="field span6 report-check"><input type="checkbox" name="show_cost" checked> 客户报告显示TCO金额</label><label class="field span6 report-check"><input type="checkbox" name="show_client_name" checked> 客户报告显示客户名称</label><div class="span12 actions"><button class="btn btn-primary" id="generateReportBtn">生成治理草稿</button><button type="button" class="btn btn-soft" onclick="closeModal()">取消</button></div></form>');
      const f=document.querySelector('#routeReportForm');document.querySelector('#reportCustomer').onchange=customerChanged;document.querySelector('#reportJiangmen').onchange=filterRoutes;document.querySelector('#selectDemoRoutes').onclick=demoSelect;document.querySelector('#showReportPassword').onclick=()=>{const x=document.querySelector('#reportAccessCode'),b=document.querySelector('#showReportPassword'),show=x.type==='password';x.type=show?'text':'password';b.textContent=show?'隐藏':'显示'};
      f.onsubmit=async e=>{e.preventDefault();const fd=new FormData(f),ids=fd.getAll('current_route_ids');if(!fd.get('jiangmen_route_id'))return toast('请先选择江门路线');if(!ids.length)return toast('请选择至少一条同口径现行路线');if(ids.length>4)return toast('最多选择4条现行路线');const b=document.querySelector('#generateReportBtn');b.disabled=true;b.textContent='正在计算并固化快照…';try{const r=await reportApi({action:'create',customer_id:fd.get('customer_id')||null,opportunity_id:fd.get('opportunity_id')||null,client_name:fd.get('client_name'),current_route_ids:ids,jiangmen_route_id:fd.get('jiangmen_route_id'),title:fd.get('title'),expiry_days:Number(fd.get('expiry_days')||30),access_code:fd.get('access_code'),assumptions:fd.get('assumptions'),show_cost:fd.get('show_cost')==='on',show_client_name:fd.get('show_client_name')==='on'});showDraftReady(r,data.can_approve)}catch(z){toast(z.message);b.disabled=false;b.textContent='生成治理草稿'}};
    }catch(e){modal('报告生成器加载失败','<div class="notice warn">'+E(e.message)+'</div>')}
  };
  function showDraftReady(r,canApprove){const blockers=r.publish_blockers||[];modal('报告草稿已生成','<div class="notice '+(blockers.length?'warn':'good')+'"><b>报告编号 '+E(r.report_number)+'</b><br>模型 '+E(r.model_version)+' · 数据就绪与证据指数 '+E(r.readiness_index)+'% · '+(r.is_demo?'演示数据':'正式数据')+'</div>'+(blockers.length?'<div class="notice warn"><b>暂不可发布，需处理：</b><ul class="report-blockers">'+blockers.map(x=>'<li>'+E(x)+'</li>').join('')+'</ul></div>':'<div class="notice good">发布前校验已通过，可以提交审核。</div>')+'<div class="actions" style="margin-top:14px">'+(!blockers.length?'<button class="btn btn-primary" id="submitDraft">提交审核</button>':'')+(!blockers.length&&canApprove?'<button class="btn btn-primary" id="adminPublishDraft">管理员审核并发布</button>':'')+(canApprove?'<button class="btn btn-soft" id="editSegments">维护链路数据</button>':'')+'<button class="btn btn-soft" id="manageDraft">报告中心</button></div>');const submit=document.querySelector('#submitDraft'),publish=document.querySelector('#adminPublishDraft'),edit=document.querySelector('#editSegments');if(submit)submit.onclick=()=>submitRouteReport(r.id);if(publish)publish.onclick=()=>adminPublishRouteReport(r.id,true);if(edit)edit.onclick=()=>openRouteSegmentEditor();document.querySelector('#manageDraft').onclick=()=>{closeModal();render('reportCenter')}}
  function showReportReady(r){const link=linkFor(r.share_token);modal('客户报告已审核发布','<div class="notice good"><b>报告已通过审核并形成不可变快照。</b><br>客户无需登录即可打开；有效期至 '+E(D(r.expires_at))+'</div><div class="security-box" style="margin-top:12px;word-break:break-all">'+E(link)+'</div><div class="actions" style="margin-top:14px"><button class="btn btn-primary" id="copyNewReport">复制链接</button><button class="btn btn-soft" id="openNewReport">打开报告</button><button class="btn btn-soft" id="manageNewReport">报告中心</button></div>');document.querySelector('#copyNewReport').onclick=()=>copyText(link,'客户报告链接已复制');document.querySelector('#openNewReport').onclick=()=>window.open(link,'_blank','noopener');document.querySelector('#manageNewReport').onclick=()=>{closeModal();render('reportCenter')}}
  window.submitRouteReport=async id=>{try{await reportApi({action:'submit_review',report_id:id});toast('已提交最高权限审核');reportCenterView()}catch(e){toast(e.message)}};
  window.adminPublishRouteReport=async(id,fromDraft=false)=>{try{if(fromDraft)await reportApi({action:'submit_review',report_id:id});const r=await reportApi({action:'approve_publish',report_id:id,review_notes:'最高权限已核验数据口径、来源、分段对账与演示标识。'});showReportReady(r)}catch(e){toast(e.message)}};
  window.returnRouteReport=async id=>{const note=prompt('请输入退回原因');if(!note)return;try{await reportApi({action:'return_draft',report_id:id,review_notes:note});toast('已退回草稿');reportCenterView()}catch(e){toast(e.message)}};
  function statusMeta(r){const expired=r.status==='published'&&r.expires_at&&new Date(r.expires_at)<new Date();if(expired)return ['已过期','b-yellow'];return ({draft:['草稿','b-gray'],pending_review:['待审核','b-yellow'],published:['分享中','b-green'],revoked:['已撤销','b-gray'],expired:['已过期','b-yellow']}[r.status]||[r.status,'b-gray'])}
  window.reportCenterView=async function(){try{setNav('reportCenter')}catch{};document.querySelector('#view').innerHTML=pageHead('客户报告中心','治理草稿、审核发布、分享安全与客户查看追踪。','<button class="btn btn-primary" onclick="openRouteReportGenerator()">＋生成报告草稿</button>')+'<div class="card empty">正在读取报告…</div>';try{const data=await reportApi({action:'list'}),rows=data.reports||[];reportCanApprove=!!data.can_approve;let html=pageHead('客户报告中心','报告由固定规则模型计算；只有通过最高权限审核的快照才能分享给客户。','<button class="btn btn-primary" onclick="openRouteReportGenerator()">＋生成报告草稿</button>'+(data.can_approve?'<button class="btn btn-soft" onclick="openRouteSegmentEditor()">维护9段链路</button>':'')+'<button class="btn btn-soft" onclick="render(\'routeModel\')">路线模型</button>');html+='<div class="kpis"><div class="card kpi"><div class="kpi-label">草稿 / 待审核</div><div class="kpi-value">'+rows.filter(x=>['draft','pending_review'].includes(x.status)).length+'</div><div class="kpi-foot">发布治理队列</div></div><div class="card kpi"><div class="kpi-label">正在分享</div><div class="kpi-value">'+rows.filter(x=>x.status==='published'&&(!x.expires_at||new Date(x.expires_at)>new Date())).length+'</div><div class="kpi-foot">审核通过且链接有效</div></div><div class="card kpi"><div class="kpi-label">独立客户</div><div class="kpi-value">'+rows.reduce((s,x)=>s+Number(x.unique_view_count||0),0)+'</div><div class="kpi-foot">匿名去重访客</div></div></div>';html+='<div class="card card-pad section"><div class="section-title"><h2>报告治理记录</h2><span class="hint">草稿链接不可对外访问</span></div>'+(rows.length?'<div class="table-wrap"><table class="table"><thead><tr><th>报告/客户</th><th>模型/数据</th><th>状态</th><th>查看</th><th>有效期</th><th>操作</th></tr></thead><tbody>'+rows.map(r=>{const [status,badgeClass]=statusMeta(r),live=status==='分享中',blockers=r.publish_blockers||[];let ops='';if(r.status==='draft')ops=blockers.length?'<button class="btn btn-soft" onclick="showRouteReportBlockers(\''+E(r.id)+'\')">查看'+blockers.length+'项阻断</button>':'<button class="btn btn-primary" onclick="submitRouteReport(\''+E(r.id)+'\')">提交审核</button>'+(data.can_approve?'<button class="btn btn-soft" onclick="adminPublishRouteReport(\''+E(r.id)+'\',true)">审核并发布</button>':'');if(r.status==='pending_review')ops=data.can_approve?'<button class="btn btn-primary" onclick="adminPublishRouteReport(\''+E(r.id)+'\')">审核发布</button><button class="btn btn-soft" onclick="returnRouteReport(\''+E(r.id)+'\')">退回</button>':'<span class="muted">等待最高权限审核</span>';if(live)ops='<button class="btn btn-primary" onclick="copyReportLink(\''+E(r.share_token)+'\')">复制链接</button><button class="btn btn-soft" onclick="openReportLink(\''+E(r.share_token)+'\')">打开</button><button class="btn btn-soft" onclick="revokeRouteReport(\''+E(r.id)+'\')">撤销</button><button class="btn btn-soft" onclick="rotateRouteReport(\''+E(r.id)+'\')">更换链接</button>';if(status==='已过期')ops='<button class="btn btn-primary" onclick="renewRouteReport(\''+E(r.id)+'\')">续期30天</button>';return '<tr><td><b>'+E(r.report_number)+'</b> '+(r.is_demo?'<i class="demo-pill">演示</i>':'')+'<div>'+E(r.client_name)+'</div><div class="muted">'+E(r.title)+'</div></td><td><b>'+E(r.model_version)+'</b><div class="muted">就绪指数 '+E(r.readiness_index??'历史')+' · '+E(r.route_count)+'条路线</div></td><td><span class="badge '+badgeClass+'">'+status+'</span>'+(r.requires_code?'<div class="muted">访问密码</div>':'')+(r.review_notes?'<div class="muted report-status-note">'+E(r.review_notes)+'</div>':'')+'</td><td><b>'+E(r.unique_view_count||0)+'人 / '+E(r.view_count||0)+'次</b><div class="muted">最近 '+E(D(r.last_viewed_at))+'</div></td><td>'+E(D(r.expires_at))+'</td><td><div class="actions">'+ops+'</div></td></tr>'}).join('')+'</tbody></table></div>':'<div class="empty">尚未生成客户报告。</div>')+'</div>';document.querySelector('#view').innerHTML=html;window.__routeReportRows=rows}catch(e){document.querySelector('#view').innerHTML=pageHead('客户报告中心','加载失败')+'<div class="notice warn">'+E(e.message)+'</div>'}}
  ;
  window.showRouteReportBlockers=id=>{const r=(window.__routeReportRows||[]).find(x=>x.id===id),xs=r?.publish_blockers||[];modal('发布阻断项','<div class="notice warn">当前草稿不能提交审核，请补齐数据后重新生成报告。</div><ul class="report-blockers">'+xs.map(x=>'<li>'+E(x)+'</li>').join('')+'</ul>'+(reportCanApprove?'<div class="actions"><button class="btn btn-primary" onclick="openRouteSegmentEditor()">维护9段链路</button></div>':'') )};
  window.copyReportLink=t=>copyText(linkFor(t),'客户报告链接已复制');window.openReportLink=t=>window.open(linkFor(t),'_blank','noopener');
  window.revokeRouteReport=async id=>{if(!confirm('撤销后客户原链接将立即失效，确认撤销？'))return;try{await reportApi({action:'revoke',report_id:id});toast('报告链接已撤销');reportCenterView()}catch(e){toast(e.message)}};window.renewRouteReport=async id=>{try{const r=await reportApi({action:'renew',report_id:id,expiry_days:30});await copyText(linkFor(r.share_token),'已续期30天，链接已复制');reportCenterView()}catch(e){toast(e.message)}};window.rotateRouteReport=async id=>{if(!confirm('更换后原链接立即失效，确认生成新链接？'))return;try{const r=await reportApi({action:'rotate',report_id:id});await copyText(linkFor(r.share_token),'新链接已生成并复制');reportCenterView()}catch(e){toast(e.message)}};
  window.openRouteSegmentEditor=async function(){try{modal('9段全链路数据维护','<div class="notice">正在读取路线与分段数据…</div>');const data=await reportApi({action:'segment_catalog'}),routes=[...(data.current_routes||[]),...(data.jiangmen_routes||[])];if(!routes.length)return modal('暂无路线','<div class="notice warn">请先建立端到端路线基准。</div>');window.__segmentCatalog=data;modal('9段全链路数据维护','<div class="notice good"><b>强制对账：</b>9段 P50、P90、成本合计必须与路线总值一致；P90不得小于P50；每段必须填写可公开来源。</div><div class="field" style="margin-top:10px"><label>选择路线</label><select id="segmentRouteSelect">'+routes.map(r=>'<option value="'+E(r.kind+'|'+r.id)+'">'+E(routeLabel(r,r.kind==='jiangmen'))+(r.is_demo?'【演示】':'')+'</option>').join('')+'</select></div><div id="segmentEditorBody"></div>');document.querySelector('#segmentRouteSelect').onchange=renderSegmentEditor;renderSegmentEditor()}catch(e){modal('链路数据加载失败','<div class="notice warn">'+E(e.message)+'</div>')}};
  window.renderSegmentEditor=function(){const data=window.__segmentCatalog,key=document.querySelector('#segmentRouteSelect').value,[kind,id]=key.split('|'),route=[...(data.current_routes||[]),...(data.jiangmen_routes||[])].find(r=>r.id===id&&r.kind===kind),existing=(data.segments||[]).filter(s=>s.route_kind===kind&&s.route_id===id).sort((a,b)=>a.segment_order-b.segment_order),byCode=Object.fromEntries(existing.map(x=>[x.segment_code,x]));document.querySelector('#segmentEditorBody').innerHTML='<div class="notice" id="segmentTotals">路线目标：P50 '+E(route.p50_days??'-')+'天 · P90 '+E(route.p90_days??'-')+'天 · TCO '+E(route.total_cost_per_container??'-')+' '+E(route.currency||'CNY')+'</div><div class="table-wrap segment-editor"><table class="table"><thead><tr><th># / 环节</th><th>起点</th><th>终点</th><th>P50天</th><th>P90天</th><th>成本</th><th>冷链风险</th><th>证据</th><th>样本</th><th>公开来源</th><th>来源网址</th><th>更新时间</th></tr></thead><tbody>'+data.segment_codes.map(def=>{const s=byCode[def.code]||{};return '<tr class="segment-row" data-code="'+E(def.code)+'"><td><b>'+E(def.order)+'. '+E(def.label)+'</b></td><td><input class="wide" data-field="origin" value="'+E(s.origin_node||'')+'"></td><td><input class="wide" data-field="destination" value="'+E(s.destination_node||'')+'"></td><td><input type="number" min="0" step="0.01" data-field="p50_days" value="'+E(s.p50_days??'')+'" required></td><td><input type="number" min="0" step="0.01" data-field="p90_days" value="'+E(s.p90_days??'')+'" required></td><td><input type="number" min="0" step="0.01" data-field="cost_amount" value="'+E(s.cost_amount??'')+'" required></td><td><input type="number" min="0" max="10" step="0.1" data-field="coldchain_risk_index" value="'+E(s.coldchain_risk_index??'')+'" required></td><td><select data-field="evidence_level">'+['A','B','C','D'].map(x=>'<option '+((s.evidence_level||'C')===x?'selected':'')+'>'+x+'</option>').join('')+'</select></td><td><input type="number" min="0" step="1" data-field="sample_size" value="'+E(s.sample_size??0)+'"></td><td><input class="source" data-field="public_source_text" value="'+E(s.public_source_text||'')+'" required></td><td><input class="source" type="url" data-field="public_source_url" value="'+E(s.public_source_url||'')+'"></td><td><input type="date" data-field="source_updated_at" value="'+E((s.source_updated_at||new Date().toISOString()).slice(0,10))+'"></td></tr>'}).join('')+'</tbody></table></div><div class="actions" style="margin-top:12px"><button class="btn btn-primary" id="saveSegmentsBtn">保存并执行三项对账</button><button class="btn btn-soft" onclick="closeModal()">取消</button></div>';document.querySelectorAll('.segment-row input').forEach(x=>x.addEventListener('input',updateSegmentTotals));document.querySelector('#saveSegmentsBtn').onclick=saveSegmentEditor;updateSegmentTotals()};
  function segmentRows(){return Array.from(document.querySelectorAll('.segment-row')).map((row,i)=>{const val=f=>row.querySelector('[data-field="'+f+'"]').value;return {code:row.dataset.code,name:window.__segmentCatalog.segment_codes[i].label,origin:val('origin'),destination:val('destination'),p50_days:val('p50_days'),p90_days:val('p90_days'),cost_amount:val('cost_amount'),coldchain_risk_index:val('coldchain_risk_index'),evidence_level:val('evidence_level'),sample_size:val('sample_size'),public_source_text:val('public_source_text'),public_source_url:val('public_source_url'),source_updated_at:val('source_updated_at')}})}
  window.updateSegmentTotals=function(){const rows=segmentRows(),sum=k=>rows.reduce((a,s)=>a+(Number(s[k])||0),0),box=document.querySelector('#segmentTotals');if(box)box.innerHTML=box.innerHTML.split('<br>')[0]+'<br><b>当前合计：</b>P50 '+sum('p50_days').toFixed(2)+'天 · P90 '+sum('p90_days').toFixed(2)+'天 · 成本 '+sum('cost_amount').toFixed(2)};
  window.saveSegmentEditor=async function(){const [kind,id]=document.querySelector('#segmentRouteSelect').value.split('|'),b=document.querySelector('#saveSegmentsBtn');b.disabled=true;b.textContent='正在校验并保存…';try{const r=await reportApi({action:'save_segments',route_kind:kind,route_id:id,segments:segmentRows()});toast('9段链路已保存并通过对账');reportOptionsCache=null;closeModal()}catch(e){toast(e.message);b.disabled=false;b.textContent='保存并执行三项对账'}};
  if(typeof PREV_ROUTE_VIEW==='function')window.routeModelView=async function(){await PREV_ROUTE_VIEW();const root=document.querySelector('#view');if(root&&!root.querySelector('#reportLaunch')){const x=document.createElement('div');x.id='reportLaunch';x.className='notice good';x.style.margin='12px 0';x.innerHTML='<b>客户决策报告 V2：</b>同口径路线、9段全链路、模型版本、审核发布和安全分享。 <button class="btn btn-primary" style="margin-left:8px" onclick="openRouteReportGenerator()">生成报告草稿</button><button class="btn btn-soft" style="margin-left:6px" onclick="render(\'reportCenter\')">报告中心</button>'+(reportCanApprove?'<button class="btn btn-soft" style="margin-left:6px" onclick="openRouteSegmentEditor()">维护9段链路</button>':'');root.insertBefore(x,root.children[1]||root.firstChild)}};
  window.render=async function(v){if(v==='reportCenter')return reportCenterView();return PREV_RENDER(v)};if(typeof PREV_TOOLS==='function')window.mobileTools=function(){PREV_TOOLS();setTimeout(()=>{const g=document.querySelector('#modalRoot .tool-grid');if(g&&!g.querySelector('[data-r103-view="reportCenter"]')){const b=document.createElement('button');b.type='button';b.className='tool-tile';b.dataset.r103View='reportCenter';b.innerHTML='<b>▤ 客户报告</b><span>生成、审核并分享全链路报告</span>';b.onclick=()=>{closeModal();render('reportCenter')};g.appendChild(b)}},0)};if(typeof PREV_SHOW==='function')window.showApp=function(){const r=PREV_SHOW.apply(this,arguments);setTimeout(addReportNav,0);return r};[100,500,1500,3500].forEach(ms=>setTimeout(addReportNav,ms));
})();
