/* FONKON Market OS R11 - Stability Convergence Core
   Single boot / single router / single session owner / request dedupe / paged tables.
   ROUTE1 business functions are preserved from the loaded modules. */
(function(){
  'use strict';
  const CORE={version:'R11-EMBEDDED-LOCATION-MAP1',startedAt:new Date().toISOString(),booted:false,booting:false,rendering:false,queuedView:null,currentView:'dashboard',requests:{deduped:0,cacheHits:0,countOptimized:0,writes:0},performance:{bootReadyMs:null,lastRenderMs:null,renders:[]},errors:[]};
  window.FONKON_R11=CORE;
  const E=e=>String(e?.message||e?.error_description||e?.error||e||'未知错误').slice(0,360);
  const safeAll=(sel,root=document)=>Array.from((root||document).querySelectorAll(sel));
  const admin=()=>!!(profile?.role==='admin'&&profile?.active===true&&profile?.approval_status==='approved');
  const rememberError=(where,e)=>{CORE.errors.unshift({at:new Date().toISOString(),where,message:E(e)});CORE.errors=CORE.errors.slice(0,50);console.error('[FONKON R11]',where,e)};
  const SESSION_KEY='fonkon_session',LAST_VIEW_KEY='fonkon_last_view';
  function readStoredSession(){try{if(typeof window.fonkonReadStoredSession==='function')return window.fonkonReadStoredSession()}catch{}try{const raw=localStorage.getItem(SESSION_KEY);return raw?JSON.parse(raw):null}catch{return null}}
  const hasStoredSession=()=>!!readStoredSession()?.access_token;
  function writeLastView(v){try{localStorage.setItem(LAST_VIEW_KEY,v)}catch{try{sessionStorage.setItem(LAST_VIEW_KEY,v)}catch{}}}
  function readLastView(){let v='dashboard';try{v=localStorage.getItem(LAST_VIEW_KEY)||sessionStorage.getItem(LAST_VIEW_KEY)||v}catch{}return ['dashboard','customers','opportunities','visits','field','tasks','manager','competitiveness','knowledge','fuel','routeModel','team','settings','account','import','kpi','reminders','recycle'].includes(v)?v:'dashboard'}
  function clearLastView(){try{localStorage.removeItem(LAST_VIEW_KEY)}catch{}try{sessionStorage.removeItem(LAST_VIEW_KEY)}catch{}}
  function markRelease(){const tm=document.querySelector('.top-meta');if(tm)tm.remove();const b=document.querySelector('#releaseBadge');if(b)b.remove();document.title='FONKON Market OS'}
  function makeNav(view,ico,label){const b=document.createElement('button');b.type='button';b.className='nav-btn';b.dataset.view=view;b.innerHTML='<span class="ico">'+ico+'</span><span>'+label+'</span>';return b}
  function ensureNav(){const side=document.querySelector('.sidebar');if(!side)return;const managerBtn=side.querySelector('[data-view="manager"]');if(managerBtn){const s=managerBtn.querySelector('span:last-child');if(s)s.textContent='最高权限看板'}const knowledgeBtn=side.querySelector('[data-view="knowledge"]');if(knowledgeBtn){const s=knowledgeBtn.querySelector('span:last-child');if(s)s.textContent='知识中心'}const teamBtn=side.querySelector('[data-view="team"]');if(teamBtn){const s=teamBtn.querySelector('span:last-child');if(s)s.textContent='账号审批'}if(!side.querySelector('[data-view="competitiveness"]')&&managerBtn)managerBtn.insertAdjacentElement('afterend',makeNav('competitiveness','📐','江门竞争力'));if(!side.querySelector('[data-view="fuel"]')&&knowledgeBtn)knowledgeBtn.insertAdjacentElement('afterend',makeNav('fuel','⛽','数据燃料'));const fuel=side.querySelector('[data-view="fuel"]')||knowledgeBtn;if(!side.querySelector('[data-view="routeModel"]')&&fuel)fuel.insertAdjacentElement('afterend',makeNav('routeModel','🛣','路线模型'));if(!side.querySelector('[data-r11-tools-section]')){const sec=document.createElement('div');sec.className='nav-section';sec.dataset.r11ToolsSection='1';sec.textContent='工具';side.appendChild(sec)}[['account','◇','账号安全'],['import','⇧','Excel导入'],['kpi','▦','KPI目标'],['reminders','◉','提醒中心'],['recycle','♲','回收站']].forEach(([v,i,t])=>{if(!side.querySelector('[data-view="'+v+'"]'))side.appendChild(makeNav(v,i,t))});const isA=admin();safeAll('[data-view="manager"],[data-view="competitiveness"],[data-view="team"],[data-view="settings"],[data-view="recycle"]').forEach(b=>b.style.display=isA?'':'none');safeAll('.nav-btn').forEach(b=>{if(!b.dataset.r11Bound){b.dataset.r11Bound='1';b.addEventListener('click',e=>{e.preventDefault();window.render(b.dataset.view)})}});try{if(typeof window.__r11MountTools==='function')window.__r11MountTools()}catch(e){rememberError('mountTools',e)}}
  window.fonkonR11EnsureNav=ensureNav;
  const RAW_REQ=window.req||req,inflight=new Map(),cache=new Map(),now=()=>Date.now(),getMethod=opt=>String(opt?.method||'GET').toUpperCase(),ttlFor=path=>/notification_state|knowledge_submissions|market_intel/.test(path)?30000:60000;
  function clearReadCache(){cache.clear()}
  async function countOptimized(path){const p=path.replace(/([?&])limit=5000(?:&|$)/,(m,pfx)=>pfx+'limit=1&');const h={apikey:K,'content-type':'application/json',Prefer:'count=exact'};if(ses?.access_token)h.authorization='Bearer '+ses.access_token;const r=await fetch(U+p,{headers:h,cache:'no-store'}),text=await r.text();if(!r.ok){let j;try{j=JSON.parse(text)}catch{j={}}throw Error(j?.message||j?.error||('HTTP '+r.status))}const cr=r.headers.get('content-range')||'',m=cr.match(/\/(\d+|\*)$/),n=m&&m[1]!=='*'?Number(m[1]):(text?1:0);CORE.requests.countOptimized++;return {length:Number.isFinite(n)?n:0}}
  async function r11Req(path,opt={}){const method=getMethod(opt),key=method+' '+path;if(method!=='GET'){CORE.requests.writes++;clearReadCache();try{window.fonkonR11InvalidatePaging?.()}catch{}return RAW_REQ(path,opt)}if(/(?:\?|&)select=id(?:&|$)/.test(path)&&/(?:\?|&)limit=5000(?:&|$)/.test(path))return countOptimized(path);const c=cache.get(key);if(c&&c.expires>now()){CORE.requests.cacheHits++;return c.value}if(inflight.has(key)){CORE.requests.deduped++;return inflight.get(key)}const p=Promise.resolve(RAW_REQ(path,opt)).then(v=>{cache.set(key,{value:v,expires:now()+ttlFor(path)});return v}).finally(()=>inflight.delete(key));inflight.set(key,p);return p}
  try{req=r11Req;window.req=r11Req}catch{}window.fonkonClearReadCache=clearReadCache;
  const RAW_REFRESH=window.refreshAll||refreshAll;let refreshFlight=null;async function r11RefreshAll(force=false){if(refreshFlight)return refreshFlight;if(force)clearReadCache();refreshFlight=Promise.resolve(RAW_REFRESH()).finally(()=>{refreshFlight=null});return refreshFlight}try{refreshAll=r11RefreshAll;window.refreshAll=r11RefreshAll}catch{}
  const RAW_BELL=window.updateReminderBell;let bellFlight=null,bellAt=0;if(typeof RAW_BELL==='function')window.updateReminderBell=async function(force=false){if(!force&&Date.now()-bellAt<30000)return;if(bellFlight)return bellFlight;bellFlight=Promise.resolve(RAW_BELL()).then(x=>{bellAt=Date.now();return x}).finally(()=>bellFlight=null);return bellFlight};
  const PAGE_SIZE=50,pageState={customers:1,opportunities:1};
  function pager(kind,total){const pages=Math.max(1,Math.ceil(total/PAGE_SIZE)),p=Math.min(pageState[kind],pages);pageState[kind]=p;return '<div class="actions" style="justify-content:flex-end;margin-top:10px;align-items:center"><span class="muted">共 '+total+' 条 · 第 '+p+'/'+pages+' 页 · 每页 '+PAGE_SIZE+' 条</span><button class="btn btn-soft" '+(p<=1?'disabled':'')+' onclick="fonkonR11Page(\''+kind+'\',-1)">上一页</button><button class="btn btn-soft" '+(p>=pages?'disabled':'')+' onclick="fonkonR11Page(\''+kind+'\',1)">下一页</button></div>'}
  window.fonkonR11Page=function(kind,delta){pageState[kind]=Math.max(1,(pageState[kind]||1)+delta);if(kind==='customers')window.renderCustomerTable();else window.renderOppTable()};
  const RAW_CUSTOMER_TABLE=window.renderCustomerTable;window.renderCustomerTable=function(){const root=document.querySelector('#customerTable');if(!root||typeof RAW_CUSTOMER_TABLE!=='function')return RAW_CUSTOMER_TABLE?.();try{const q=(document.querySelector('#cq')?.value||'').toLowerCase(),g=document.querySelector('#cg')?.value||'',t=document.querySelector('#ct')?.value||'',cc=document.querySelector('#cc')?.value||'',rows=(customers||[]).filter(c=>(!q||[c.company_name,c.contact_name,c.primary_fruit,c.market_name,c.current_ports].join(' ').toLowerCase().includes(q))&&(!g||c.cv_grade===g)&&(!t||c.customer_type===t)&&(!cc||c.primary_cargo_group===cc)),pages=Math.max(1,Math.ceil(rows.length/PAGE_SIZE));pageState.customers=Math.min(pageState.customers,pages);const start=(pageState.customers-1)*PAGE_SIZE,viewRows=rows.slice(start,start+PAGE_SIZE);root.innerHTML='<div class="table-wrap"><table class="table"><thead><tr><th>客户</th><th>角色/决策</th><th>水果</th><th>年柜量</th><th>CV</th><th>DC</th><th>市场/口岸</th><th>下次跟进</th><th>负责人</th></tr></thead><tbody>'+viewRows.map(c=>'<tr><td><div class="link" onclick="openCustomer(\''+c.id+'\')">'+esc(c.company_name)+'</div><div class="muted">'+esc(c.contact_name||'')+' '+esc(c.phone||'')+'</div></td><td>'+esc(c.customer_type||'-')+'<div class="muted">'+esc(c.decision_role||'待确认')+' / DP '+fmt(c.decision_power)+'</div></td><td>'+esc(c.primary_fruit||c.core_fruits||'-')+' '+(c.primary_cargo_group?'<span class="chip">'+esc(c.primary_cargo_group)+'</span>':'')+'</td><td>'+fmt(c.annual_containers)+'</td><td>'+badge(c.cv_grade)+' <b>'+fmt(c.cv_score)+'</b></td><td><b>'+fmt(c.dc_score)+'%</b>'+progress(c.dc_score)+'</td><td>'+esc(c.market_name||'-')+'<div class="muted">'+esc(c.current_ports||'-')+'</div></td><td>'+dt(c.next_followup_at)+'</td><td>'+esc(ownerName(c.owner_id))+'</td></tr>').join('')+'</tbody></table></div>'+pager('customers',rows.length)}catch(e){rememberError('customer-pagination',e);return RAW_CUSTOMER_TABLE()}};
  const RAW_OPP_TABLE=window.renderOppTable;window.renderOppTable=function(){const root=document.querySelector('#oppTable');if(!root||typeof RAW_OPP_TABLE!=='function')return RAW_OPP_TABLE?.();try{const q=(document.querySelector('#oq')?.value||'').toLowerCase(),g=document.querySelector('#og')?.value||'',a=document.querySelector('#oa')?.value||'',s=document.querySelector('#os')?.value||'',arr=(opportunities||[]).filter(o=>(!q||[customerName(o.customer_id),o.fruit,o.current_port,o.origin_country].join(' ').toLowerCase().includes(q))&&(!g||o.cargo_group===g)&&(!a||o.alert_level===a)&&(!s||o.stage===s)),pages=Math.max(1,Math.ceil(arr.length/PAGE_SIZE));pageState.opportunities=Math.min(pageState.opportunities,pages);const start=(pageState.opportunities-1)*PAGE_SIZE,rows=arr.slice(start,start+PAGE_SIZE),F=(v,d=1)=>v===null||v===undefined||v===''?'-':Number(v).toLocaleString('zh-CN',{maximumFractionDigits:d}),MONEY=v=>v===null||v===undefined||v===''?'待补':'¥'+Number(v).toLocaleString('zh-CN',{maximumFractionDigits:2});root.innerHTML='<div class="table-wrap"><table class="table"><thead><tr><th>客户/水果</th><th>阶段</th><th>JSOI</th><th>JGI</th><th>可信度</th><th>P50/P90差</th><th>ΔTCO</th><th>优先分</th><th>建议/操作</th></tr></thead><tbody>'+rows.map(o=>'<tr><td><span class="link" onclick="openCustomer(\''+o.customer_id+'\')">'+esc(customerName(o.customer_id))+'</span><div><b>'+esc(o.fruit)+'</b> · '+esc(o.current_transport||'')+'</div><div class="muted">'+esc(o.origin_country||'')+' '+esc(o.origin_region||'')+' → '+esc(o.current_port||'-')+' → '+esc(o.destination_market||'未填终点')+'</div></td><td><span class="stage">'+esc(o.stage)+'</span><div style="margin-top:6px">'+badge(o.alert_level)+'</div></td><td><b>'+F(o.jsoi_score)+'</b><div class="muted">统一切换指数</div></td><td><b>'+F(o.jgi_score)+'</b><div class="muted">江门短板</div></td><td>'+F(o.comparison_confidence,0)+'%</td><td>P50 '+F(o.delta_p50_days)+'天<div class="muted">P90 '+F(o.delta_p90_days)+'天</div></td><td>'+MONEY(o.delta_tco)+'</td><td><b>'+F(o.final_priority)+'</b></td><td>'+esc(o.client_recommendation||o.recommendation||'-')+'<div class="muted">短板：'+esc(o.jiangmen_gaps||'待补')+'</div><div style="margin-top:7px"><button class="btn btn-primary" onclick="routeCompareModal(\''+o.id+'\',false)">路线对比</button> <button class="btn btn-soft" onclick="routeCompareModal(\''+o.id+'\',true)">客户版</button> <button class="btn btn-soft" onclick="opportunityModal(\''+o.id+'\')">编辑</button></div></td></tr>').join('')+'</tbody></table></div>'+pager('opportunities',arr.length)}catch(e){rememberError('opportunity-pagination',e);return RAW_OPP_TABLE()}};
  document.addEventListener('input',e=>{if(['cq','cg','ct','cc'].includes(e.target?.id))pageState.customers=1;if(['oq','og','oa','os'].includes(e.target?.id))pageState.opportunities=1},true);
  const LEGACY_RENDER=window.render;
  function routeFunction(v){const m={dashboard:()=>typeof todayDashboard==='function'?todayDashboard():LEGACY_RENDER('dashboard'),customers:()=>{pageState.customers=1;return typeof customersView==='function'?customersView():LEGACY_RENDER('customers')},opportunities:()=>{pageState.opportunities=1;return typeof oppsView==='function'?oppsView():LEGACY_RENDER('opportunities')},visits:()=>typeof visitsView==='function'?visitsView():LEGACY_RENDER('visits'),field:()=>typeof window.fieldView==='function'?window.fieldView():LEGACY_RENDER('dashboard'),tasks:()=>typeof tasksView==='function'?tasksView():LEGACY_RENDER('tasks'),manager:()=>typeof managerView==='function'?managerView():LEGACY_RENDER('manager'),competitiveness:()=>typeof window.competitivenessView==='function'?window.competitivenessView():LEGACY_RENDER('competitiveness'),knowledge:()=>typeof window.knowledgeCenterView==='function'?window.knowledgeCenterView():typeof knowledgeView==='function'?knowledgeView():LEGACY_RENDER('knowledge'),fuel:()=>typeof window.fuelView==='function'?window.fuelView():LEGACY_RENDER('fuel'),routeModel:()=>typeof window.routeModelView==='function'?window.routeModelView():LEGACY_RENDER('routeModel'),team:()=>typeof teamView==='function'?teamView():LEGACY_RENDER('team'),settings:()=>typeof settingsView==='function'?settingsView():LEGACY_RENDER('settings'),account:()=>typeof window.accountView==='function'?window.accountView():LEGACY_RENDER('account'),import:()=>typeof window.importView==='function'?window.importView():LEGACY_RENDER('import'),kpi:()=>typeof window.kpiView==='function'?window.kpiView():LEGACY_RENDER('kpi'),reminders:()=>typeof window.remindersView==='function'?window.remindersView():LEGACY_RENDER('reminders'),recycle:()=>typeof window.recycleView==='function'?window.recycleView():LEGACY_RENDER('recycle')};return m[v]||(()=>LEGACY_RENDER(v))}
  function setNavSafe(v){safeAll('.nav-btn,.bottom-nav button').forEach(b=>b.classList.toggle('active',b.dataset.view===v))}try{setNav=setNavSafe;window.setNav=setNavSafe}catch{}
  async function renderOne(v){if(typeof window.fonkonEnsureProfile==='function')await window.fonkonEnsureProfile(false);ensureNav();if(!admin()&&['manager','competitiveness','team','settings','recycle'].includes(v))v='dashboard';CORE.currentView=v;writeLastView(v);setNavSafe(v);return await Promise.resolve(routeFunction(v)())}
  async function r11Render(v='dashboard'){CORE.queuedView=v;if(CORE.rendering)return;CORE.rendering=true;try{while(CORE.queuedView){const next=CORE.queuedView,started=performance.now();CORE.queuedView=null;try{await renderOne(next)}catch(e){rememberError('render:'+next,e);const box=document.querySelector('#view');if(box)box.innerHTML=pageHead('页面加载失败','R11已阻止错误扩散。')+'<div class="card card-pad"><div class="danger-note"><b>'+esc(E(e))+'</b></div><button class="btn btn-primary" style="margin-top:10px" onclick="render(\''+next+'\')">重试</button></div>'}finally{const ms=Math.round(performance.now()-started);CORE.performance.lastRenderMs=ms;CORE.performance.renders.unshift({view:CORE.currentView,ms,at:new Date().toISOString()});CORE.performance.renders=CORE.performance.renders.slice(0,30);document.documentElement.dataset.fonkonView=CORE.currentView;document.documentElement.dataset.fonkonRenderMs=String(ms)}}}finally{CORE.rendering=false}}try{render=r11Render;window.render=r11Render}catch{}
  function updateIdentity(){const who=document.querySelector('#who');if(who&&profile)who.innerHTML='<b>'+esc(profile.name||profile.email||ses?.user?.email||'当前账号')+'</b> · '+(profile.role==='admin'?'最高权限':'业务员')}
  const FAST_BOOT_VIEWS=new Set(['field','customers','opportunities','knowledge','fuel','routeModel','account','import','kpi','reminders']);
  let businessSyncFlight=null,businessSyncTimer=null;
  function queueBusinessSync(view=CORE.currentView,delay=500){clearTimeout(businessSyncTimer);businessSyncTimer=setTimeout(()=>{businessSyncTimer=null;if(CORE.rendering){queueBusinessSync(view,250);return}if(businessSyncFlight)return;document.documentElement.dataset.fonkonSync='running';businessSyncFlight=Promise.resolve(window.refreshAll()).then(()=>{document.documentElement.dataset.fonkonSync='ready';if(CORE.currentView===view&&['dashboard','visits','tasks','manager','competitiveness'].includes(view))return r11Render(view)}).catch(e=>{document.documentElement.dataset.fonkonSync='degraded';rememberError('background-data',e)}).finally(()=>{businessSyncFlight=null})},Math.max(0,delay))}
  window.fonkonR11QueueBusinessRefresh=queueBusinessSync;
  let resumeFlight=null,resumeTimer=null,resumeAttempts=0,backGuardArmed=false,lastBackAt=0;
  const terminal=e=>typeof window.fonkonIsTerminalAuthError==='function'&&window.fonkonIsTerminalAuthError(e);
  function showLogin(message=''){const auth=document.querySelector('#auth'),app=document.querySelector('#app');auth?.classList.remove('hide');app?.classList.add('hide');const m=document.querySelector('#authMsg');if(m&&message)m.textContent=message}
  function showRecoveryShell(message='正在恢复登录状态…'){const auth=document.querySelector('#auth'),app=document.querySelector('#app'),box=document.querySelector('#view');auth?.classList.add('hide');app?.classList.remove('hide');ensureNav();markRelease();if(box)box.innerHTML=pageHead('正在恢复登录状态','刷新、返回或切回手机浏览器后会自动续接。')+'<div class="card card-pad"><div class="muted">'+esc(message)+'</div><div class="muted" style="margin-top:8px">登录信息仍然保留，请不要重复输入密码。</div><button class="btn btn-primary" style="margin-top:12px" onclick="fonkonR11Resume(true)">立即重新连接</button></div>'}
  function armBackGuard(){if(backGuardArmed||!hasStoredSession())return;try{const base=history.state&&typeof history.state==='object'?history.state:{};history.replaceState(Object.assign({},base,{fonkonBase:true}),'',location.href);history.pushState({fonkonGuard:true},'',location.href);backGuardArmed=true}catch{}}
  function scheduleResume(){if(resumeTimer||document.visibilityState==='hidden'||!hasStoredSession())return;const delay=Math.min(30000,1200*Math.pow(2,Math.min(resumeAttempts++,4)));resumeTimer=setTimeout(()=>{resumeTimer=null;resume(false)},delay)}
  async function resume(force=false){if(resumeFlight)return resumeFlight;showRecoveryShell(force?'正在重新连接…':'正在恢复登录状态…');resumeFlight=(async()=>{if(typeof window.fonkonEnsureProfile==='function'){if(typeof window.fonkonEnsureSession==='function')await window.fonkonEnsureSession(false);await window.fonkonEnsureProfile(force)}else{const saved=readStoredSession();if(!saved)throw Error('没有可恢复的登录会话');ses=saved;await loadProfile()}if(!profile)throw Error('账号档案暂时未加载');if(!profile.active||profile.approval_status==='rejected'){try{clearSession()}catch{}const z=Error('账号已停用或未通过审批');z.fonkonTerminalAuth=true;throw z}resumeAttempts=0;const auth=document.querySelector('#auth'),app=document.querySelector('#app'),target=readLastView();auth?.classList.add('hide');app?.classList.remove('hide');updateIdentity();ensureNav();markRelease();armBackGuard();if(FAST_BOOT_VIEWS.has(target)){await r11Render(target);queueBusinessSync(target,400)}else{try{await window.refreshAll()}catch(e){rememberError('resume-data',e)}await r11Render(target)}return true})().catch(e=>{rememberError('resume-session',e);if(terminal(e)||!hasStoredSession()){try{clearSession()}catch{}showLogin('登录状态已失效，请重新登录一次。');return false}showRecoveryShell(E(e));scheduleResume();return false}).finally(()=>{resumeFlight=null});return resumeFlight}
  window.fonkonR11Resume=resume;
  window.showApp=function(){const auth=document.querySelector('#auth'),app=document.querySelector('#app');if(!ses||!profile){if(hasStoredSession()){showRecoveryShell();scheduleResume()}else showLogin();return}if(!profile.active||profile.approval_status==='rejected'){try{clearSession()}catch{}showLogin('账号当前不可用，请联系最高权限。');return}auth?.classList.add('hide');app?.classList.remove('hide');updateIdentity();ensureNav();markRelease();armBackGuard();if(!CORE.booting&&!CORE.rendering)window.render(readLastView())};
  const logout=document.querySelector('#logout');if(logout)logout.onclick=()=>{try{clearSession();clearLastView()}finally{CORE.booted=false;backGuardArmed=false;showLogin()}};
  async function boot(){if(CORE.booted||CORE.booting)return;CORE.booting=true;try{markRelease();ensureNav();if(hasStoredSession())await resume(false);else showLogin();CORE.booted=true}finally{CORE.booting=false;markRelease();const started=Number(window.__FONKON_R11_LOADER?.startedAt||Date.now());CORE.performance.bootReadyMs=Math.max(0,Date.now()-started);document.documentElement.dataset.fonkonBootMs=String(CORE.performance.bootReadyMs);document.documentElement.dataset.fonkonBoot='ready'}}
  window.fonkonR11Boot=boot;
  window.addEventListener('popstate',()=>{if(!backGuardArmed||!hasStoredSession())return;const t=Date.now();if(t-lastBackAt<1800){backGuardArmed=false;history.back();return}lastBackAt=t;try{history.pushState({fonkonGuard:true},'',location.href)}catch{}if(CORE.currentView!=='dashboard')r11Render('dashboard');try{toast('已保持登录；再次返回可退出系统')}catch{}});
  const lifecycleResume=()=>{if(document.visibilityState!=='hidden'&&hasStoredSession()&&!profile)resume(false)};
  window.addEventListener('pageshow',lifecycleResume);window.addEventListener('online',lifecycleResume);document.addEventListener('visibilitychange',()=>{if(document.visibilityState==='visible')lifecycleResume()});
  window.fonkonR11SelfCheck=async function(){const funcs=['render','showApp','customerModal','opportunityModal','visitModal','taskModal','accountView','importView','kpiView','remindersView','knowledgeCenterView','managerView','teamView','settingsView','fuelView','routeModelView','competitivenessView','routeCompareModal'],rows=funcs.map(n=>[n,typeof window[n]==='function'||typeof globalThis[n]==='function']),api=[];for(const [n,p] of [['客户','/rest/v1/customers?select=id&limit=1'],['商机','/rest/v1/opportunities?select=id&limit=1'],['路线','/rest/v1/jiangmen_routes?select=id&limit=1'],['Fuel','/rest/v1/opportunity_fuel_observations?select=id&limit=1']]){try{await window.req(p);api.push([n,true])}catch(e){api.push([n,false,E(e)])}}return {version:CORE.version,booted:CORE.booted,currentView:CORE.currentView,functions:rows,api,requests:{...CORE.requests},errors:CORE.errors.slice(0,10)}};
  Promise.resolve().then(boot).catch(e=>{rememberError('boot',e);try{window.showApp()}catch{}});
})();
