/* FONKON Stage3 E: native CSV importer, XLSX optional */
(function(){
let rows=[];
const cMap={'公司名称':'company_name','联系人':'contact_name','手机/微信':'phone','客户类型':'customer_type','真实决策权':'decision_role','年进口柜量':'annual_containers','月均柜量':'monthly_containers','东货/西货':'primary_cargo_group','核心水果':'primary_fruit','来源国':'primary_country','产区':'primary_region','当前运输':'transport_mode','当前口岸':'current_ports','批发市场/办公楼':'market_name','市场区域':'market_area','楼栋/楼层':'building_floor','档口/办公室':'stall_office','痛点发生频率':'pain_frequency','异常损失/柜（元）':'loss_per_container','主动寻找解决方案':'actively_seeking_solution','付费事实':'payment_willingness','客户行为':'cooperation_signal','行业影响力':'influence_level','下次跟进时间':'next_followup_at','客户原话/备注':'notes'};
const oMap={'客户公司':'customer_company','水果':'fruit','东货/西货':'cargo_group','来源国':'origin_country','产区':'origin_region','业务月份':'season_month_num','当前口岸':'current_port','当前运输':'current_transport','需求事实':'need_status','项目时间':'planned_timeframe','客户行动':'action_signal','竞争窗口':'competition_window','商机阶段':'stage','总进口量/柜':'total_volume','可匹配量/柜':'matchable_volume','可切换量/柜':'switchable_volume','愿尝试量/柜':'willing_volume','首批试柜量/柜':'trial_volume','当前总时效/天':'current_total_days','当前排队小时':'current_queue_hours','当前综合成本/柜':'current_cost_per_container','查验比例%':'inspection_rate','送检比例%':'testing_rate','查验耗时/天':'inspection_days','送检耗时/天':'testing_days','江门总时效/天':'jiangmen_total_days','江门综合成本/柜':'jiangmen_cost_per_container','计划试柜日期':'target_trial_date','成交柜量':'won_volume'};
function parseCSV(text){text=text.replace(/^\ufeff/,'');const out=[],row=[];let cur='',q=false;for(let i=0;i<text.length;i++){const ch=text[i];if(q){if(ch==='"'&&text[i+1]==='"'){cur+='"';i++}else if(ch==='"')q=false;else cur+=ch}else{if(ch==='"')q=true;else if(ch===','){row.push(cur);cur=''}else if(ch==='\n'){row.push(cur);out.push(row.splice(0));cur=''}else if(ch!=='\r')cur+=ch}}row.push(cur);if(row.some(x=>x!==''))out.push(row);if(out.length<2)return[];const h=out[0].map(x=>x.trim());return out.slice(1).filter(r=>r.some(x=>String(x).trim()!=='')).map(r=>Object.fromEntries(h.map((k,i)=>[k,r[i]??''])))}
async function parseFile(f){if(/\.csv$/i.test(f.name))return parseCSV(await f.text());if(!window.XLSX){await new Promise((res,rej)=>{const s=document.createElement('script');s.src='https://cdn.jsdelivr.net/npm/xlsx@0.18.5/dist/xlsx.full.min.js';s.onload=res;s.onerror=()=>rej(Error('当前网络无法加载Excel组件。请在Excel中另存为CSV后再导入，CSV无需外部组件。'));document.head.appendChild(s)})}const wb=XLSX.read(await f.arrayBuffer(),{type:'array',cellDates:true}),ws=wb.Sheets[wb.SheetNames[0]];return XLSX.utils.sheet_to_json(ws,{defval:''})}
function cv(r,map){const o={};Object.entries(map).forEach(([cn,en])=>{const v=r[cn];if(v!==undefined&&v!==null&&String(v).trim()!=='')o[en]=v});return o}function nn(v){if(v===''||v==null)return null;const x=Number(v);return Number.isFinite(x)?x:null}function bv(v){return ['是','true','TRUE','1','yes','YES'].includes(String(v).trim())}
function preview(a,kind){const ks=Object.keys(kind==='customer'?cMap:oMap).slice(0,8);return '<div class="table-wrap"><table class="table"><thead><tr>'+ks.map(k=>'<th>'+esc(k)+'</th>').join('')+'</tr></thead><tbody>'+a.slice(0,10).map(r=>'<tr>'+ks.map(k=>'<td>'+esc(r[k]||'')+'</td>').join('')+'</tr>').join('')+'</tbody></table></div><div class="muted">共 '+a.length+' 行，预览前10行。</div>'}
window.importView=function(){const ownerOpts=profile.role==='admin'?(profiles||[]).filter(p=>p.role==='sales'&&p.active&&p.approval_status==='approved').map(p=>'<option value="'+p.id+'">'+esc(p.name||p.email)+'</option>').join(''):'';$('#view').innerHTML=pageHead('Excel批量导入','CSV可离线解析；XLSX/XLS需要Excel组件。数据库负责去重和自动评分。')+'<div class="card card-pad"><div class="grid"><div class="field span4"><label>类型</label><select id="niKind"><option value="customer">客户</option><option value="opportunity">商机</option></select></div>'+(profile.role==='admin'?'<div class="field span4"><label>客户归属</label><select id="niOwner"><option value="'+ses.user.id+'">最高权限本人</option>'+ownerOpts+'</select></div>':'')+'<div class="field span4"><label>文件</label><input id="niFile" type="file" accept=".csv,.xlsx,.xls"></div><div class="span12 stage3-toolbar"><button class="btn btn-soft" onclick="downloadImportTemplate(document.querySelector(\'#niKind\').value)">下载CSV模板</button><button id="niParse" class="btn btn-primary">读取并预览</button></div></div><div id="niPreview" class="section"></div><div id="niResult"></div></div>';$('#niParse').onclick=async()=>{const f=$('#niFile').files[0];if(!f)return toast('请选择文件');try{rows=await parseFile(f);if(!rows.length)throw Error('文件没有可导入数据');$('#niPreview').innerHTML=preview(rows,$('#niKind').value)+'<div style="margin-top:12px"><button class="btn btn-primary" onclick="nativeBulkImport()">确认导入 '+rows.length+' 行</button></div>'}catch(e){toast(e.message)}}};
window.nativeBulkImport=async function(){const kind=$('#niKind').value,owner=profile.role==='admin'?($('#niOwner')?.value||ses.user.id):ses.user.id;let ok=0,skip=0,err=[];if(!confirm('确认导入 '+rows.length+' 行？'))return;$('#niResult').innerHTML='<div class="notice">正在导入…</div>';for(let i=0;i<rows.length;i++){try{if(kind==='customer'){const x=cv(rows[i],cMap);if(!x.company_name){skip++;continue}['annual_containers','monthly_containers','loss_per_container'].forEach(k=>{if(k in x)x[k]=nn(x[k])});if('actively_seeking_solution'in x)x.actively_seeking_solution=bv(x.actively_seeking_solution);x.core_fruits=x.primary_fruit||null;x.origin_countries=x.primary_country||null;x.created_by=ses.user.id;x.owner_id=owner;await req('/rest/v1/customers',{method:'POST',headers:{Prefer:'return=minimal'},body:JSON.stringify(x)});ok++}else{const x=cv(rows[i],oMap);const c=customers.find(c=>String(c.company_name).trim().toLowerCase()===String(x.customer_company||'').trim().toLowerCase());if(!c||!x.fruit){skip++;err.push('第'+(i+2)+'行：客户不存在或水果为空');continue}delete x.customer_company;x.customer_id=c.id;x.created_by=ses.user.id;['season_month_num','total_volume','matchable_volume','switchable_volume','willing_volume','trial_volume','current_total_days','current_queue_hours','current_cost_per_container','inspection_rate','testing_rate','inspection_days','testing_days','jiangmen_total_days','jiangmen_cost_per_container','won_volume'].forEach(k=>{if(k in x)x[k]=nn(x[k])});await req('/rest/v1/opportunities',{method:'POST',headers:{Prefer:'return=minimal'},body:JSON.stringify(x)});ok++}}catch(e){skip++;err.push('第'+(i+2)+'行：'+e.message)}}await refreshAll();$('#niResult').innerHTML='<div class="notice good"><b>完成：</b>成功 '+ok+'，跳过/失败 '+skip+'</div>'+(err.length?'<div class="muted" style="white-space:pre-wrap;margin-top:8px">'+esc(err.slice(0,50).join('\n'))+'</div>':'')};
const r0=render;render=function(v){if(v==='import')return importView();return r0(v)};
})();