untry].join(' ').toLowerCase().includes(q))&&(!g||o.cargo_group===g)&&(!a||o.alert_level===a)&&(!s||o.stage===s));$('#oppTable').innerHTML='<div class="table-wrap"><table class="table"><thead><tr><th>客户/水果</th><th>阶段</th><th>窗口</th><th>时效差</th><th>OT</th><th>EPI</th><th>LSOI/PSOI</th><th>AQ</th><th>优先分</th><th>建议</th></tr></thead><tbody>'+arr.map(o=>{const sw=o.cargo_group==='东货'?o.lsoi_score:o.psoi_score;const td=(num(o.current_total_days)-num(o.jiangmen_total_days)).toFixed(1);return '<tr><td><span class="link" onclick="openCustomer(\''+o.customer_id+'\')">'+esc(customerName(o.customer_id))+'</span><div><b>'+esc(o.fruit)+'</b> · '+esc(o.cargo_group||'')+'</div><div class="muted">'+esc(o.origin_country||'')+' '+esc(o.origin_region||'')+' → '+esc(o.current_port||'-')+'</div></td><td><span class="stage">'+esc(o.stage)+'</span><div style="margin-top:6px">'+badge(o.alert_level)+'</div></td><td>'+(o.season_window_days===0?'<b style="color:#0f766e">正在高峰</b>':o.season_window_days!=null?'<b>'+o.season_window_days+'天</b>':'-')+'<div class="muted">'+esc(o.season_status||'')+'</div></td><td><b>'+td+'天</b><div class="muted">当前 '+fmt(o.current_total_days)+' / 江门 '+fmt(o.jiangmen_total_days)+'</div></td><td>'+fmt(o.ot_score)+'</td><td>'+fmt(o.epi_score)+'</td><td><b>'+fmt(sw)+'</b></td><td>'+fmt(o.switchable_volume)+'柜<div class="muted">试柜 '+fmt(o.trial_volume)+'</div></td><td><b>'+fmt(o.final_priority)+'</b></td><td>'+esc(o.recommendation||'-')+'<div class="muted">缺：'+esc(o.missing_fields||'无')+'</div><div style="margin-top:6px"><button class="btn btn-soft" onclick="opportunityModal(\''+o.id+'\')">编辑</button></div></td></tr>'}).join('')+'</tbody></table></div>'}
function visitsView(){let html=pageHead('15分钟拜访','第一次只采决定“是否继续”的最小数据；第二次只补系统缺项。','<button class="btn btn-primary" onclick="visitModal()">✎ 记录拜访</button>');html+='<div class="notice">标准节奏：0–2分钟身份｜2–5分钟货｜5–8分钟路｜8–11分钟风险｜11–13分钟假设验证｜13–15分钟机会判断。超过20分钟必须复盘是否出现无效沟通。</div><div class="card card-pad section"><div class="section-title"><h2>最近拜访</h2><span class="hint">客户原话、假设、验证、下一步全部留痕</span></div><div class="timeline">'+visits.slice(0,30).map(v=>'<div class="timeline-item"><div class="timeline-date">'+dt(v.visited_at)+' · '+esc(v.visit_type||'拜访')+' · '+fmt(v.duration_minutes)+'分钟</div><div class="timeline-text"><b class="link" onclick="openCustomer(\''+v.customer_id+'\')">'+esc(customerName(v.customer_id))+'</b><br>事实：'+esc(v.facts||'-')+'<br>痛点：'+esc(v.pain_points||'-')+'<br>客户原话：'+esc(v.customer_quote||'-')+'<br>下一步：'+esc(v.next_action||'-')+'</div></div>').join('')+'</div></div>';$('#view').innerHTML=html}
function tasksView(){const arr=tasks.filter(t=>t.status==='待办');$('#view').innerHTML=pageHead('跟进任务','每个重点客户必须有明确负责人、时间和下一动作。','<button class="btn btn-primary" onclick="taskModal()">＋ 新任务</button>')+'<div class="card card-pad">'+taskCards(arr)+'</div>'}
function managerView(){if(!isManager()){$('#view').innerHTML=pageHead('主管看板','仅主管/管理员可查看团队数据。')+'<div class="card empty">当前账号无主管权限</div>';return}const stages=['待验证','需求确认','方案','报价','试柜','持续合作'];const funnel=stages.map(s=>({s,n:opportunities.filter(o=>o.stage===s).length}));const reps=profiles.filter(p=>p.role==='sales'||p.role==='manager').map(p=>{const cs=customers.filter(c=>c.owner_id===p.id),ops=opportunities.filter(o=>cs.some(c=>c.id===o.customer_id)),vs=visits.filter(v=>v.created_by===p.id);return {id:p.id,name:p.name||'未命名',customers:cs.length,high:cs.filter(c=>['S','A'].includes(c.cv_grade)).length,red:ops.filter(o=>o.alert_level==='红').length,trial:ops.reduce((s,o)=>s+num(o.trial_volume),0),visits:vs.length,score:ops.reduce((s,o)=>s+num(o.final_priority),0)}}).sort((a,b)=>b.score-a.score);const fruitMap={};opportunities.forEach(o=>{const k=o.fruit||'未知';fruitMap[k]??={fruit:k,count:0,switchable:0,avg:0,sum:0};fruitMap[k].count++;fruitMap[k].switchable+=num(o.switchable_volume);fruitMap[k].sum+=num(o.final_priority)});const fruits=Object.values(fruitMap).map(x=>({...x,avg:x.count?x.sum/x.count:0})).sort((a,b)=>b.avg-a.avg).slice(0,8);let html=pageHead('主管作战看板','看漏斗，不听“感觉不错”；看业务员在哪一层掉单。','<button class="btn btn-soft" onclick="exportCustomers()">导出客户</button><button class="btn btn-soft" onclick="exportOpps()">导出商机</button>');html+='<div class="kpis"><div class="card kpi"><div class="kpi-label">本期客户</div><div class="kpi-value">'+customers.length+'</div><div class="kpi-foot">团队客户池</div></div><div class="card kpi"><div class="kpi-label">有效商机</div><div class="kpi-value">'+opportunities.filter(o=>o.stage!=='关闭').length+'</div><div class="kpi-foot">未关闭</div></div><div class="card kpi"><div class="kpi-label">试柜柜量</div><div class="kpi-value">'+fmt(opportunities.reduce((s,o)=>s+num(o.trial_volume),0))+'</div><div class="kpi-foot">首批验证</div></div><div class="card kpi"><div class="kpi-label">持续合作</div><div class="kpi-value">'+opportunities.filter(o=>o.stage==='持续合作').length+'</div><div class="kpi-foot">已进入复购</div></div></div>';html+='<div class="card card-pad"><div class="section-title"><h2>市场开发漏斗</h2><span class="hint">客户→数据→需求→方案→报价→试柜→持续合作</span></div><div class="funnel">'+funnel.map(x=>'<div class="funnel-step"><b>'+x.n+'</b><span>'+x.s+'</span></div>').join('')+'</div></div><div class="two section"><div class="card card-pad"><div class="section-title"><h2>业务员作战排行</h2><span class="hint">不是电话量，是有效商机与试柜</span></div>'+reps.map((r,i)=>'<div class="rank"><div class="rank-no">'+(i+1)+'</div><div class="rank-main"><b>'+esc(r.name)+'</b><div>'+r.customers+'客户 · '+r.high+' A/S · '+r.red+'红灯 · '+r.visits+'拜访</div></div><div class="rank-val">'+fmt(r.trial)+'柜</div></div>').join('')+'</div><div class="card card-pad"><div class="section-title"><h2>水果机会排行</h2><span class="hint">平均作战优先分</span></div>'+fruits.map((f,i)=>'<div class="rank"><div class="rank-no">'+(i+1)+'</div><div class="rank-main"><b>'+esc(f.fruit)+'</b><div>'+f.count+'个商机 · 可切换 '+fmt(f.switchable)+'柜</div></div><div class="rank-val">'+f.avg.toFixed(1)+'</div></div>').join('')+'</div></div>';$('#view').innerHTML=html}
async function knowledgeView(){const [fs,lp,sp,jr]=await Promise.all([req('/rest/v1/fruit_seasons?select=*&order=updated_at.desc&limit=200'),req('/rest/v1/land_port_stats?select=*&order=updated_at.desc&limit=200'),req('/rest/v1/sea_port_stats?select=*&order=updated_at.desc&limit=200'),req('/rest/v1/jiangmen_routes?select=*&order=updated_at.desc&limit=200')]);const tb=(arr,cols)=>'<div class="table-wrap"><table class="table"><thead><tr>'+cols.map(c=>'<th>'+c[1]+'</th>').join('')+'</tr></thead><tbody>'+arr.map(r=>'<tr>'+cols.map(c=>'<td>'+esc(r[c[0]]??'-')+'</td>').join('')+'</tr>').join('')+'</tbody></table></div>';let html=pageHead('行业基础数据库','所有自动评分都要能追溯到来源、样本数和更新时间。',isManager()?'<button class="btn btn-primary" onclick="knowledgeModal()">＋录入行业数据</button>':'');html+='<div class="card card-pad"><div class="section-title"><h2>水果产区 / 上市季 / 产量</h2></div>'+tb(fs,[['fruit','水果'],['cargo_group','东/西'],['country','国家'],['region','产区'],['peak_start_month','高峰开始'],['peak_end_month','高峰结束'],['annual_output_tons','年产量吨'],['concentration_score','集中度'],['evidence_level','证据']])+'</div><div class="card card-pad section"><div class="section-title"><h2>东货陆路口岸</h2></div>'+tb(lp,[['port_name','口岸'],['fruit','水果'],['month','月份'],['normal_clearance_days','淡季天'],['peak_clearance_days','旺季天'],['avg_queue_hours','排队小时'],['inspection_rate','查验%'],['testing_rate','送检%'],['congestion_index','PCI'],['sample_size','样本']])+'</div><div class="card card-pad section"><div class="section-title"><h2>西货海运口岸</h2></div>'+tb(sp,[['port_name','口岸'],['fruit','水果'],['month','月份'],['normal_clearance_days','正常天'],['peak_clearance_days','旺季天'],['avg_wait_hours','等待小时'],['inspection_rate','查验%'],['testing_rate','送检%'],['congestion_index','PCI'],['sample_size','样本']])+'</div><div class="card card-pad section"><div class="section-title"><h2>江门路线</h2></div>'+tb(jr,[['cargo_group','东/西'],['country','国家'],['origin_region','产区'],['origin_port','起运港'],['origin_to_hk_days','至香港'],['hk_transfer_days','香港中转'],['hk_to_jiangmen_days','驳船'],['jiangmen_clearance_days','江门通关'],['total_days','总时效'],['total_cost_per_container','总成本/柜'],['evidence_level','证据']])+'</div>';$('#view').innerHTML=html}
function teamView(){if(!isManager()){$('#view').innerHTML=pageHead('团队账号','仅主管/管理员可使用。')+'<div class="card empty">无权限</div>';return}$('#view').innerHTML=pageHead('团队账号','邀请码7天有效且一次性使用。')+'<div class="two"><div class="card card-pad"><div class="section-title"><h2>生成邀请码</h2></div><div class="grid"><div class="field span6"><label>角色</label><select id="inviteRole"><option value="sales">业务员</option><option value="manager">主管</option></select></div><div class="field span6"><label>&nbsp;</label><button id="makeInvite" class="btn btn-primary">生成邀请码</button></div><div class="span12"><div id="inviteResult" class="notice hide"></div></div></div></div><div class="card card-pad"><div class="section-title"><h2>团队成员</h2></div>'+profiles.map(p=>'<div class="rank"><div class="rank-no">'+(p.name||'?').slice(0,1)+'</div><div class="rank-main"><b>'+esc(p.name||'未命名')+'</b><div>'+roleName(p.role)+' · '+(p.active?'启用':'停用')+'</div></div></div>').join('')+'</div></div>';$('#makeInvite').onclick=async()=>{try{const x=await edge({action:'invite',role:$('#inviteRole').value});$('#inviteResult').classList.remove('hide');$('#inviteResult').textContent='邀请码：'+x.code+'（7天内有效，一次性使用）'}catch(e){toast(e.message)}}}
async function settingsView(){if(!isManager()){$('#view').innerHTML=pageHead('评分规则','仅主管/管理员可查看管理规则。')+'<div class="card empty">无权限</div>';return}const rules=await req('/rest/v1/fruit_value_rules?select=*&order=cargo_group,fruit');$('#view').innerHTML=pageHead('评分规则','业务员不打分，系统根据事实和统一规则自动计算。')+'<div class="notice good"><b>已启用客观评分：</b>V由年柜量自动计算；Q来自水果价值规则；P由痛点频率/损失/主动找方案计算；T和R由真实商机及监管复杂度反哺；M由付费事实；F由合作行为；W由客户影响力。OT同样由需求、时间、行为、决策权和竞争窗口自动计算。</div><div class="card card-pad section"><div class="section-title"><h2>水果Q评分规则</h2><span class="hint">主管可调整，业务员不可调整</span></div><div class="table-wrap"><table class="table"><thead><tr><th>水果</th><th>东/西</th><th>Q分