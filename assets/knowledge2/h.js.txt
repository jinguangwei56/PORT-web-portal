/* Knowledge Center completeness patch: access/docs + routes + unified search */
(function(){
window.knowledgeCenterView=async function(tab='fruits'){
 knowledgeTab=tab;const admin=profile?.role==='admin';
 const actions='<button class="btn btn-primary" onclick="marketIntelModal()">＋市场情报</button><button class="btn btn-soft" onclick="knowledgeSubmissionModal()">＋知识补充</button>'+(admin?'<button class="btn btn-soft" onclick="knowledgeReviewView()">审核台</button>':'');
 $('#view').innerHTML=pageHead('FONKON 水果知识中心','查标准、看情报、做拜访准备；未经审核的信息不会污染公司知识。',actions)+
 '<div class="card card-pad" style="margin-bottom:10px"><div class="filters"><input id="knowledgeSearchInput" placeholder="搜索水果 / 国家 / 产区 / 检测项目 / 口岸 / 市场情报"><button class="btn btn-primary" id="knowledgeSearchBtn">搜索全部知识</button></div></div>'+
 '<div class="k-tabs"><button data-k="fruits">水果百科</button><button data-k="origins">产区产季</button><button data-k="access">准入单证</button><button data-k="clearance">通关检测</button><button data-k="coldchain">冷链品质</button><button data-k="routes">口岸航线</button><button data-k="intel">市场情报</button><button data-k="assistant">拜访作战助手</button></div><div id="knowledgeBody"><div class="card k-empty">加载中…</div></div>';
 $$('.k-tabs button').forEach(b=>{b.classList.toggle('active',b.dataset.k===tab);b.onclick=()=>knowledgeCenterView(b.dataset.k)});$('#knowledgeSearchBtn').onclick=()=>knowledgeGlobalSearch($('#knowledgeSearchInput').value);$('#knowledgeSearchInput').onkeydown=e=>{if(e.key==='Enter')knowledgeGlobalSearch(e.target.value)};
 if(tab==='origins')return knowledgeOrigins();if(tab==='access')return knowledgeAccess();if(tab==='clearance')return knowledgeClearance();if(tab==='coldchain')return knowledgeColdchain();if(tab==='routes')return knowledgeRoutes();if(tab==='intel')return marketIntelView();if(tab==='assistant')return preVisitAssistantView();return knowledgeFruits();
}
window.knowledgeView=window.knowledgeCenterView;
window.knowledgeAccess=async function(){const a=await req('/rest/v1/fruit_access_knowledge?select=*&order=fruit,origin_country');$('#knowledgeBody').innerHTML=knowledgeSimpleTable(a,[['fruit','水果'],['origin_country','来源国'],['access_status','准入状态'],['allowed_regions','允许产区'],['orchard_registration','果园注册'],['packinghouse_registration','包装厂注册'],['phytosanitary_certificate','植物检疫证'],['protocol_requirements','议定书/特殊要求'],['evidence_level','证据']],'暂无已审核准入/单证知识。')}
window.knowledgeRoutes=async function(){const [lp,sp,jp,jr]=await Promise.all([req('/rest/v1/land_port_stats?select=*&order=updated_at.desc&limit=300'),req('/rest/v1/sea_port_stats?select=*&order=updated_at.desc&limit=300'),req('/rest/v1/jiangmen_port_stats?select=*&order=updated_at.desc&limit=300'),req('/rest/v1/jiangmen_routes?select=*&order=updated_at.desc&limit=300')]);$('#knowledgeBody').innerHTML='<div class="knowledge-block"><h3>东货陆路口岸</h3>'+knowledgeSimpleTable(lp,[['port_name','口岸'],['fruit','水果'],['month','月'],['normal_clearance_days','淡季天'],['peak_clearance_days','旺季天'],['avg_queue_hours','排队h'],['inspection_rate','查验%'],['testing_rate','送检%'],['congestion_index','PCI']],'暂无数据')+'</div><div class="knowledge-block"><h3>西货海运口岸</h3>'+knowledgeSimpleTable(sp,[['port_name','口岸'],['fruit','水果'],['month','月'],['normal_clearance_days','正常天'],['peak_clearance_days','旺季天'],['avg_wait_hours','等待h'],['inspection_rate','查验%'],['testing_rate','送检%'],['congestion_index','PCI']],'暂无数据')+'</div><div class="knowledge-block"><h3>江门口岸</h3>'+knowledgeSimpleTable(jp,[['fruit','水果'],['month','月'],['normal_clearance_days','正常天'],['peak_clearance_days','旺季天'],['avg_wait_hours','等待h'],['inspection_rate','查验%'],['testing_rate','送检%'],['congestion_index','PCI']],'暂无数据')+'</div><div class="knowledge-block"><h3>江门航线</h3>'+knowledgeSimpleTable(jr,[['cargo_group','东/西'],['country','国家'],['origin_region','产区'],['origin_port','起运港'],['total_days','总时效'],['total_cost_per_container','成本/柜'],['evidence_level','证据']],'暂无数据')+'</div>'}
window.knowledgeGlobalSearch=async function(q){q=(q||'').trim().toLowerCase();if(!q)return toast('请输入搜索关键词');$('#knowledgeBody').innerHTML='<div class="card k-empty">全库搜索中…</div>';const [fc,or,ac,cl,co,mi,ks,lp,sp,jr]=await Promise.all([req('/rest/v1/fruit_catalog?select=*'),req('/rest/v1/fruit_origin_knowledge?select=*&limit=500'),req('/rest/v1/fruit_access_knowledge?select=*&limit=500'),req('/rest/v1/fruit_clearance_knowledge?select=*&limit=500'),req('/rest/v1/fruit_coldchain_knowledge?select=*&limit=500'),req('/rest/v1/market_intel?select=*&limit=500'),req('/rest/v1/knowledge_submissions?review_status=eq.'+encodeURIComponent('已采纳')+'&select=*&limit=500'),req('/rest/v1/land_port_stats?select=*&limit=500'),req('/rest/v1/sea_port_stats?select=*&limit=500'),req('/rest/v1/jiangmen_routes?select=*&limit=500')]);const hit=(x)=>JSON.stringify(x).toLowerCase().includes(q);const groups=[['水果百科',fc.filter(hit),x=>'<b class="link" onclick="openFruitKnowledge(\''+esc(x.fruit)+'\')">'+esc(x.fruit)+'</b> · '+esc(x.cargo_group)],['产区产季',or.filter(hit),x=>'<b>'+esc(x.fruit)+'</b> · '+esc(x.country)+' '+esc(x.region||'')+' · 高峰 '+(x.peak_start_month||'-')+'–'+(x.peak_end_month||'-')+'月'],['准入单证',ac.filter(hit),x=>'<b>'+esc(x.fruit)+'</b> · '+esc(x.origin_country)+' · '+esc(x.access_status)+' · '+esc(x.protocol_requirements||x.notes||'')],['通关检测',cl.filter(hit),x=>'<b>'+esc(x.fruit)+'</b> · '+esc(x.stage)+' · '+esc(x.item_name)+' · '+esc(x.port_name||'通用')],['冷链品质',co.filter(hit),x=>'<b>'+esc(x.fruit)+'</b> · '+esc(x.origin_country||'通用')+' · '+esc(x.common_damage_risks||x.transport_notes||'')],['市场情报',mi.filter(hit),x=>'<b>'+esc(x.fruit)+'</b> · '+esc(x.market_name||x.city||'')+' · '+esc(x.finding||x.notes||'')],['共享补充',ks.filter(hit),x=>'<b>'+esc(x.fruit||'通用')+'</b> · '+esc(x.title)+' · '+esc(x.content)],['陆路口岸',lp.filter(hit),x=>'<b>'+esc(x.port_name)+'</b> · '+esc(x.fruit||'')+' · '+fmt(x.peak_clearance_days)+'天 · PCI '+fmt(x.congestion_index)],['海运口岸',sp.filter(hit),x=>'<b>'+esc(x.port_name)+'</b> · '+esc(x.fruit||'')+' · '+fmt(x.peak_clearance_days)+'天 · PCI '+fmt(x.congestion_index)],['江门航线',jr.filter(hit),x=>'<b>'+esc(x.country)+' '+esc(x.origin_region||'')+'</b> → 江门 · '+fmt(x.total_days)+'天 · '+fmt(x.total_cost_per_container)+'元/柜']];const found=groups.reduce((n,g)=>n+g[1].length,0);$('#knowledgeBody').innerHTML='<div class="notice">关键词“<b>'+esc(q)+'</b>”共找到 <b>'+found+'</b> 条可见结果。业务员只能搜索到公司共享知识、已采纳情报和自己有权限的数据。</div>'+groups.filter(g=>g[1].length).map(g=>'<div class="knowledge-block"><h3>'+g[0]+' '+g[1].length+'</h3>'+g[1].slice(0,50).map(x=>'<div class="knowledge-row">'+g[2](x)+'</div>').join('')+'</div>').join('')+(found?'':'<div class="card k-empty">没有找到结果。可以提交“知识补充”进入审核。</div>')}
const prevOpen=window.openFruitKnowledge;window.openFruitKnowledge=async function(fruit){await prevOpen(fruit);const a=await req('/rest/v1/fruit_access_knowledge?fruit=eq.'+encodeURIComponent(fruit)+'&select=*&order=origin_country');const block='<div class="knowledge-block"><h3>📄 准入 / 注册 / 单证</h3>'+(a.length?a.map(x=>'<div class="knowledge-row"><b>'+esc(x.origin_country)+'｜'+esc(x.access_status)+'</b><div>允许产区：'+esc(x.allowed_regions||'-')+'｜果园：'+esc(x.orchard_registration||'-')+'｜包装厂：'+esc(x.packinghouse_registration||'-')+'</div><div>植物检疫证：'+esc(x.phytosanitary_certificate||'-')+'｜原产地证：'+esc(x.certificate_of_origin||'-')+'</div><div>议定书/特殊要求：'+esc(x.protocol_requirements||'-')+'</div><div class="muted">来源：'+esc(x.source_text||'-')+' · 证据 '+esc(x.evidence_level)+' · 最后核验 '+esc(x.last_verified_at||'-')+'</div></div>').join(''):'<div class="k-empty">暂无已审核准入/单证知识。</div>')+'</div>';$('#view').insertAdjacentHTML('beforeend',block);if(profile?.role==='admin')$('#view').insertAdjacentHTML('beforeend','<div class="k-actions" style="margin:10px 0"><button class="btn btn-primary" onclick="standardKnowledgeModal(\'access\',\''+esc(fruit)+'\')">＋维护准入单证</button></div>')}
const oldStd=window.standardKnowledgeModal;window.standardKnowledgeModal=function(kind,fruit){if(kind!=='access')return oldStd(kind,fruit);if(profile?.role!=='admin')return toast('仅最高权限可维护标准知识');modal('维护准入/单证｜'+fruit,'<form id="accessForm" class="grid">'+input('origin_country','来源国*','','text','span4','required')+select('access_status','准入状态',['准入','不准入','部分准入','暂停','待核验'],'待核验')+input('allowed_regions','允许产区','','text','span6')+input('orchard_registration','果园注册要求','','text','span6')+input('packinghouse_registration','包装厂注册要求','','text','span6')+input('phytosanitary_certificate','植物检疫证要求','','text','span6')+input('certificate_of_origin','原产地证要求','','text','span6')+input('labeling_requirements','标签/包装要求','','text','span6')+'<div class="field span12"><label>议定书/特殊检疫要求</label><textarea name="protocol_requirements"></textarea></div><div class="field span6"><label>冷处理等特殊要求</label><textarea name="cold_treatment_requirements"></textarea></div><div class="field span6"><label>其他单证</label><textarea name="other_documents"></textarea></div>'+input('source_text','官方/可靠来源*','','text','span6','required')+input('source_date','来源日期','','date','span3')+select('evidence_level','证据等级',['A','B','C','D','未知'],'A','span3')+input('last_verified_at','最后核验日期','','date','span3')+'<div class="span12"><button class="btn btn-primary">保存准入知识</button></div></form>');$('#accessForm').onsubmit=async e=>{e.preventDefault();try{const x=Object.fromEntries(new FormData(e.target));x.fruit=fruit;x.created_by=ses.user.id;await req('/rest/v1/fruit_access_knowledge',{method:'POST',headers:{Prefer:'return=minimal'},body:JSON.stringify(x)});closeModal();toast('准入/单证知识已保存');openFruitKnowledge(fruit)}catch(z){toast(z.message)}}}
})();