/* FONKON Stage3 B: Excel/CSV bulk import */
(function(){
let importRows=[];
const customerMap={
 '公司名称':'company_name','联系人':'contact_name','手机/微信':'phone','客户类型':'customer_type','真实决策权':'decision_role','年进口柜量':'annual_containers','月均柜量':'monthly_containers','东货/西货':'primary_cargo_group','核心水果':'primary_fruit','来源国':'primary_country','产区':'primary_region','当前运输':'transport_mode','当前口岸':'current_ports','批发市场/办公楼':'market_name','市场区域':'market_area','楼栋/楼层':'building_floor','档口/办公室':'stall_office','痛点发生频率':'pain_frequency','异常损失/柜（元）':'loss_per_container','主动寻找解决方案':'actively_seeking_solution','付费事实':'payment_willingness','客户行为':'cooperation_signal','行业影响力':'influence_level','下次跟进时间':'next_followup_at','客户原话/备注':'notes'
};
const oppMap={'客户公司':'customer_company','水果':'fruit','东货/西货':'cargo_group','来源国':'origin_country','产区':'origin_region','业务月份':'season_month_num','当前口岸':'current_port','当前运输':'current_transport','需求事实':'need_status','项目时间':'planned_timeframe','客户行动':'action_signal','竞争窗口':'competition_window','商机阶段':'stage','总进口量/柜':'total_volume','可匹配量/柜':'matchable_volume','可切换量/柜':'switchable_volume','愿尝试量/柜':'willing_volume','首批试柜量/柜':'trial_volume','当前总时效/天':'current_total_days','当前排队小时':'current_queue_hours','当前综合成本/柜':'current_cost_per_container','查验比例%':'inspection_rate','送检比例%':'testing_rate','查验耗时/天':'inspection_days','送检耗时/天':'testing_days','江门总时效/天':'jiangmen_total_days','江门综合成本/柜':'jiangmen_cost_per_container','计划试柜日期':'target_trial_date','成交柜量':'won_volume'};
function loadXLSX(){return new Promise((resolve,reject)=>{if(window.XLSX)return resolve(window.XLSX);const s=document.createElement('script');s.src='https://cdn.jsdelivr.net/npm/xlsx@0.18.5/dist/xlsx.full.min.js';s.onload=()=>resolve(window.XLSX);s.onerror=()=>reject(Error('Excel解析组件加载失败，可改用CSV格式'));document.head.appendChild(s)})}
function csvEscape(v){v=String(v??'');return /[",\n]/.test(v)?'"'+v.replace(/"/g,'""')+'"':v}
window.downloadImportTemplate=function(kind){const headers=kind==='customer'?Object.keys(customerMap):Object.keys(oppMap);const example=kind==='customer'?['示例果业','张总','13800000000','货主','老板/最终决策人','300','25','东货','龙眼','泰国','尖竹汶','陆运','磨憨','江南市场','','','','频繁/持续','3000','是','愿为确定性付费','愿意提供数据','区域重要客户','','客户原话填写这里']:['示例果业','龙眼','东货','泰国','尖竹汶','11','磨憨','陆运','明确痛点','30天内','要求方案','正在比较供应商','需求确认','300','200','80','10','2','6','18','22000','40','10','0.5','3','','23000','',''];const csv='\ufeff'+headers.map(csvEscape).join(',')+'\n'+example.map(csvEscape).join(',');const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv;charset=utf-8'}));a.download=kind==='customer'?'FONKON_客户导入模板.csv':'FONKON_商机导入模板.csv';a.click();URL.revokeObjectURL(a.href)};
function convertRow(row,map){const o={};Object.entries(map).forEach(([cn,en])=>{if(row[cn]!==undefined&&row[cn]!==null&&String(row[cn]).trim()!=='')o[en]=row[cn]});return o}
async function parseImportFile(file){const XLSX=await loadXLSX();const buf=await file.arrayBuffer();const wb=XLSX.read(buf,{type:'array',cellDates:true});const ws=wb.Sheets[wb.SheetNames[0]];return XLSX.utils.sheet_to_json(ws,{defval:''})}
function previewRows(rows,kind){const map=kind==='customer'?customerMap:oppMap,keys=Object.keys(map).slice(0,8);return '<div class="table-wrap"><table class="table"><thead><tr>'+keys.map(k=>'<th>'+esc(k)+'</th>').join('')+'</tr></thead><tbody>'+rows.slice(0,10).map(r=>'<tr>'+keys.map(k=>'<td>'+esc(r[k]??'')+'</td>').join('')+'</tr>').join('')+'</tbody></table></div><div class="muted" style="margin-top:8px">共 '+rows.length+' 行；这里只预览前10行。</div>'}
window.importView=function(){const ownerOpts=profile?.role==='admin'?(profiles||[]).filter(p=>p.role==='sales'&&p.active&&p.approval_status==='approved').map(p=>'<option value="'+p.id+'">'+esc(p.name||p.email)+'</option>').join(''):'';let html=pageHead('Excel批量导入','支持 .xlsx / .xls / .csv；导入后由数据库自动去重、自动评分。','');html+='<div class="card card-pad"><div class="grid"><div class="field span4"><label>导入类型</label><select id="importKind"><option value="customer">客户</option><option value="opportunity">商机</option></select></div>'+(profile?.role==='admin'?'<div class="field span4"><label>客户归属业务员</label><select id="importOwner"><option value="'+ses.user.id+'">最高权限本人</option>'+ownerOpts+'</select></div>':'')+'<div class="field span4"><label>Excel/CSV文件</label><input id="importFile" type="file" accept=".xlsx,.xls,.csv"></div><div class="span12 stage3-toolbar"><button class="btn btn-soft" onclick="downloadImportTemplate(document.querySelector(\'#importKind\').value)">下载模板</button><button id="parseImport" class="btn btn-primary">读取文件</button></div></div><div id="importPreview" class="section"></div><div id="importResult"></div></div>';$('#view').innerHTML=html;$('#parseImport').onclick=async()=>{const f=$('#importFile').files[0],kind=$('#importKind').value;if(!f)return toast('请选择文件');try{importRows=await parseImportFile(f);if(!importRows.length)throw Error('文件没有数据');$('#importPreview').innerHTML=previewRows(importRows,kind)+'<div style="margin-top:12px"><button class="btn btn-primary" onclick="runBulkImport()">确认导入 '+importRows.length+' 行</button></div>'}catch(e){toast(e.message)}}};
function n(v){if(v===undefined||v===null||v==='')return null;const x=Number(v);return Number.isFinite(x)?x:null}
function boolv(v){return ['是','true','TRUE','1','yes','YES'].includes(String(v).trim())}
function isoDate(v){if(!v)return null;if(v instanceof Date)return v.toISOString();const d=new Date(v);return isNaN(d)?null:d.toISOString()}
window.runBulkImport=async function(){const kind=$('#importKind').value,owner=profile?.role==='admin'?($('#importOwner')?.value||ses.user.id):ses.user.id;if(!confirm('确认导入 '+importRows.length+' 行？重复客户会跳过，不会覆盖他人数据。'))return;let ok=0,skip=0,errors=[];$('#importResult').innerHTML='<div class="notice">正在导入，请勿关闭页面…</div>';for(let i=0;i<importRows.length;i++){try{if(kind==='customer'){const x=convertRow(importRows[i],customerMap);if(!x.company_name){skip++;continue}['annual_containers','monthly_containers','loss_per_container'].forEach(k=>{if(k in x)x[k]=n(x[k])});if('actively_seeking_solution'in x)x.actively_seeking_solution=boolv(x.actively_seeking_solution);if(x.next_followup_at)x.next_followup_at=isoDate(x.next_followup_at);x.core_fruits=x.primary_fruit||null;x.origin_countries=x.primary_country||null;x.created_by=ses.user.id;x.owner_id=owner;await req('/rest/v1/customers',{method:'POST',headers:{Prefer:'return=minimal'},body:JSON.stringify(x)});ok++;}else{const x=convertRow(importRows[i],oppMap);if(!x.customer_company||!x.fruit){skip++;continue}const c=customers.find(c=>String(c.company_name).trim().toLowerCase()===String(x.customer_company).trim().toLowerCase());if(!c){skip++;errors.push('第'+(i+2)+'行：找不到客户 '+x.customer_company);continue}delete x.customer_company;x.customer_id=c.id;x.created_by=ses.user.id;['season_month_num','total_volume','matchable_volume','switchable_volume','willing_volume','trial_volume','current_total_days','current_queue_hours','current_cost_per_container','inspection_rate','testing_rate','inspection_days','testing_days','jiangmen_total_days','jiangmen_cost_per_container','won_volume'].forEach(k=>{if(k in x)x[k]=n(x[k])});if(x.target_trial_date){const d=new Date(x.target_trial_date);x.target_trial_date=isNaN(d)?null:d.toISOString().slice(0,10)}await req('/rest/v1/opportunities',{method:'POST',headers:{Prefer:'return=minimal'},body:JSON.stringify(x)});ok++;}}catch(e){skip++;errors.push('第'+(i+2)+'行：'+e.message)}}await refreshAll();$('#importResult').innerHTML='<div class="notice good"><b>导入完成</b>：成功 '+ok+' 行，跳过/失败 '+skip+' 行。</div>'+(errors.length?'<div class="card card-pad" style="margin-top:8px"><b>异常明细</b><div class="muted" style="white-space:pre-wrap;margin-top:8px">'+esc(errors.slice(0,50).join('\n'))+'</div></div>':'');};
const oldRender=render;render=function(v){if(v==='import')return importView();return oldRender(v)};
})();