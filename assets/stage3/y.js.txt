/* FONKON Market OS - Secure Route Report Center 1.0 */
(function(){
  const API=U+'/functions/v1/route-report';
  const PREV_RENDER=window.render;
  const PREV_ROUTE_VIEW=window.routeModelView;
  const PREV_TOOLS=window.mobileTools;
  const PREV_SHOW=window.showApp;
  const E=v=>esc(String(v??''));
  const D=v=>v?new Date(v).toLocaleString('zh-CN',{year:'numeric',month:'2-digit',day:'2-digit'}):'-';
  const linkFor=t=>location.origin+'/report.html?t='+encodeURIComponent(t);
  async function reportApi(body){
    if(!ses?.access_token)throw Error('请先登录');
    const r=await fetch(API,{method:'POST',headers:{apikey:K,authorization:'Bearer '+ses.access_token,'content-type':'application/json'},body:JSON.stringify(body)});
    const raw=await r.text();let j={};try{j=raw?JSON.parse(raw):{}}catch{}
    if(!r.ok)throw Error(j?.error||j?.message||'报告服务暂时不可用');return j;
  }
  async function copyText(value,message='已复制'){
    try{await navigator.clipboard.writeText(value);toast(message);return true}catch{prompt('请复制以下内容',value);return false}
  }
  function addReportNav(){
    if(document.querySelector('[data-view="reportCenter"]'))return;
    const anchor=document.querySelector('.nav-btn[data-view="routeModel"]')||document.querySelector('.nav-btn[data-view="opportunities"]');if(!anchor)return;
    const b=document.createElement('button');b.className='nav-btn';b.dataset.view='reportCenter';b.innerHTML='<span class="ico">▤</span><span>客户报告</span>';b.onclick=()=>{try{setNav('reportCenter')}catch{};reportCenterView()};anchor.insertAdjacentElement('afterend',b);
  }
  function readiness(r){const keys=['p50_days','p90_days','total_cost_per_container','stability_index','route_congestion_index','schedule_frequency_per_week','capacity_score','coldchain_risk_index','abnormal_handling_score'];const n=keys.filter(k=>r[k]!==null&&r[k]!==undefined&&r[k]!=='').length;return n+'/'+keys.length+'项'}
  function routeLabel(r,jiangmen=false){return (jiangmen?'江门｜':(r.current_port||'当前口岸')+'｜')+(r.fruit||'通用')+'｜'+(r.country||'国家待补')+(r.origin_region?' '+r.origin_region:'')+(r.month?'｜'+r.month+'月':'｜月份待补')+'｜'+readiness(r)}
  window.openRouteReportGenerator=async function(){
    try{
      modal('生成客户供应链路线报告','<div class="notice">正在读取可用路线和客户资料…</div>');
      const [cur,jm]=await Promise.all([req('/rest/v1/current_route_baselines?select=*&active=eq.true&order=updated_at.desc&limit=200'),req('/rest/v1/jiangmen_routes?select=*&order=updated_at.desc&limit=200')]);
      if(!cur.length||!jm.length){return modal('暂时不能生成报告','<div class="notice warn">至少需要1条客户当前/其他口岸路线和1条江门路线。请先在“路线模型”补充路线基准。</div>')}
      const customerOptions='<option value="">不绑定CRM客户</option>'+(customers||[]).map(c=>'<option value="'+E(c.id)+'">'+E(c.company_name)+'</option>').join('');
      const currentOptions=cur.map(r=>'<label class="report-route-option"><input type="checkbox" name="current_route_ids" value="'+E(r.id)+'"><span><b>'+E(r.current_port||'当前路线')+'</b><small>'+E(routeLabel(r,false))+'</small></span></label>').join('');
      const jmOptions=jm.map(r=>'<option value="'+E(r.id)+'">'+E(routeLabel(r,true))+'</option>').join('');
      modal('生成FONKON路线决策报告','<form id="routeReportForm" class="grid"><div class="span12 notice good"><b>报告规则：</b>数字由固定模型计算，生成后保存为快照；未知数据不按0计算。最多选择4条其他口岸路线与1条江门路线。</div><div class="field span6"><label>绑定CRM客户</label><select name="customer_id" id="reportCustomer">'+customerOptions+'</select></div><div class="field span6"><label>客户显示名称</label><input name="client_name" id="reportClientName" placeholder="例如：鑫华果业"></div><div class="field span12"><label>选择客户当前/其他口岸路线（1—4条）</label><div class="report-route-list">'+currentOptions+'</div></div><div class="field span12"><label>选择江门路线</label><select name="jiangmen_route_id" required><option value="">请选择</option>'+jmOptions+'</select></div><div class="field span6"><label>报告标题</label><input name="title" value="FONKON供应链路线决策报告" required></div><div class="field span3"><label>分享有效期</label><select name="expiry_days"><option value="7">7天</option><option value="30" selected>30天</option><option value="90">90天</option><option value="365">365天</option></select></div><div class="field span3"><label>访问密码（可选）</label><input name="access_code" minlength="4" maxlength="32" autocomplete="new-password" placeholder="至少4位"></div><div class="field span12"><label>比较口径/补充说明</label><input name="assumptions" value="同起点、同终点、同水果、同一业务月份；未知数据不按0计算。"></div><label class="field span6 report-check"><input type="checkbox" name="show_cost" checked> 客户报告显示TCO金额</label><label class="field span6 report-check"><input type="checkbox" name="show_client_name" checked> 客户报告显示客户名称</label><div class="span12 actions"><button class="btn btn-primary" id="generateReportBtn">一键生成并发布</button><button type="button" class="btn btn-soft" onclick="closeModal()">取消</button></div></form>');
      const f=document.querySelector('#routeReportForm'),customer=document.querySelector('#reportCustomer'),name=document.querySelector('#reportClientName');customer.onchange=()=>{const c=(customers||[]).find(x=>x.id===customer.value);if(c&&!name.value)name.value=c.company_name||''};
      f.onsubmit=async e=>{e.preventDefault();const fd=new FormData(f),ids=fd.getAll('current_route_ids');if(!ids.length)return toast('请选择至少一条其他口岸路线');if(ids.length>4)return toast('最多选择4条其他口岸路线');const b=document.querySelector('#generateReportBtn');b.disabled=true;b.textContent='正在生成…';try{const r=await reportApi({action:'create',customer_id:fd.get('customer_id')||null,client_name:fd.get('client_name'),current_route_ids:ids,jiangmen_route_id:fd.get('jiangmen_route_id'),title:fd.get('title'),expiry_days:Number(fd.get('expiry_days')||30),access_code:fd.get('access_code'),assumptions:fd.get('assumptions'),show_cost:fd.get('show_cost')==='on',show_client_name:fd.get('show_client_name')==='on'});showReportReady(r)}catch(z){toast(z.message);b.disabled=false;b.textContent='一键生成并发布'}};
    }catch(e){modal('报告生成器加载失败','<div class="notice warn">'+E(e.message)+'</div>')}
  };
  function showReportReady(r){const link=linkFor(r.share_token);modal('客户报告已生成','<div class="notice good"><b>报告编号 '+E(r.report_number)+'</b><br>报告已形成固定数据快照，可立即发给客户。</div><div class="security-box" style="margin-top:12px;word-break:break-all">'+E(link)+'</div><div class="muted" style="margin-top:8px">有效期至 '+E(D(r.expires_at))+(r.requires_code?' · 已启用访问密码':' · 客户无需登录')+'</div><div class="actions" style="margin-top:14px"><button class="btn btn-primary" id="copyNewReport">复制链接</button><button class="btn btn-soft" id="openNewReport">打开报告</button><button class="btn btn-soft" id="manageNewReport">报告中心</button></div>');document.querySelector('#copyNewReport').onclick=()=>copyText(link,'客户报告链接已复制');document.querySelector('#openNewReport').onclick=()=>window.open(link,'_blank','noopener');document.querySelector('#manageNewReport').onclick=()=>{closeModal();render('reportCenter')}}
  window.reportCenterView=async function(){
    try{setNav('reportCenter')}catch{};document.querySelector('#view').innerHTML=pageHead('客户报告中心','生成、分享、续期、撤销并追踪客户查看情况。','<button class="btn btn-primary" onclick="openRouteReportGenerator()">＋生成客户报告</button><button class="btn btn-soft" onclick="render(\'routeModel\')">路线模型</button>')+'<div class="card empty">正在读取报告…</div>';
    try{const data=await reportApi({action:'list'}),rows=data.reports||[];let html=pageHead('客户报告中心','客户无需登录即可打开安全链接；内部数据和利润不会进入客户报告。','<button class="btn btn-primary" onclick="openRouteReportGenerator()">＋生成客户报告</button><button class="btn btn-soft" onclick="render(\'routeModel\')">路线模型</button>');html+='<div class="kpis"><div class="card kpi"><div class="kpi-label">报告总数</div><div class="kpi-value">'+rows.length+'</div><div class="kpi-foot">当前权限范围</div></div><div class="card kpi"><div class="kpi-label">正在分享</div><div class="kpi-value">'+rows.filter(x=>x.status==='published'&&(!x.expires_at||new Date(x.expires_at)>new Date())).length+'</div><div class="kpi-foot">链接有效</div></div><div class="card kpi"><div class="kpi-label">客户查看</div><div class="kpi-value">'+rows.reduce((s,x)=>s+Number(x.view_count||0),0)+'</div><div class="kpi-foot">累计打开次数</div></div></div>';html+='<div class="card card-pad section"><div class="section-title"><h2>已生成报告</h2><span class="hint">撤销后原链接立即失效</span></div>'+(rows.length?'<div class="table-wrap"><table class="table"><thead><tr><th>报告/客户</th><th>比较范围</th><th>状态</th><th>查看</th><th>有效期</th><th>操作</th></tr></thead><tbody>'+rows.map(r=>{const expired=r.expires_at&&new Date(r.expires_at)<new Date(),live=r.status==='published'&&!expired,status=live?'分享中':expired?'已过期':'已撤销',badgeClass=live?'b-green':expired?'b-yellow':'b-gray';return '<tr><td><b>'+E(r.report_number)+'</b><div>'+E(r.client_name)+'</div><div class="muted">'+E(r.title)+'</div></td><td>'+E(r.scope?.fruit||'水果待补')+' · '+E(r.scope?.country||'国家待补')+'<div class="muted">'+E(r.route_count)+'条路线 · '+E(r.scope?.destination||'终点待补')+'</div></td><td><span class="badge '+badgeClass+'">'+status+'</span>'+(r.requires_code?'<div class="muted">访问密码</div>':'')+'</td><td><b>'+E(r.view_count||0)+'</b><div class="muted">最近 '+E(D(r.last_viewed_at))+'</div></td><td>'+E(D(r.expires_at))+'</td><td><div class="actions">'+(live?'<button class="btn btn-primary" onclick="copyReportLink(\''+E(r.share_token)+'\')">复制链接</button><button class="btn btn-soft" onclick="openReportLink(\''+E(r.share_token)+'\')">打开</button><button class="btn btn-soft" onclick="revokeRouteReport(\''+E(r.id)+'\')">撤销</button>':'<button class="btn btn-primary" onclick="renewRouteReport(\''+E(r.id)+'\')">续期30天</button>')+'<button class="btn btn-soft" onclick="rotateRouteReport(\''+E(r.id)+'\')">更换链接</button></div></td></tr>'}).join('')+'</tbody></table></div>':'<div class="empty">尚未生成客户报告。先进入路线模型选择同口径路线。</div>')+'</div>';document.querySelector('#view').innerHTML=html}catch(e){document.querySelector('#view').innerHTML=pageHead('客户报告中心','加载失败')+'<div class="notice warn">'+E(e.message)+'</div>'}
  };
  window.copyReportLink=t=>copyText(linkFor(t),'客户报告链接已复制');
  window.openReportLink=t=>window.open(linkFor(t),'_blank','noopener');
  window.revokeRouteReport=async id=>{if(!confirm('撤销后客户原链接将立即失效，确认撤销？'))return;try{await reportApi({action:'revoke',report_id:id});toast('报告链接已撤销');reportCenterView()}catch(e){toast(e.message)}};
  window.renewRouteReport=async id=>{try{const r=await reportApi({action:'renew',report_id:id,expiry_days:30});await copyText(linkFor(r.share_token),'已续期30天，链接已复制');reportCenterView()}catch(e){toast(e.message)}};
  window.rotateRouteReport=async id=>{if(!confirm('更换后原链接立即失效，确认生成新链接？'))return;try{const r=await reportApi({action:'rotate',report_id:id});await copyText(linkFor(r.share_token),'新链接已生成并复制');reportCenterView()}catch(e){toast(e.message)}};
  if(typeof PREV_ROUTE_VIEW==='function')window.routeModelView=async function(){await PREV_ROUTE_VIEW();const root=document.querySelector('#view');if(root&&!root.querySelector('#reportLaunch')){const x=document.createElement('div');x.id='reportLaunch';x.className='notice good';x.style.margin='12px 0';x.innerHTML='<b>客户决策报告：</b>选择同起点、同终点、同水果、同月份的路线，一键生成可分享链接。 <button class="btn btn-primary" style="margin-left:8px" onclick="openRouteReportGenerator()">生成对比报告</button><button class="btn btn-soft" style="margin-left:6px" onclick="render(\'reportCenter\')">报告中心</button>';root.insertBefore(x,root.children[1]||root.firstChild)}};
  window.render=async function(v){if(v==='reportCenter')return reportCenterView();return PREV_RENDER(v)};
  if(typeof PREV_TOOLS==='function')window.mobileTools=function(){PREV_TOOLS();setTimeout(()=>{const g=document.querySelector('#modalRoot .tool-grid');if(g&&!g.querySelector('[data-r102-view="reportCenter"]')){const b=document.createElement('button');b.type='button';b.className='tool-tile';b.dataset.r102View='reportCenter';b.innerHTML='<b>▤ 客户报告</b><span>生成并分享路线决策报告</span>';b.onclick=()=>{closeModal();render('reportCenter')};g.appendChild(b)}},0)};
  if(typeof PREV_SHOW==='function')window.showApp=function(){const r=PREV_SHOW.apply(this,arguments);setTimeout(addReportNav,0);return r};
  [100,500,1500,3500].forEach(ms=>setTimeout(addReportNav,ms));
})();
