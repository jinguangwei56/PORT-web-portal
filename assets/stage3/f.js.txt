/* FONKON Stage3 F: real sample statistics */
(function(){
const previousKC=window.knowledgeCenterView;
window.knowledgeSamples=async function(){try{const a=await req('/rest/v1/market_intel_port_stats?select=*&order=sample_count.desc,fruit,port_name');const body=$('#knowledgeBody');if(!body)return;body.innerHTML='<div class="notice"><b>实操统计只使用“已采纳”的市场情报。</b> 样本不足时不要把比例当成行业真实概率；建议至少累计30个同水果×同口岸有效样本后再用于强判断。</div>'+(a.length?'<div class="table-wrap"><table class="table"><thead><tr><th>水果</th><th>口岸</th><th>总样本</th><th>查验观察样本</th><th>观察查验率</th><th>送检观察样本</th><th>观察送检率</th><th>平均通关</th><th>平均排队</th><th>送检P50</th><th>送检P90</th><th>时效样本</th><th>最新样本</th></tr></thead><tbody>'+a.map(x=>'<tr><td><b>'+esc(x.fruit)+'</b></td><td>'+esc(x.port_name)+'</td><td>'+fmt(x.sample_count)+'</td><td>'+fmt(x.inspection_observed_count)+'</td><td>'+(x.observed_inspection_rate==null?'-':fmt(x.observed_inspection_rate)+'%')+'</td><td>'+fmt(x.testing_observed_count)+'</td><td>'+(x.observed_testing_rate==null?'-':fmt(x.observed_testing_rate)+'%')+'</td><td>'+(x.avg_clearance_days==null?'-':fmt(x.avg_clearance_days)+'天')+'</td><td>'+(x.avg_queue_hours==null?'-':fmt(x.avg_queue_hours)+'h')+'</td><td>'+(x.testing_p50_days==null?'-':fmt(x.testing_p50_days)+'天')+'</td><td>'+(x.testing_p90_days==null?'-':fmt(x.testing_p90_days)+'天')+'</td><td>'+fmt(x.testing_duration_samples)+'</td><td>'+dt(x.latest_sample_at)+'</td></tr>').join('')+'</tbody></table></div>':'<div class="card k-empty">暂无已采纳的口岸实操样本。业务员跑市场后在“市场情报”录入，最高权限审核采纳后会自动出现在这里。</div>')}catch(e){toast(e.message)}};
window.knowledgeCenterView=async function(tab='fruits'){if(tab==='samples'){await previousKC('fruits');const tabs=document.querySelector('.k-tabs');if(tabs&&!tabs.querySelector('[data-k="samples"]')){const b=document.createElement('button');b.dataset.k='samples';b.textContent='实操统计';b.onclick=()=>knowledgeCenterView('samples');const ref=tabs.querySelector('[data-k="assistant"]');tabs.insertBefore(b,ref||null)}$$('.k-tabs button').forEach(b=>b.classList.toggle('active',b.dataset.k==='samples'));return knowledgeSamples()}await previousKC(tab);const tabs=document.querySelector('.k-tabs');if(tabs&&!tabs.querySelector('[data-k="samples"]')){const b=document.createElement('button');b.dataset.k='samples';b.textContent='实操统计';b.onclick=()=>knowledgeCenterView('samples');const ref=tabs.querySelector('[data-k="assistant"]');tabs.insertBefore(b,ref||null)}};
window.knowledgeView=window.knowledgeCenterView;
const prevOpenFruit=window.openFruitKnowledge;window.openFruitKnowledge=async function(fruit){await prevOpenFruit(fruit);try{const a=await req('/rest/v1/market_intel_port_stats?fruit=eq.'+encodeURIComponent(fruit)+'&select=*&order=sample_count.desc');if(a.length)$('#view').insertAdjacentHTML('beforeend','<div class="knowledge-block"><h3>📊 已审核实操样本</h3>'+a.map(x=>'<div class="knowledge-row"><b>'+esc(x.port_name)+' · '+fmt(x.sample_count)+'个样本</b><div>观察查验率 '+(x.observed_inspection_rate==null?'-':fmt(x.observed_inspection_rate)+'%')+'｜观察送检率 '+(x.observed_testing_rate==null?'-':fmt(x.observed_testing_rate)+'%')+'｜通关 '+(x.avg_clearance_days==null?'-':fmt(x.avg_clearance_days)+'天')+'｜送检P50/P90 '+(x.testing_p50_days==null?'-':fmt(x.testing_p50_days))+'/'+(x.testing_p90_days==null?'-':fmt(x.testing_p90_days))+'天</div><div class="muted">注意：这是FONKON已审核样本统计，不代表海关公布的固定查验概率。</div></div>').join('')+'</div>')}catch{}};
})();